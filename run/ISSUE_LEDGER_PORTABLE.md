# Portable Issue Record

Generated by the autonomous run documented in `run/PHASE0_AUDIT.md`. This file is the tracker-independent record required by the run directive; it stands on its own if no external tracker or forge is present.

## Closed this run

| ID | Summary | Severity | Path to closure | Evidence | Commit(s) |
|---|---|---|---|---|---|
| RUN-1 | `hse-core` (144 tests) + `wasm-ui` had zero CI/local-gate coverage since the PR #509 crate split — a correctness- and supply-chain-critical entity model running fully unverified | High (verification gap on core correctness code) | (a) root-cause fix — wired both crates into the actual gate | `scripts/gate.sh --quick` 15/15 green locally; PR #544 CI: all checks green (Check&test, aarch64-android build, MSRV, clippy×2, gitleaks, install.sh, hse-core+wasm-ui, cargo-audit×2) | PR #544, merge commit `97a17a07d` |
| RUN-2 | `wasm-ui::to_js_error` threw a bare JS string instead of a real `Error`; every JS call site reading `e.message` on a caught wasm error would render `undefined` | Medium (real user-facing error-display defect, not a crash) | (a) root-cause fix — construct `js_sys::Error` | Confirmed the defect by tracing all `to_js_error` call sites and 6+ JS consumers reading `.message`; fix verified via a real `wasm32-unknown-unknown` rebuild + regenerated `wasm-bindgen` glue diff showing the new `new Error(...)` binding; PR #547 CI all green | PR #547, commits `93839299c` + `330a7297f`, merge commit `e67e0c6ba` |
| OD-20 | `ip2location` module carries no hosting/datacenter signal, unlike sibling `criminal_ip`'s `is_hosting` (pre-existing item, inherited from `.agent/state.json`) | Low-Medium | (d) disproof — the free tier this module intentionally targets genuinely lacks the field; not a code defect | Initially left open pending a live API check believed to need a credential. On reflection the module's own doc comment already showed the endpoint is keyless for its tier, so the call needed no credential at all: made it directly (`curl https://api.ip2location.io/?ip=8.8.8.8`) — response has no hosting/usage-type field. Cross-checked the vendor's public API docs: that field (`usage_type`, `DCH` value) exists but requires a paid "Starter Plan" this module (`ModuleCost::Free`, no `cost()` override) doesn't use by design. Also re-verified the ledger's own suggested heuristic-reuse path (`core::scan::classify::INFRA_DOMAINS`) and found it operates on a different data shape (domain-name suffixes, not AS/org-name strings) — not reusable as originally claimed, and building a fresh unvetted heuristic wasn't judged proportionate to a Low-Medium finding. Full reasoning in `.agent/state.json`'s OD-20 `resolution_note`, doc comment added at the module itself. | PR #550, commit `1a0050763` |

## Open

None. Every item this run's own census surfaced, and the one pre-existing open item it inherited, is now closed with cited evidence.

## Explicitly disproven / not-defects (carried forward, not re-litigated)

The pre-existing ledger (`.agent/state.json`) records ~40 additional closed defects and a comparable number of investigated-and-rejected candidates across 42 cycles, each with cited evidence (file:line, commit, test name). This run did not find cause to reopen or dispute any of them and did not duplicate that record here in full — see `.agent/state.json` and `docs/gap_register.md` directly for the complete history.

## Documentation-drift finding (not an issue, recorded for completeness)

`.agent/state.json`'s own recorded state is stale relative to ~50 commits now on `main` (PR #509's crate split, the wasm-ui view-porting series, PR #544, PR #545, PR #547). Not treated as a defect in the ledger's substance — every entry it does record remains independently verifiable — but flagged so a future cycle updates it rather than assuming it is current.
