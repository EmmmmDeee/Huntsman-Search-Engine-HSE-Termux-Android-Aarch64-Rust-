# Dev-tooling only — see docs/OSINT_MODEL_FINE_TUNING.md. Nothing here is a
# dependency of the hse/huntsman_search_engine Rust crate; none of it runs in
# scripts/gate.sh. Install on the machine doing the fine-tune, which needs a
# real GPU (see the guide's Prerequisites section for VRAM ballparks).
torch
transformers
datasets
peft
trl
bitsandbytes
accelerate
