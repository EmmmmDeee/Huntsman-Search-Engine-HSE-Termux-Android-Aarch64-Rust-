{
  "baseline_sha": "cf86bded5bedbd6584b676af058c5a946072b7fe",
  "work_branch": "claude/charming-meitner-85h3aj",
  "baseline_failures": [],
  "completed": [
    {
      "contract": "A live iteration's scan id is in flight before any client learns it (core::live)",
      "category": "DEFECT",
      "evidence": "Revert check: baseline src/core/live/mod.rs restored (SSE1-O) fails a_live_iteration_never_hands_out_a_scan_id_before_it_is_registered with 'LiveTick announced ... while the registry was locked'; record_scan alone moved above the install (SSE1-S) fails with 'the session listed ... while the registry was locked'; moving the announcement above the install (SSE1-L) is rejected by the compiler (E0425 x2); restored, it passes. Targeted lib tests 36/36. Adversarial review: no path hands out an unregistered id; its test gap (the scan-list channel) and four wording errors were fixed.",
      "fresh_worktree": "Passed. The gate (scripts/gate.sh --quick) passed in a fresh worktree of the commit, and the suite ran twice there: 8534 tests (8507 passed, 27 ignored, 0 failed) in both runs, identical test by test. Against the previous commit only the two new tests differ. Doctests: 86 (83 passed, 3 ignored) twice, identical."
    },
    {
      "contract": "No test frees a port and then relies on it staying refused (test_server::ClosedPort)",
      "category": "DEFECT",
      "evidence": "Mechanism reproduced on the kernel before the fix: under bind(0) churn from 16 threads, 5 of 20,000 bind-then-close ports accepted a connection, 0 of 20,000 held ports did. a_closed_port_refuses_and_cannot_be_taken_while_held kills SO_REUSEADDR on (CI11-R); the new portscan assertion kills a scanner that reports a failed connect as open (CI11-P). Adversarial review: all 36 other ephemeral binds keep their listener; its wording findings were fixed.",
      "fresh_worktree": "Passed. The gate (scripts/gate.sh --quick) passed in a fresh worktree of the commit, and the suite ran twice there: 8534 tests (8507 passed, 27 ignored, 0 failed) in both runs, identical test by test and identical to the previous commit's. Doctests: 86 (83 passed, 3 ignored) twice, identical."
    },
    {
      "contract": "The console's shell is SpiderFoot 4.0's: navbar, Dark Mode switch, footer tip, light default (REQ-UI-002)",
      "category": "FEATURE",
      "evidence": "Operator request: remake the UI to mimic SpiderFoot 4.0. On the unchanged baseline all three new route tests fail (no navbar markers; --bg is not #ffffff; nine icons with no rule, plus a tenth built from a template). Mutations UI2-A..G are each killed. An independent review found one blocking defect (from 768 to about 1,020px wide the bar wrapped over every page title) and six should-fix ones; each was confirmed on a real page and fixed. A Playwright run on a sandboxed hse serve passed 51 checks, from 1280px down to a 320px phone; the build before the review failed 10 of them. The all-routes hostile sweep found no page error, injection or 5xx, and the server logged no panic. Gate passed (GATE_EXIT=0) before the review fixes, whose route and SPA tests pass; wasm-ui/pkg matches a pinned regeneration.",
      "fresh_worktree": "Passed. The gate (scripts/gate.sh --quick) passed in a fresh worktree of the commit, and the suite ran twice there: 8537 tests (8510 passed, 27 ignored, 0 failed) in both runs, identical test by test. Against the previous commit only the three new route tests differ. Doctests: 86 (83 passed, 3 ignored) twice, identical."
    }
  ],
  "rejected": [
    {
      "candidate": "Production unwrap/expect/panic reachable from external input",
      "reason": "None found. All 79 production sites are constant regexes, invariants guarded a few lines above, or documented fail-fast configuration (SSL_CERT_FILE)."
    },
    {
      "candidate": "Declared feature.* settings keys that nothing reads",
      "reason": "None: every key in util::settings has a reader in core::engine."
    },
    {
      "candidate": "Swallowed store errors in archive_if_eligible and record_pathway_template",
      "reason": "Both are documented best-effort caches; a lost write costs a re-query, never a wrong answer."
    },
    {
      "candidate": "history.json cycle-9 deferred finding F22 (scan-log pill never resolves)",
      "reason": "Already fixed: src/web/js/scan_info/log.js calls onTerminal on scan_complete."
    }
  ],
  "open_defects": [
    {
      "description": "Tests use fixed ports 127.0.0.1:1 and :9 as refused endpoints and assume nothing on the machine listens there (an inetd discard service answers on 9). Not a race: both sit below the 32768-60999 ephemeral range.",
      "evidence": ":9 in src/api/mod.rs:233, src/api/tiles.rs:414, tests/common/mod.rs:716; :1 in src/modules/api_key_probe/tests.rs:197, src/util/egress/mod.rs:401, src/util/http/tests.rs:1396, src/modules/plc_directory/tests.rs:802, src/modules/wigle/tests.rs:1314, tests/query_floor_skips.rs:49"
    },
    {
      "description": "abn_lookup's transport-failure test depends on the proxy environment: curl_with_status passes no --noproxy, so with http_proxy or ALL_PROXY set and 127.0.0.1 missing from NO_PROXY the proxy answers and the test sees Ok(None).",
      "evidence": "src/modules/abn_lookup/fetch.rs curl arguments (no --noproxy); adversarial review of change 2"
    },
    {
      "description": "keyed_cascade_gives_up_cleanly_on_401_with_no_extra_pooled_key cannot fail on the retry it guards against: its server accepts once and drops the listener, so an in-place retry is refused and never counted.",
      "evidence": "src/util/http/tests.rs:1289-1325"
    },
    {
      "description": "key_pool backup_and_fresh ignores a failed rename: its warning says the pool was backed up, and the next save_pool replaces the unreadable pool file.",
      "evidence": "src/util/key_pool/persistence.rs:50-54 (let _ = std::fs::rename); reachable when the .json.bak path exists as a directory"
    }
  ],
  "cycle": 3
}
