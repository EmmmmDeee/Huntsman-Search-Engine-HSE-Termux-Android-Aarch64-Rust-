# Rust build artefacts
/target/

# Generated delivery package (git archive of the tree — circular to commit)
/huntsman_refactored.zip

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Runtime database / keys (HSE writes to $HOME, not repo, but be safe)
*.db
*.db-journal
*.db-wal
*.db-shm
.huntsman.env
.claude/worktrees/
.claude/settings.local.json

# Generated investigation output — scan results and dossiers contain
# third-party PII and must never be committed (they belong in $HOME, not
# the repo). See docs/FAULT_TREE_ANALYSIS.md (finding E2.1). This blocks
# RECURRENCE only; already-tracked files must be removed explicitly.
scan_*.json
*.scan.json
DOSSIER_*.md
*.dossier.md
