target

# wasm-bindgen's TypeScript declarations — regenerated by every build
# alongside the .js glue and .wasm binary (which ARE committed, since
# src/api/routes/mod.rs embeds them), but unused by this plain-JS project.
pkg/*.d.ts
