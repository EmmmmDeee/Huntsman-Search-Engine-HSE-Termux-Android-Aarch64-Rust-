//! Ports of `src/web/js/scan_info/*.js` — the scan-detail tab views. One
//! module per JS file, named identically, so the mapping between the two
//! trees stays obvious as more are ported.

pub mod benchmark;
pub mod communities;
pub mod duplicates;
pub mod gaps;
pub mod identities;
pub mod location;
pub mod metrics;
pub mod trust;
