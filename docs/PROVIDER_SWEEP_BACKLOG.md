# Provider sweep backlog — round 3 (Pass 17 discovery)

One finder per chunk read all 134 provider modules no earlier pass had covered,
against a six-point checklist derived from the defect classes already fixed on
this branch. Findings then went to 3-lens adversarial verification.

**Verification was cut short by a model usage limit**, not by the findings: 9 were
verified unanimously, 1 was refuted, and 43 never reached a verifier. The 43 are
recorded here as UNVERIFIED — they are leads, not findings, and none may be acted
on or reported as a defect until it has been re-derived from the source. Most
restate the class this branch has already fixed eight times (a failed lookup
reading as a clean negative), which is a reason to check them, not to believe them.

Directive framing: a module that reports "found nothing" when it actually failed
violates ABSENCE OF EASY EVIDENCE ≠ ABSENCE OF A NEXUS; the identity-attribution
leads violate IDENTIFIER MATCH ≠ ENTITY IDENTITY. `core::claim::Support::Failed`
is the type that makes the first class impossible to express once modules adopt it.

## Confirmed (3/3 adversarial verification) — actionable

| # | Module | Finding |
|---|---|---|
| 0 | `src/modules/asic_business_names/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** `ckan_query` returns CKAN's own `r.total` as `server_total`, and `is_truncated(server_total, records.len())` drives the `truncated` tag and `total_matches`. |
| 1 | `src/modules/acma_rrl/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** The body is read with `read_body_capped_or_fail`: a mid-body transport failure is the module's error, never an empty page parsed to "no licence". |
| 2 | `src/modules/ahpra/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** The body is read with `read_body_capped_or_fail`: a mid-body transport failure is the module's error, never "not a registered practitioner". |
| 4 | `src/modules/au_property/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** au_property: `run_leg` returned `LegOutcome::Ok` after `read_body_capped` yielded `None`. The body is read with `read_body_capped_or_fail`; a failure is `LegOutcome::Unreachable` and nothing parsed from a partial body is kept. Loopback test: a body whose Content-Length promises more than arrives → `Unreachable`. See REQ-AUPROP-001. |
| 5 | `src/modules/auspost/mod.rs` | **FIXED against the published contract (2026-09-15); live decode UNVERIFIED (no key here).** auspost: the struct expected a bare `localities` list with a `locality` string and a string postcode; Australia Post documents `localities.locality[]` with `location` and a numeric postcode (a lone object for one match, `""` for none). A tolerant deserializer now accepts every documented variant and the pre-fix spelling, and rejects any other shape as a decode failure. See REQ-AUSPOST-001. |
| 6 | `src/modules/bitcoin/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** bitcoin: the `?` on the `/txs` call errored the module after `/address` had answered, discarding the ledger reading. `lookup` keeps the reading and stamps the anchor's evidence with the failed co-spend lookup (never a silent "no co-spends"); a failed ledger call is the error, Esplora's 404 the clean negative. Loopback tests. See REQ-BITCOIN-001. |
| 7 | `src/modules/austlii/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** `ok_or_absent(SRC, resp, &[])` on the fixed `sinosrch.cgi` path: a 404 is the endpoint gone, not a clean miss (the module's own comment records the reasoning). |
| 8 | `src/modules/austlii/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** The body is read with `read_body_capped_or_fail`; a mid-body transport failure is the module's error. |
| 9 | `src/modules/chess_profile/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** chess_profile: both lookups swallowed every 429 / 5xx / transport failure / open breaker into an empty batch, so an outage on both platforms was "no Chess.com and no Lichess account". Each lookup returns `Result<Vec<Entity>>` (`chesscom_lookup` / `lichess_lookup`, endpoint as a parameter, driven against a loopback: hit / 404 / 503) and `combine` keeps either platform's evidence but makes a failure with nothing found the module's error (`or_hard_failure`). Locked; falsified. |

## Refuted by verification — do not act

| # | Module | Claim |
|---|---|---|
| 3 | `src/modules/au_property/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** au_property: NSW's retired `maps.six.nsw.gov.au` 308-redirects to the SDT Explorer SPA on another host; reqwest followed it, the SPA shell parsed to nothing and the leg was tallied `Ok` — "register consulted, no records". A 2xx whose final host differs from the requested host is now `LegOutcome::Migrated` (`landed_off_host`), a dead endpoint in `leg_failure`'s verdict. Loopback test: a 308 to another host → `Migrated`. See REQ-AUPROP-001. |

## Unverified leads (43) — re-derive before believing

| # | Module | Lead |
|---|---|---|
| 10 | `src/modules/dns_axfr/mod.rs` | **VERIFIED (narrowed) AND FIXED (2026-09-15).** dns_axfr: the NS-enumeration stage already failed closed; the probe stage still read a set of nameservers that could not be reached (TCP/53 filtered, connect / read timeout, unresolvable glue) exactly as a set that refused — an empty result, "no zone-transfer exposure". Nameservers that answered, could not be reached, and were skipped as private are now counted apart; `sweep_verdict` makes any unreached nameserver the module's error naming it, all-answered the clean negative, and nothing-probed a typed `NotApplicable` skip. Locked; falsified. |
| 11 | `src/modules/data_gov_au/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** data_gov_au: `fetch_json_or_404` mapped a 404 on CKAN's `package_search` to "no matching agency", yet CKAN signals zero matches with a 200 `count: 0` and the header records that a 404 means the path is gone. `package_search(client, api_base, query)` goes through `fetch_json` (every non-2xx fails) and rejects `success: false`; loopback-locked (404 → error; success=false → error; count 0 → the miss); falsified. |
| 12 | `src/modules/comb_search/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** comb_search: the request went through `fetch_json_or_404`, so a 404 (the endpoint moved, a WAF page) became `Ok(empty)` — a clean-negative breach claim about the named subject. The query now goes through `fetch_json` (every non-2xx is a failed lookup; the endpoint signals a miss in the body of a 200), behind `query_comb(client, endpoint, value)` so the real request path is exercised against a loopback server: 404 → error, 503 → error, `{count:0, lines:[]}` → the genuine miss. |
| 13 | `src/modules/comb_search/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** comb_search: for a Username seed the match is on the exact local part of strangers' addresses (`john@gmail.com`, `john@yahoo.com` — every `john` in the compilation), yet the seed's own Username entity came back tagged `breach` with "N leaked credential line(s)" — the tag is load-bearing downstream (breach-sector enrichment, AU-061, lead triage). The secrets stay candidate-quarantined leads; the Username seed is never enriched or tagged from those lines (an emitted copy would merge with the confirmed seed and lose a candidate tag). Locked by `a_username_seed_is_never_tagged_breach_from_strangers_same_local_part_lines`; falsified. |
| 14 | `src/modules/crossref_search/mod.rs` | **VERIFIED AND FIXED (2026-09-15; observed live).** crossref_search: the request was the all-fields `query=`, so `query=Ada Lovelace` returned "Introduction to the Ada Lovelace Symposium" (Alexander Wolf) and "Ada Lovelace lives forever" (Betty Toole) — works ABOUT the name, minted as the subject's. The query is now `query.author=` for a name / `query.affiliation=` for an organisation (`build_query`), `author[]` (given / family / affiliation names) and `title` are decoded, and a work is emitted only when `attribution` ties it to the seed — an author whose family name and given name or initial match (Western and family-first orders, diacritic-folded), or an affiliation naming the organisation. The evidence names the matched author / affiliation and the title. Locked and falsified. |
| 15 | `src/modules/crates_io/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** crates_io: the `?` on the second (crate-listing) call discarded the account the first call had confirmed — handle, real name, GitHub pivot — into a bare ModuleError. `expand_crates` is best-effort: a listing failure keeps the confirmed entities and writes `crates_listing: failed` onto the Username's evidence. Loopback-locked (503 keeps everything and says so; a listing that answers is expanded); falsified. |
| 16 | `src/modules/device_sensors/wifi.rs` | **VERIFIED AND FIXED (2026-09-15).** device_sensors/wifi.rs and its sibling signal_radar/wifi.rs: `rssi_dbm=0` (the strongest possible signal), `frequency_mhz=0`, `link_speed_mbps=0`, `timestamp=0` and an SSID of `<hidden>` were asserted whenever the tool omitted the field. Absent readings are now omitted (the `filter_map`/`fold` shape `device_fix::parse_fix` uses) and an unreported SSID is "(SSID not reported)", never a hidden-network claim. Locked in both modules; falsified. |
| 17 | `src/modules/fofa/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** fofa: the HTTP-200 `error: true` envelope (dead key, unpaid plan, exhausted quota) was a warn line and an empty result, and the key pool never learnt of it. `envelope_failure` classifies the envelope; a key/quota-shaped message is reported to the pool (`note_keyed_error`) and every envelope is the module's error. Locked; falsified. Live shape unverified (no key here). |
| 18 | `src/modules/gaming_profile/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** gaming_profile: both lookups returned an empty batch on any transport/HTTP/decode failure, so an outage on either platform read as "no such account". Each lookup now returns `Result<Vec<Entity>>` (a non-2xx or undecodable resolver answer is the platform's error; only a resolver answer naming no match, or Mojang's documented 404, is the miss) and `combine` applies `ModuleResult::or_hard_failure`: a failure is kept, a partial outage never discards a confirmed account, and a failure with nothing found is the module's error. Loopback tests drive both real request paths (503/500 → error; `{data:[]}`/404 → miss) and the combination policy is pinned. |
| 19 | `src/modules/doh_resolver/mod.rs` | **REFUTED on re-derivation (2026-09-15):** `process` gates on `status().is_success()` before `json_decode` (mod.rs ~913); a Cloudflare 400 is a module error, not a negative. Already fixed on `main`. |
| 20 | `src/modules/exa_search/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** exa_search: the body sent `num_results` / `use_autoprompt` / `contents.text.max_characters` and read `published_date` — none of them Exa's documented names (`numResults`, `useAutoprompt`, `maxCharacters`, `publishedDate`), so the API applied its defaults and the `published` attribute was dead. `request_body` sends the documented names; the response field is renamed with the old spelling as an alias. Locked; falsified. Live decode unverified (no key here). |
| 21 | `src/modules/europepmc_search/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** europepmc_search: `ok_or_absent(.., &[404])` on a fixed REST endpoint — a 404 is the endpoint gone, never "no publications by this author". Every non-2xx is now the failed lookup (`search(client, endpoint, query)`); a loopback test drives the real request path: 404 → error, 500 → error, `hitCount: 0` → the genuine miss. |
| 22 | `src/modules/greynoise/mod.rs` | **MECHANISM VERIFIED AND FIXED (2026-09-15); the schema claim itself remains unsettled.** greynoise: every `PaidResp` field was defaulted, so a body of any other shape (a nested `internet_scanner_intelligence` envelope, a renamed field) decoded to an all-false record that the no-findings gate read as "never observed" — a clean negative on the keyed path only, with no error or log. `seen` is now `Option<bool>` and `recognised` makes its absence a failed lookup. Whether the live v3 body is flat (as the crate's own key probe and fixtures say) or nested cannot be settled here (no key); either way a drift now surfaces as an error. Locked; falsified. |
| 23 | `src/modules/github_commits/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** github_commits: `if !status.is_success() { return Ok(empty) }` — a 5xx, a 401 on a revoked token and a 403/429 throttle all read as "no commits by this author". `search_commits(ctx, api_base, email)` now classifies: a throttle (429, or a 403 that names the rate limit — `github_api::throttled`, shared with github_code_search) is the typed `Error::RateLimited`, anything else the module's error; a configured token is still reported to the key pool. Loopback test: 500 → error, 403 "API rate limit exceeded" → RateLimited, 429 → RateLimited, 401 → error, `total_count: 0` → the genuine miss. |
| 24 | `src/modules/launchpad_user/mod.rs` | **VERIFIED AND FIXED (2026-09-15; observed live).** launchpad_user: people and teams share the `~name` namespace and resource shape; `~ubuntu-desktop` answers `is_team: true` / `resource_type_link …/#team`, `~mvo` `false` / `#person`. `LpPerson` decodes `is_team` and a team yields no entities (no person owns the handle — the honest clean negative) instead of a confirmed 0.85 personal account, a Person from the team's display name, the description's emails and a country tag from its time zone. Locked against the live shape; falsified. |
| 25 | `src/modules/ip_registry/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** ip_registry's BGPView upstream (`api.bgpview.io`) no longer resolves — confirmed here (no DNS; proxy CONNECT 502) and by the 2026-09-14 live sweep. The BGPView IP half is removed (`ripestat`'s `network-info` is the announcing-ASN / covering-prefix authority) and the ASN path reads RDAP `autnum` via ARIN's root (redirected to the authoritative RIR), parsed from the live RIR answers captured 2026-09-15; the `bgpview` module is retired outright. See REQ-BGP-001. |
| 26 | `src/modules/osintcat/mod.rs` | **REFUTED on re-derivation (2026-09-15):** all three data endpoints accumulate `hard_failure` and the module closes with `result.or_hard_failure(hard_failure)` (mod.rs ~191–246); the credits preflight is fail-closed. The parenthetical was stale. |
| 27 | `src/modules/overpass/mod.rs` | **REFUTED on re-derivation (2026-09-15):** an HTTP-200 `remark` runtime error is surfaced as "Overpass returned a runtime error, not an empty area" (mod.rs ~48-56). Already fixed on `main`. |
| 28 | `src/modules/mastodon_user/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** mastodon_user: an instance failure was dropped without a log and a partial sweep (1 answered, 9 failed with 429s) still returned the clean negative "no presence on any major instance". Failures are logged and collected; `sweep_verdict` makes any unanswered instance the module's error naming it (the total outage stays an error; cancellation stays the caller's decision). Locked; falsified. |
| 29 | `src/modules/mylnikov/mod.rs` | **VERIFIED AND FIXED (2026-09-15; observed live).** mylnikov: every in-body `result` other than 200 — the 404 miss, a 400 rejected query (`"desc":"Empty or bad search query"`, captured live), a block, a backend error — was "BSSID not located". `classify` keeps only 404 as the miss, decodes `data` only on a 200 (an error body's `{}` or string `data` no longer breaks the decode) and fails every other code with the provider's `desc`. Locked on the live bodies; falsified. |
| 30 | `src/modules/plc_directory/resolve.rs` | **REFUTED on re-derivation (2026-09-15):** `resolve::audit_log` returns `fetch_json_or_404`'s `Result<Option<_>>` and the call site propagates it with `?` (resolve.rs ~121–130, mod.rs ~256); only a real 404 is the clean miss. Already fixed on `main`. |
| 31 | `src/modules/ripestat/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** ripestat: every RIPEstat sub-fetch error was `.ok()`'d and the empty result returned without `or_hard_failure` — a total outage read as a clean negative (reproduced offline through the real code path with a refused-proxy client). Each endpoint stays best-effort, but an answer built from no endpoint is now the module failure `or_hard_failure` exists for. |
| 32 | `src/modules/qld_cadastre/mod.rs` | **REFUTED on re-derivation (2026-09-15):** `features_or_error` turns an HTTP-200 `{"error":{…}}` envelope into a module error (mod.rs ~69–81, applied at ~267); a non-2xx, a 429 after one retry and a non-JSON body are all closed. Already fixed on `main`. |
| 33 | `src/modules/pypi_user/mod.rs` | **VERIFIED AND FIXED (2026-09-15; observed live).** pypi_user: an XML-RPC `<fault>` (rate limit, disabled API, unknown method — captured live: HTTP 200, `faultCode -32601`) carries one `<string>`, which the pair parser zipped to nothing and reported as "owns no packages". `xmlrpc_fault` detects the fault before parsing and the lookup fails with the `faultString`. Locked on the live body; falsified. |
| 34 | `src/modules/phone_geo/data.rs` | **VERIFIED AND FIXED (2026-09-15).** phone_geo: area-code pass geolocated a bare national number with no international marker — reproduced end to end with the built binary (`hse scan -k phone -v "817-555-1234" -m phone_geo -d 0` → Address "Kyoto" at 0.58). `process` now gates both passes on `phone_intl::international_digits`, the predicate its offline siblings already use; a number without a marker is a typed `NotApplicable` skip. |
| 35 | `src/modules/sanctions_ofac/list.rs` | **VERIFIED AND FIXED (2026-09-15).** sanctions_ofac: Consolidated (non-SDN) list rows were merged untagged and every hit was reported as an 'OFAC SDN list match' with register = 'Specially Designated Nationals (SDN) List'. Each row now carries `OfacList::{Sdn, Consolidated}`; register, summary and a per-list tag name the actual list, on the subject and the wallet pivot alike. |
| 36 | `src/modules/pgp/mod.rs` | **REFUTED on re-derivation (2026-09-15):** the body is read with `read_body_capped_or_fail` and a mid-stream failure fails closed (mod.rs ~103-109). Already fixed on `main`. |
| 37 | `src/modules/rubygems_user/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** rubygems_user: the first path segment of every gem's `source_code_uri` is the repository OWNER, minted unconditionally as a `Username` at HIGH_PLUS — `rails`, an employer's org, a co-maintainer's fork host became the subject's handles. Only an owner equal to the subject's handle is the subject's GitHub account; any other owner is candidate-quarantined, tagged `repo-owner`, and its evidence says it is the repository's owner. Locked; falsified. |
| 38 | `src/modules/social_probe/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** social_probe: reproduced at the transport with the production curl arguments — `--max-filesize 8192` makes curl REFUSE a not-found page whose Content-Length exceeds the cap (exit 63, status 200, EMPTY body) and cut a chunked one at 8 KiB with the marker past the cut; `fetch_with_status` returned `(200, "")`, the negative-marker check found nothing, and the module minted the 0.92 `verified-detection` / `body-marker` profile — for every negative-marker (adult / cam) platform, for any handle. Fixed at both layers: `util::curl::fetch_with_status` returns a typed `StatusProbe { status, body, truncated }` (exit 63 reported, cap raised to 256 KiB) and the pure `classify_probe` treats a presence status with a truncated marker-free body as inconclusive (never a hit, never an absence); a marker seen in a partial body is still a definitive not-found. Falsified: without the truncation guard the regression test reports `Found { confidence: 0.92, verified: true }` for the refused download. See REQ-SOCIAL-001. |
| 39 | `src/modules/shodan/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** shodan: `query_internetdb` swallowed every transport failure, throttle, outage and unreadable body with a debug line (its comment said "so the paid path can still proceed", but the paths are exclusive). It now returns `Result<()>`: a 404 is InternetDB's documented "No information available" — the one clean negative — and everything else is the module's error. Loopback test drives the real request path: 500 → error, 429 → error, unreadable 200 → error, 404 → empty, a genuine answer → the port summary and PTR pivot. |
| 40 | `src/modules/stolen_tax/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** stolen_tax: a non-key `success: false` envelope was `BodyVerdict::Absent` — an empty result on a key-gated paid breach lookup, cached for a day. `body_verdict` accepts it and `accepted` fails it with the provider's own error text (no documented `success: false` "no results" shape exists; a zero-hit search answers `success: true`). Locked; falsified. |
| 41 | `src/modules/smtp_vrfy/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** smtp_vrfy: every RCPT TO reply other than exactly `250` became `Invalid(code)` → `smtp-invalid`, so greylisting (`450 4.7.1`), a full queue (452), a busy server (421) — the normal first-contact reply of many MTAs — recorded a deliverable address as rejected. RFC 5321 first-digit classification: 2yz accepted (250/251/252), 4yz the new `SmtpVerdict::Transient` (`smtp-transient`, speculative tier), 5yz the only `Invalid`, anything else `Unreachable`. Locked with a duplex mock answering 450; falsified. |
| 42 | `src/modules/social_location/mod.rs` | **REFUTED on re-derivation (2026-09-15):** `ok_or_absent(SRC, resp, &[404])` gates every non-2xx before the body is read (mod.rs ~84). Already fixed on `main`. |
| 43 | `src/modules/sunrise_sunset/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** sunrise_sunset: `status != OK → Ok(empty)`, `results` missing → `Ok(empty)`, and `ok_or_absent(.., &[404])` on a fixed endpoint. The provider computes phases for ANY coordinates, so none of those is an empty answer: `fetch_solar(client, endpoint, lat, lon, date)` makes every one the module's error, naming the provider's status. Loopback test: `UNKNOWN_ERROR` → error, `INVALID_REQUEST` → error, 404 → error, `OK` without `results` → error, a genuine `OK` → parsed. |
| 44 | `src/modules/stackoverflow_user/mod.rs` | **VERIFIED AND FIXED (2026-09-15; observed live).** stackoverflow_user: `inname=John Smith` in the API's reputation order returns 95 exact "John Smith" accounts in the first 100, and the first was taken as the subject — its reputation, website, location and cross-SE footprint. The query is now the exact-name page (`sort=name&min=X&max=X`, live-verified: `Jon Skeet` → 2 accounts, `has_more: false`); `resolve_name` attributes only a unique holder on a page that did not overflow, and a shared name is a typed `NotApplicable` skip that says how many accounts share it. Locked; falsified. |
| 45 | `src/modules/structured_id/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** structured_id: the ObjectID / KSUID decoders' only validation is the plausibility window, which about 1 in 5 (ObjectID) / 1 in 11 (KSUID) random tokens of the right shape pass — and the date was asserted as an `account-age` finding. Those two formats now emit a candidate-quarantined lead whose evidence says "if this token is one" and records the validation and its false-positive rate; ULID (~0.3 %) still asserts. Locked; falsified. |
| 46 | `src/modules/trove_au/mod.rs` | **VERIFIED AND FIXED (2026-09-15) against the v3 documentation; live decode UNVERIFIED (no key).** trove_au: the module decoded v2's `response.zone[]` from the v3 endpoint with the v2 `zone=` selector, so every keyed search read as zero hits. The request selects `category=newspaper`; `TroveResp` is v3's top-level `category[]` with `records.total` / `records.article[]`; an article's headline is `heading`, its `title` the masthead object (decoded either as v3's object or v2's string), `troveUrl` the reader page; a body without `category` is a failed lookup, never zero hits. Locked; falsified. |
| 47 | `src/modules/whois/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** whois parsed IANA's TLD record as the target domain's WHOIS when the referral hop failed, minting fabricated registration data and 13 root-server `whois-ns` Domain entities. Re-derived from source and reproduced offline against the live IANA `COM`/`VN` records: worse than the lead said — for a registry with no WHOIS server (IANA's `whois:` blank, e.g. `.vn`) the fabrication was deterministic on every lookup, not only on a failed hop. Same pass found and fixed three siblings in the address path: ARIN allocations discarded whole by the domain-shaped "actionable data" gate (the live sweep's persistent `whois 8.8.8.8 → empty`), the parent carrier attributed on ARIN's nested answers, and RPSL `org:` handles / `person:` contacts minted as Organisation / Person entities. Regression tests on authentic IANA/ARIN/RIPE wire text in `src/modules/whois/tests.rs`; the "registry has no WHOIS server" case is the first user of the typed `Error::Skipped` → `ModuleSkipped { NotApplicable }` path. |
| 48 | `src/modules/urlscan/mod.rs` | **VERIFIED AND FIXED (2026-09-15; observed live).** urlscan: the bare `domain:` selector matches every page that loaded a resource from the domain — `domain:"example.com"` returned `dodeliver.com.pk` pages, `fonts.googleapis.com` five unrelated sites — whose IP, country, ASN, PTR and domain were minted as the target's. The query is `page.domain:` (live-verified to match the host and its subdomains) and `page_is_the_targets` gates every hit on its own page (Domain: host or subdomain; Url: the URL's host; IpAddress: the answering IP), with the reported total following the gate. Locked; falsified. |
| 49 | `src/modules/urlscan/mod.rs` | **VERIFIED AND FIXED (2026-09-15; observed live).** urlscan: a search hit carries no `verdicts` (hit keys: `_id`, `_score`, `canonical`, `page`, `result`, `screenshot`, `sort`, `stats`, `submitter`, `task`), and the per-scan result endpoint that does answers 403 "You're not logged in!" without a key — so `urlscan-malicious` could never fire. The dead read, tag, attribute and confidence bump are removed and the header says why; a keyed result fetch can restore the signal once its shape is captured. |
| 50 | `src/modules/urlhaus/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** urlhaus: a 401/403 on the Auth-Key was a clean empty result ("not in the URLhaus corpus" about a host never checked) and a 429 never reached the key pool; every non-`ok` `query_status` folded into the same negative. 401/403/429 are reported to the pool and fail the lookup; `has_results` keeps only `no_results` as the clean negative and fails any other status. Locked; falsified. |
| 51 | `src/modules/wiki_geosearch/mod.rs` | **REFUTED on re-derivation (2026-09-15):** `geosearch_places` runs `MwError::check(&resp.error, SRC)?` before reading places (mod.rs ~55–58, applied at ~183); a MediaWiki 200-envelope is a module error. Already fixed on `main`. |
| 52 | `src/modules/wikidata/mod.rs` | **REFUTED on re-derivation (2026-09-15):** `wbsearchentities` runs `MwError::check(&search.error, SRC)?` (mod.rs ~155–161). Residual, not this lead: the best-effort `wbgetentities` claims call decodes no `error` field, so a 200-envelope there drops claim pivots — a degraded primary entity, never an empty result. |
