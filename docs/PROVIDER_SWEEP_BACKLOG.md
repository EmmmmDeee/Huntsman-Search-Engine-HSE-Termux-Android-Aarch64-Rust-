# Provider sweep backlog — round 3 (Pass 17 discovery)

One finder per chunk read all 134 provider modules no earlier pass had covered,
against a six-point checklist derived from the defect classes already fixed on
this branch. Findings then went to 3-lens adversarial verification.

**Verification was cut short by a model usage limit**, not by the findings: 9 were
verified unanimously, 1 was refuted, and 43 never reached a verifier. The 43 are
recorded here as UNVERIFIED — they are leads, not findings, and none may be acted
on or reported as a defect until it has been re-derived from the source. Most
restate the class this branch has already fixed eight times (a failed lookup
reading as a clean negative), which is a reason to check them, not to believe them.

Directive framing: a module that reports "found nothing" when it actually failed
violates ABSENCE OF EASY EVIDENCE ≠ ABSENCE OF A NEXUS; the identity-attribution
leads violate IDENTIFIER MATCH ≠ ENTITY IDENTITY. `core::claim::Support::Failed`
is the type that makes the first class impossible to express once modules adopt it.

## Confirmed (3/3 adversarial verification) — actionable

| # | Module | Finding |
|---|---|---|
| 0 | `src/modules/asic_business_names/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** `ckan_query` returns CKAN's own `r.total` as `server_total`, and `is_truncated(server_total, records.len())` drives the `truncated` tag and `total_matches`. |
| 1 | `src/modules/acma_rrl/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** The body is read with `read_body_capped_or_fail`: a mid-body transport failure is the module's error, never an empty page parsed to "no licence". |
| 2 | `src/modules/ahpra/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** The body is read with `read_body_capped_or_fail`: a mid-body transport failure is the module's error, never "not a registered practitioner". |
| 4 | `src/modules/au_property/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** au_property: `run_leg` returned `LegOutcome::Ok` after `read_body_capped` yielded `None`. The body is read with `read_body_capped_or_fail`; a failure is `LegOutcome::Unreachable` and nothing parsed from a partial body is kept. Loopback test: a body whose Content-Length promises more than arrives → `Unreachable`. See REQ-AUPROP-001. |
| 5 | `src/modules/auspost/mod.rs` | **FIXED against the published contract (2026-09-15); live decode UNVERIFIED (no key here).** auspost: the struct expected a bare `localities` list with a `locality` string and a string postcode; Australia Post documents `localities.locality[]` with `location` and a numeric postcode (a lone object for one match, `""` for none). A tolerant deserializer now accepts every documented variant and the pre-fix spelling, and rejects any other shape as a decode failure. See REQ-AUSPOST-001. |
| 6 | `src/modules/bitcoin/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** bitcoin: the `?` on the `/txs` call errored the module after `/address` had answered, discarding the ledger reading. `lookup` keeps the reading and stamps the anchor's evidence with the failed co-spend lookup (never a silent "no co-spends"); a failed ledger call is the error, Esplora's 404 the clean negative. Loopback tests. See REQ-BITCOIN-001. |
| 7 | `src/modules/austlii/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** `ok_or_absent(SRC, resp, &[])` on the fixed `sinosrch.cgi` path: a 404 is the endpoint gone, not a clean miss (the module's own comment records the reasoning). |
| 8 | `src/modules/austlii/mod.rs` | **Already fixed on `main` (verified from source 2026-09-15).** The body is read with `read_body_capped_or_fail`; a mid-body transport failure is the module's error. |
| 9 | `src/modules/chess_profile/mod.rs` | chess_profile: every upstream failure (429/5xx/transport/breaker short-circuit) on BOTH platforms collapses to an empty ModuleResult |

## Refuted by verification — do not act

| # | Module | Claim |
|---|---|---|
| 3 | `src/modules/au_property/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** au_property: NSW's retired `maps.six.nsw.gov.au` 308-redirects to the SDT Explorer SPA on another host; reqwest followed it, the SPA shell parsed to nothing and the leg was tallied `Ok` — "register consulted, no records". A 2xx whose final host differs from the requested host is now `LegOutcome::Migrated` (`landed_off_host`), a dead endpoint in `leg_failure`'s verdict. Loopback test: a 308 to another host → `Migrated`. See REQ-AUPROP-001. |

## Unverified leads (43) — re-derive before believing

| # | Module | Lead |
|---|---|---|
| 10 | `src/modules/dns_axfr/mod.rs` | dns_axfr: when no nameserver could be reached at all, the module reports "no zone-transfer exposure" — the exact fail-open its own doc comment forbids |
| 11 | `src/modules/data_gov_au/mod.rs` | data_gov_au: HTTP 404 from CKAN package_search is mapped to a clean "no matching agency" — but CKAN never 404s for a zero-match, and the module's own doc says a 404 means the endpoint path is wrong |
| 12 | `src/modules/comb_search/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** comb_search: the request went through `fetch_json_or_404`, so a 404 (the endpoint moved, a WAF page) became `Ok(empty)` — a clean-negative breach claim about the named subject. The query now goes through `fetch_json` (every non-2xx is a failed lookup; the endpoint signals a miss in the body of a 200), behind `query_comb(client, endpoint, value)` so the real request path is exercised against a loopback server: 404 → error, 503 → error, `{count:0, lines:[]}` → the genuine miss. |
| 13 | `src/modules/comb_search/mod.rs` | comb_search: for a Username target, the subject's own Username entity is tagged `breach` and enriched with "N leaked credential line(s)" from strangers' same-local-part accounts, while the doc says username matches are candidate-quarantined |
| 14 | `src/modules/crossref_search/mod.rs` | crossref_search: doc claims an author/affiliation-name search but the request is the generic all-fields `query=`, so works ABOUT a name (or merely mentioning it) are minted as the subject's works |
| 15 | `src/modules/crates_io/mod.rs` | crates_io: a failure on the second (crate-listing) request discards the already-confirmed user entities from the first request |
| 16 | `src/modules/device_sensors/wifi.rs` | device_sensors/wifi.rs: absent sensor readings are asserted as literal 0 / "<hidden>" in evidence, contradicting the contract the sibling device_fix.rs states for the same sensor family |
| 17 | `src/modules/fofa/mod.rs` | FOFA HTTP-200 `error:true` envelope (dead key / quota) collapses to a clean empty result and never reaches the key pool |
| 18 | `src/modules/gaming_profile/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** gaming_profile: both lookups returned an empty batch on any transport/HTTP/decode failure, so an outage on either platform read as "no such account". Each lookup now returns `Result<Vec<Entity>>` (a non-2xx or undecodable resolver answer is the platform's error; only a resolver answer naming no match, or Mojang's documented 404, is the miss) and `combine` applies `ModuleResult::or_hard_failure`: a failure is kept, a partial outage never discards a confirmed account, and a failure with nothing found is the module's error. Loopback tests drive both real request paths (503/500 → error; `{data:[]}`/404 → miss) and the combination policy is pinned. |
| 19 | `src/modules/doh_resolver/mod.rs` | **REFUTED on re-derivation (2026-09-15):** `process` gates on `status().is_success()` before `json_decode` (mod.rs ~913); a Cloudflare 400 is a module error, not a negative. Already fixed on `main`. |
| 20 | `src/modules/exa_search/mod.rs` | exa_search sends snake_case body keys and reads snake_case response fields that Exa's documented API does not use (`numResults`, `contents.text.maxCharacters`, `publishedDate`) |
| 21 | `src/modules/europepmc_search/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** europepmc_search: `ok_or_absent(.., &[404])` on a fixed REST endpoint — a 404 is the endpoint gone, never "no publications by this author". Every non-2xx is now the failed lookup (`search(client, endpoint, query)`); a loopback test drives the real request path: 404 → error, 500 → error, `hitCount: 0` → the genuine miss. |
| 22 | `src/modules/greynoise/mod.rs` | greynoise keyed path decodes the wrong /v3/ip schema — a configured key silently turns every IP into a clean negative |
| 23 | `src/modules/github_commits/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** github_commits: `if !status.is_success() { return Ok(empty) }` — a 5xx, a 401 on a revoked token and a 403/429 throttle all read as "no commits by this author". `search_commits(ctx, api_base, email)` now classifies: a throttle (429, or a 403 that names the rate limit — `github_api::throttled`, shared with github_code_search) is the typed `Error::RateLimited`, anything else the module's error; a configured token is still reported to the key pool. Loopback test: 500 → error, 403 "API rate limit exceeded" → RateLimited, 429 → RateLimited, 401 → error, `total_count: 0` → the genuine miss. |
| 24 | `src/modules/launchpad_user/mod.rs` | launchpad_user resolves Launchpad TEAMS as people — a team slug is minted as a confirmed user handle and its display name as a Person |
| 25 | `src/modules/ip_registry/mod.rs` | ip_registry's BGPView upstream no longer exists (api.bgpview.io is NXDOMAIN) — ASN targets always hard-error and the IP path silently loses its announcing-ASN/prefix/operator half **Re-confirmed 2026-09-15:** `api.bgpview.io` has no DNS here and the 2026-09-14 sweep recorded the `bgpview` canary unreachable; the sweep now fails on it (REQ-DRIFT-001). Retirement of the endpoint — `bgpview` module and `ip_registry`'s ASN path (RDAP `autnum` is the replacement; authentic fixtures captured) — is the next unit of work. |
| 26 | `src/modules/osintcat/mod.rs` | osintcat: after a passing credits preflight, failure of all three data endpoints collapses to a clean 'no footprint, no breach' negative (no or_hard_failure) |
| 27 | `src/modules/overpass/mod.rs` | **REFUTED on re-derivation (2026-09-15):** an HTTP-200 `remark` runtime error is surfaced as "Overpass returned a runtime error, not an empty area" (mod.rs ~48-56). Already fixed on `main`. |
| 28 | `src/modules/mastodon_user/mod.rs` | mastodon_user: instance-probe failures are dropped without a log and a partial sweep (1 answered, 9 failed) still returns a clean negative for all ten instances |
| 29 | `src/modules/mylnikov/mod.rs` | mylnikov: every in-body `result` code other than 200 — not just the 404 miss — is folded into 'BSSID not located', and the provider's `desc` is never read |
| 30 | `src/modules/plc_directory/resolve.rs` | plc_directory: audit-log fetch failure (5xx/429/transport/breaker) collapses into 'no PLC history' clean miss |
| 31 | `src/modules/ripestat/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** ripestat: every RIPEstat sub-fetch error was `.ok()`'d and the empty result returned without `or_hard_failure` — a total outage read as a clean negative (reproduced offline through the real code path with a refused-proxy client). Each endpoint stays best-effort, but an answer built from no endpoint is now the module failure `or_hard_failure` exists for. |
| 32 | `src/modules/qld_cadastre/mod.rs` | qld_cadastre: ArcGIS HTTP-200 error envelope decodes to `features: []` and is reported as 'no cadastral parcel at this point' |
| 33 | `src/modules/pypi_user/mod.rs` | pypi_user: an XML-RPC `<fault>` (HTTP 200 — PyPI's rate-limit and deprecation signal) parses to zero package pairs and is reported as 'user has no packages / not on PyPI' |
| 34 | `src/modules/phone_geo/data.rs` | **VERIFIED AND FIXED (2026-09-15).** phone_geo: area-code pass geolocated a bare national number with no international marker — reproduced end to end with the built binary (`hse scan -k phone -v "817-555-1234" -m phone_geo -d 0` → Address "Kyoto" at 0.58). `process` now gates both passes on `phone_intl::international_digits`, the predicate its offline siblings already use; a number without a marker is a typed `NotApplicable` skip. |
| 35 | `src/modules/sanctions_ofac/list.rs` | **VERIFIED AND FIXED (2026-09-15).** sanctions_ofac: Consolidated (non-SDN) list rows were merged untagged and every hit was reported as an 'OFAC SDN list match' with register = 'Specially Designated Nationals (SDN) List'. Each row now carries `OfacList::{Sdn, Consolidated}`; register, summary and a per-list tag name the actual list, on the subject and the wallet pivot alike. |
| 36 | `src/modules/pgp/mod.rs` | **REFUTED on re-derivation (2026-09-15):** the body is read with `read_body_capped_or_fail` and a mid-stream failure fails closed (mod.rs ~103-109). Already fixed on `main`. |
| 37 | `src/modules/rubygems_user/mod.rs` | rubygems_user: the GitHub org/user segment of every gem's `source_code_uri` is minted as the subject's Username at HIGH_PLUS, attributing third-party org accounts to the handle |
| 38 | `src/modules/social_probe/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** social_probe: reproduced at the transport with the production curl arguments — `--max-filesize 8192` makes curl REFUSE a not-found page whose Content-Length exceeds the cap (exit 63, status 200, EMPTY body) and cut a chunked one at 8 KiB with the marker past the cut; `fetch_with_status` returned `(200, "")`, the negative-marker check found nothing, and the module minted the 0.92 `verified-detection` / `body-marker` profile — for every negative-marker (adult / cam) platform, for any handle. Fixed at both layers: `util::curl::fetch_with_status` returns a typed `StatusProbe { status, body, truncated }` (exit 63 reported, cap raised to 256 KiB) and the pure `classify_probe` treats a presence status with a truncated marker-free body as inconclusive (never a hit, never an absence); a marker seen in a partial body is still a definitive not-found. Falsified: without the truncation guard the regression test reports `Found { confidence: 0.92, verified: true }` for the refused download. See REQ-SOCIAL-001. |
| 39 | `src/modules/shodan/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** shodan: `query_internetdb` swallowed every transport failure, throttle, outage and unreadable body with a debug line (its comment said "so the paid path can still proceed", but the paths are exclusive). It now returns `Result<()>`: a 404 is InternetDB's documented "No information available" — the one clean negative — and everything else is the module's error. Loopback test drives the real request path: 500 → error, 429 → error, unreadable 200 → error, 404 → empty, a genuine answer → the port summary and PTR pivot. |
| 40 | `src/modules/stolen_tax/mod.rs` | stolen_tax maps any non-key in-body error envelope (success:false) to a clean empty result on a key-gated paid lookup |
| 41 | `src/modules/smtp_vrfy/mod.rs` | smtp_vrfy tags a 4yz transient SMTP reply (greylisting, 452, 421) as smtp-invalid |
| 42 | `src/modules/social_location/mod.rs` | **REFUTED on re-derivation (2026-09-15):** `ok_or_absent(SRC, resp, &[404])` gates every non-2xx before the body is read (mod.rs ~84). Already fixed on `main`. |
| 43 | `src/modules/sunrise_sunset/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** sunrise_sunset: `status != OK → Ok(empty)`, `results` missing → `Ok(empty)`, and `ok_or_absent(.., &[404])` on a fixed endpoint. The provider computes phases for ANY coordinates, so none of those is an empty answer: `fetch_solar(client, endpoint, lat, lon, date)` makes every one the module's error, naming the provider's status. Loopback test: `UNKNOWN_ERROR` → error, `INVALID_REQUEST` → error, 404 → error, `OK` without `results` → error, a genuine `OK` → parsed. |
| 44 | `src/modules/stackoverflow_user/mod.rs` | stackoverflow_user attributes the FIRST reputation-sorted display-name match's website, location and cross-SE footprint to the seed handle (display names are not unique) |
| 45 | `src/modules/structured_id/mod.rs` | structured_id's ObjectID decoder mints an 'account-age' date from roughly one in five arbitrary 24-hex tokens, contradicting the module's own no-fabrication claim |
| 46 | `src/modules/trove_au/mod.rs` | trove_au decodes the Trove v2 response shape (`response.zone[]`) from the v3 endpoint, so every search silently reads as zero hits |
| 47 | `src/modules/whois/mod.rs` | **VERIFIED AND FIXED (2026-09-15).** whois parsed IANA's TLD record as the target domain's WHOIS when the referral hop failed, minting fabricated registration data and 13 root-server `whois-ns` Domain entities. Re-derived from source and reproduced offline against the live IANA `COM`/`VN` records: worse than the lead said — for a registry with no WHOIS server (IANA's `whois:` blank, e.g. `.vn`) the fabrication was deterministic on every lookup, not only on a failed hop. Same pass found and fixed three siblings in the address path: ARIN allocations discarded whole by the domain-shaped "actionable data" gate (the live sweep's persistent `whois 8.8.8.8 → empty`), the parent carrier attributed on ARIN's nested answers, and RPSL `org:` handles / `person:` contacts minted as Organisation / Person entities. Regression tests on authentic IANA/ARIN/RIPE wire text in `src/modules/whois/tests.rs`; the "registry has no WHOIS server" case is the first user of the typed `Error::Skipped` → `ModuleSkipped { NotApplicable }` path. |
| 48 | `src/modules/urlscan/mod.rs` | urlscan attributes third-party pages' IPs, domains, countries and ASNs to a Domain target because `domain:` matches any contacted resource domain |
| 49 | `src/modules/urlscan/mod.rs` | urlscan reads `verdicts.malicious` from search hits, which never carry `verdicts` — the documented `urlscan-malicious` tag can never fire |
| 50 | `src/modules/urlhaus/mod.rs` | urlhaus never reports a 429 on the configured Auth-Key to the pool (and folds a rejected key into a clean empty result) |
| 51 | `src/modules/wiki_geosearch/mod.rs` | wiki_geosearch treats a MediaWiki HTTP-200 `error` envelope as "no nearby places" |
| 52 | `src/modules/wikidata/mod.rs` | wikidata treats a MediaWiki HTTP-200 `error` envelope on `wbsearchentities` as "no matching item" |
