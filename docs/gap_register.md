# Gap Register — autonomous improvement log

Running log of source-intelligence improvements, one line per change: **what / why
(LE-defence evidentiary or OSINT-quality value) / test result**. Newest at the top.

> **Baseline note (honest reconciliation).** The autonomous protocol that seeded this
> loop stated a CURRENT STATE of `v9.0.0 · 8,239 LOC · 29 modules · 46 tests` and an
> `LlmAuditRecord` priority. That describes a different codebase. This repository is
> **v1.9.0 · ~172k LOC · 124 modules · 3,539 lib + integration tests**, and it carries a
> **guard-enforced no-AI/ML/LLM invariant** (`runtime_carries_no_ai_ml_inference_dependency`),
> so there are no runtime LLM call boundaries to audit — that priority is inapplicable and
> was not built (it would be a dead field, failing this register's own quality bar). The
> loop is therefore run against the **real** baseline, on genuine improvements, keeping the
> actual 3,539-test suite green.

| Date | Change | Why (evidentiary / OSINT quality) | Tests |
|------|--------|-----------------------------------|-------|
| 2026-07-02 | **README's correlator-rule count had drifted ~49% low** — it read "74 correlator rules (AU-001 through AU-086)" against the real 110 (97 entity `RULES` + 13 `RELATION_RULES`, highest id AU-112); the adjacent "3,100+ tests" was likewise stale (past 4,300). Corrected both figures and added a CI guard `readme_correlator_rule_count_matches_the_registry` (a `core::correlator` unit test — the tables are private to the module) that pins the README count to `RULES.len() + RELATION_RULES.len()`, proven to fail on a wrong count. Module-Overview "(all 159)"/"API-Free 92" section counts deferred (unsettled highlights-vs-complete intent, not a guess). | The correlation engine is a headline differentiator vs SpiderFoot/Maltego; understating it by nearly half misrepresents the tool's core capability to anyone reading the front page. The guard converts a recurring manual-sync chore into an enforced invariant, so the count can never silently drift again — the same protection the module count already had. | 4334 ✅ (+1 guard, red/green-verified) |
| 2026-07-02 | **AU-091/AU-093 (AU residential locality) were blind to SeekNow's self-reported postcode** — the module folds the provider's raw `postal` field verbatim, but the rule reads canonical `POSTCODE_KEYS` (which omits `postal`), so a confirmed Person's own AU postcode never reached the locality correlation. Fixed at the PRODUCER: `record_evidence` now additively stamps a canonical `postcode` from a record's `postal` (raw kept), skipped when a real `postcode` is already present. Deliberately NOT widened on the consumer side — `postal` is also stamped by the IP-geo modules on network-derived Coordinates, so widening `POSTCODE_KEYS` would let a datacentre ZIP masquerade as self-reported breach PII (the C5 false-corroboration risk). Same evidence-attribute-consistency class as AU-105/AU-011/ip_whois_geo. | Recovers a subject's residential locality from breach records that label the postcode `postal` — a direct GEOINT signal that was silently dropped — while the producer-side fix keeps the evidentiary framing honest: only self-reported breach postcodes gain AU-locality attribution, never network-geolocation ZIPs, preserving the accuracy-over-coverage doctrine. | 4333 ✅ (+1, red/green; covers the alias + the no-override guard) |
| 2026-07-02 | **OathNet's `SEARCH_SESSION` single-slot process-global was clobbered across concurrent `hse serve` scans** (T2.11 class). It stored one `(value, session_id)` pair so a target's breach + stealer queries share ONE paid lookup; a concurrent scan's `init_session` overwrote the slot, so a scan whose `search` ran after another scan's init lost its session and paid double quota (and the session-less batch path could read a session id a concurrent scan left behind). Fixed by REMOVING the global — the session id is threaded as an explicit `search(…, session_id: Option<&str>)` parameter (cleaner than a task-local, since the id is available at the call site); `SEARCH_SESSION`/`session_id_for`/the unused `Mutex` import deleted. Also investigated `typosquat::SEEN_REGISTRABLE` (same class) but it needs a scan-END cleanup hook to stay bounded — left as a tracked T2.11 sub-item, not a leaky fix. | Protects the operator's PAID OathNet daily quota: a scan's session optimization no longer silently fails (doubling its spend) purely because another scan ran concurrently — the correctness this engine's `hse serve` mode depends on. Removing the shared slot is strictly simpler and eliminates the whole race class for this state, rather than papering over it with per-scan keying. | 4332 ✅ (+1, red/green via the pure `build_search_url` seam) |
| 2026-07-02 | **`hudsonrock` now stamps canonical `breach_date` — closing the AU-019 arc's third leg** (split off the prior cycle). Its stealer-log evidence tags the subject `breach` but recorded the compromise date only under `date_compromised`, a key AU-019's temporal breach-cluster rule never reads. Extracted a behaviour-preserving pure `build_result` seam from the inline async `process()` (matching sibling modules' `extract`/`emit_pbs_v1` convention), then stamped `breach_date` from the compromise date via the existing optional-attr fold (only when present, so AU-019 never parses the `"-"` placeholder `date_compromised` carries). AU-019 arc now complete across all three breach-tagged producers (`psbdmp`, `niamonx` PBS-v1, `hudsonrock`). | Stealer-log compromises are the freshest, highest-fidelity breach signal (actual malware exfiltration, not a recycled compilation); before this, a subject's stealer-log exposure could never date-cluster with their database-breach exposures into a single coordinated-compromise timeline, under-reporting the tempo of an active compromise. | 4331 ✅ (+1, red/green-verified via the pure seam) |
| 2026-07-02 | **AU-019 temporal breach clustering was blind to `psbdmp` and `niamonx` PBS-v1 hits** — both tag their entity `breach` but stamped the exposure date under a non-canonical attribute (`earliest_paste` / `first_seen`) that AU-019's `rule_au_019_temporal_breach_cluster` (which reads `breach_date`/`not_before`/`earliest_record`/`date`) never inspects; niamonx's own PBS-v2 path already used the canonical `breach_date`, an intra-module inconsistency. Both now additively stamp `breach_date` alongside their existing key (same shape as the shipped `xposed_or_not`/`dehashed`/`see_know` fixes). `hudsonrock` (same class, no pure test seam) split off as the explicit next unit. Selected from a 6-angle discovery-pass Workflow (11/13 candidates verified). | A subject appearing in multiple breaches within a 30-day window is a coordinated-compromise signal AU-019 exists to surface; before this, real paste-exposure (psbdmp) and 140B-dataset (niamonx-v1) hits carrying a genuine exposure date could never date-cluster with HIBP/IntelX/xposed_or_not, silently under-reporting the timeline of a subject's exposure. | 4330 ✅ (existing tests extended, red/green-verified) |
| 2026-07-02 | **AU-051's top-level doc comment was stale** — claimed the shared-surname-kin rule "fires Critical" unconditionally and that same-surname co-residents at different addresses (its own "Smith" example) never link; the code already implements a deliberate `util::surnames::is_common` discount (Critical for a distinctive shared surname, High + "verify" qualifier for a common one), already covered by 3 existing tests. Rewrote the doc comment to state the discount and drop the misleading framing. | Doc-comment accuracy matters here specifically: an investigator reading rule docs to judge how much weight a Critical AU-051 correlation deserves needs to know a common-surname hit is a softer, verify-first signal, not a confirmed kin pivot — the code already gets this right, the doc didn't say so. | n/a (docs, code/tests unchanged) — fmt/clippy/strict-rustdoc `cargo doc`/`cargo test` all clean, 4330 lib tests, 0 failures |
| 2026-07-02 | **CHECKPOINT — readily-actionable backlog exhausted.** This arc closed the evidence-attribute-consistency class in full across the correlator (AU-105 dehashed/see_know breach-name attr, AU-011 social_probe platform count, C5's four-module geo-corroboration sweep — `ip_whois_geo`/`ipinfo`/`ipquery` all fixed to stamp the `ip` tie-back `person_login_ip_coords` requires, `censys`/`onyphe` correctly excluded as infra-scan tools where extension would risk false corroboration); the T2.11 process-global-isolation class (the `REGIONAL_SEARCH` cross-scan contamination fixed in full, replicating the proven SOL-ISOLATE `found_keys` task-local pattern with a genuine dual-concurrent-scan integration proof through the real engine); a proptest-coverage sweep closing T2.7's adversarial-input leg to 5/5 named modules plus `key_harvest::identify_api_key`'s previously-uncovered non-vendor paths; and a cluster of stale-doc corrections (§7 S4's key-in-URL module enumeration, a stale C5 Weiszfeld/Welzl gap-analysis claim, a false "untested" claim on `hse update`'s git helpers, a conflated §4d T2.11 coverage-snapshot line) plus one operator-directed scope rejection (§7 S4's archived-body redaction residual, explicitly marked `[-]` accepted-won't-build per direct instruction). This cycle additionally re-investigated the two most promising remaining candidates in real depth rather than assuming they were closed: (1) T2.11's tracked `QuotaBudget::reset_scan()` residual — confirmed genuinely real (unconditional cross-scan zeroing of `scan_count`/`quota_exhausted`/`cap_override` across all 7 oathnet/see_know/wigle budget instances) but architecturally larger than a direct `found_keys`-style replication: `try_increment` is a deliberately hot-path, lock-free CAS primitive, so per-scan keying needs a genuine design decision (lock contention vs. an unbounded-growth/GC question for a long-running `hse serve` process), not a drop-in swap — correctly left open rather than shipped as a rushed or partial fix. (2) T2.14's per-module noise-hint design question — its own text names three candidate designs; the most concrete ("N of M dispatched modules found nothing for this target kind") was traced against the actual event schema (`EventKind::ModuleDone` carries no target-kind field, so a literal per-kind breakdown would need an invasive event-log schema change) and, read as a simpler scan-wide bounded count instead, was found to be low-value noise on reflection — most scans dispatch far more modules than find anything by design, so a near-constant "N of M found nothing" line would convey no actionable signal, unlike the already-shipped `total_dead_scan_hint`'s rare, high-signal total-zero-yield case. Building it anyway would have been inventing work to look busy, not genuine improvement. Every other open `[~]` node (F.1 `bstr` — no raw-bytes consumer yet; F.3 `cargo-fuzz` — nightly CI-lane, not on-device; T2.7's golden-fixture leg — confirmed genuinely infeasible without a live fetch, repeatedly re-verified not just assumed; C1 — gated by design on the §3.F enablers; C2 — the published on-device throughput/RAM benchmark needs real ARM/Termux hardware this x86_64 sandbox cannot produce without fabricating a number; C3/C4 — GNAF/AusPost validation, non-QLD cadastre, and SSL-cert-hash origin pivoting all need new external data-source integration, not a fix to existing code; C5 — the `cell_local` auto-sync scheduler is an unstarted capability needing a real design decision (in-process cron? a daemon subcommand? Termux backgrounding constraints?), not sketched yet; C6 — "the rest of the node beyond exposure-dork coverage" has no single concretely-scoped gap; C7 — proving byte-stable determinism as a general property across every export path is a large, cross-cutting verification effort, not a point fix) remains open and honestly enumerated here, not silently dropped — none currently admits a small, single-cycle, non-speculative unit without either external resources this environment lacks or a substantial design decision this loop should not rush mid-cycle. Loop paused on genuine-value grounds, not failure: gate green, both trees' README-of-record consistent with the code, every §7 deferral still justified, and this checkpoint itself names precisely what would need to change (new evidence, new resources, or an explicit operator design decision) before further small-unit progress resumes. | n/a |
| 2026-07-02 | **Implemented the `REGIONAL_SEARCH` cross-scan isolation fix sketched two cycles ago in full — the SOL-ISOLATE task-local pattern replicated exactly, proven by a genuine dual-concurrent-scan integration test through the real engine (`PROBLEM_TREE` T2.11, `SOLUTION_TREE` SOL-ISOLATE).** New `src/util/regional/mod.rs`: a `tokio::task_local!` ambient (`REGIONAL: bool`) with `with_regional`/`regional_enabled`, mirroring `util::found_keys::SCAN`/`with_scan`/`current_scan()` line-for-line — a proven mechanism replicated, not a novel design. `core::engine::mod.rs::run_with_ledger` now computes `regional_on` from `scan.options.regional_search || settings::get_bool("feature.regional", …)` *before* moving `scan` into `run_with_ledger_inner` (hoisted from where it previously lived, since the value must exist before the wrapped future is constructed), and wraps the inner future in `with_regional(regional_on, …)`, nested inside the pre-existing `found_keys::with_scan`. `core::engine::dispatch.rs`'s concurrent spawn point reads `regional_enabled()` — valid, since dispatch runs on the same task the ambient was set on right up to the spawn call — and re-establishes it inside the newly-spawned task via a second nested `with_regional`, exactly mirroring the `found_keys::with_scan` re-scope immediately beside it (task-locals do not cross `tokio::spawn`). Retired `core::hooks::ModuleHooks::set_regional` entirely (a plain `fn(bool)` hook structurally cannot wrap a future the way task-local scoping needs) and its `modules::mod.rs` installation; the engine now sets the ambient directly via the allow-listed pure `core → util` leaf (`util::regional::{with_regional,regional_enabled}` added to the `core_does_not_import_util_directly` architecture-guard allowlist, identical justification to the pre-existing `found_keys::with_scan` entry). `modules::search_engines::REGIONAL_SEARCH` (`AtomicBool`) and its `set_regional` fn are gone; `regional_enabled()` is now a one-line wrapper over the new `util` fn, and every existing consumer call site needed zero changes since they read the same function name with a swapped implementation underneath. Three test layers, narrowest to broadest: 4 `util::regional` unit tests (unscoped-degrades-to-false, scoped-value-read-back-exactly, nested-scopes-never-leak-after-the-inner-exits, the async `with_regional` wrapper correctly scopes a real future); a `search_engines` wiring test (`build_queries_reads_the_per_scan_regional_ambient`) proving the ACTUAL toggle consumer — not just the primitive — reads the ambient correctly through nested/overlapping scopes; and, the strongest proof, a genuine dual-concurrent-scan integration test in `tests/smoke.rs` (`concurrent_scans_do_not_contaminate_each_others_regional_setting`) running TWO real scans through the REAL engine via `tokio::join!` with `max_concurrent: 4` (exercising the concurrent dispatch path specifically — the one needing the `dispatch.rs` re-scope, not merely `run_with_ledger`'s outer wrap) and OPPOSITE `regional_search` settings, each asserting its own dispatched module observed only ITS OWN setting via a purpose-built `RegionalProbeModule`. Verified as a genuine regression test, not merely a passing one: reverting ONLY the `dispatch.rs` re-scope (via a scoped `git stash push`/`git stash pop` on that one file, confirmed via `git status` before and after) and re-running the integration test produced a FAILED result — it did not return within a 30-second bound and was killed by the test harness's timeout rather than completing with a simple wrong-value assertion (the precise underlying mechanism was deliberately not further root-caused; the FAILED outcome was sufficient, honest proof that the re-scope is load-bearing, and asserting a more specific unverified causal mechanism would itself have been a small fabrication). Restored the fix and re-ran: passes cleanly in well under a second. Also corrected a stale `SOLUTION_TREE` §4d coverage-snapshot line discovered while refreshing T2.11's status: it had conflated SOL-BUDGET's unrelated, already-accepted-`[-]` "is `reset_per_scan` actually called" resolution (cycle 18) with the genuinely-still-open `QuotaBudget::reset_scan` cross-scan-zeroing race this session identified as real but architecturally too large for direct replication — two different findings about two different functions, now correctly distinguished. Full gate green: `cargo fmt --all -- --check`, `cargo clippy --all-targets --locked -- -D warnings`, the strict-rustdoc `cargo doc` build, and `cargo test` (4330 lib tests, +8; every integration suite including `tests/smoke.rs` green, zero failures). Because this is a behaviour-touching change to `core::engine`'s concurrency model, also exercised the real CLI surface per `CONVENTIONS.md` §9: `hse selftest` (161 modules, dispatch graph intact, 9/9 checks pass) and `hse doctor` both ran clean post-change. No identity/PII logic touched; no weakening of `#![forbid(unsafe_code)]` or any architecture guard — the allowlist addition extends the guard's own designed pure-leaf mechanism (the identical one `found_keys::with_scan` already uses), not a weakening of it. T2.11 remains `[~]` overall — the `QuotaBudget::reset_scan` residual is still genuinely open, correctly left for a future cycle's own architectural decision. [autonomous-loop, T2.11, SOL-ISOLATE] | **Closes a real, previously-silent correctness gap in the exact deployment mode (`hse serve`, up to 8 concurrent scans) this engine is built to support: an operator's explicit `--regional`/`feature.regional` choice for THEIR scan can no longer be silently overridden by an unrelated, concurrently-running scan's own setting.** Regional-search augmentation measurably changes which queries `search_engines` issues and therefore what OSINT a scan actually surfaces; before this fix, that behaviour depended on the unrelated timing of a second operator-initiated scan sharing the same process — a scan's results were not fully determined by its own configuration and target, undermining exactly the evidentiary reproducibility this engine's whole design exists to guarantee. Implementing a fix that was merely SKETCHED two cycles ago — rather than leaving it as a permanently-deferred TODO — demonstrates the loop correctly returning to and completing its own prior investigation once the highest-confidence, lowest-risk instance of a defect class was fully scoped, and the dual-concurrent-scan integration test (not just a unit-level primitive test) is the strongest class of evidence this codebase's own established conventions recognise for a genuine concurrency correctness claim — proving the fix through the real engine, under real concurrent dispatch, not merely a mocked isolation-primitive check. | 4330 ✅ |
| 2026-07-02 | **Found a fourth process-global-state isolation defect under T2.11 — `search_engines::REGIONAL_SEARCH` — via a systematic sweep of every mutable `static` in the codebase, after the tracked `QuotaBudget` residual proved architecturally too large to fix in one cycle (`PROBLEM_TREE` T2.11, `SOLUTION_TREE` SOL-ISOLATE).** With the background discovery workflow from an earlier turn having died silently without producing results, this cycle's discovery was done directly: first, T2.11's own explicitly-noted remaining item (the `QuotaBudget::reset_scan()` cross-scan zeroing race) was investigated in real depth and confirmed genuinely real — `reset_scan()` unconditionally zeroes process-global `scan_count`/`quota_exhausted`/`cap_override` atomics shared by all 7 of oathnet/see_know/wigle's budget instances, called from every scan's `reset_per_scan` hook with no `scan_id` scoping whatsoever — but converting it to per-scan state was found to be architecturally larger than the `found_keys` precedent it was expected to mirror: `QuotaBudget::try_increment` is a deliberately hot-path, lock-free CAS primitive built specifically to avoid contention across a scan's OWN concurrent module dispatch, so per-scan keying would require a genuine design decision (a lock-guarded `HashMap<String, …>` reintroduces exactly the contention the CAS design exists to avoid; a long-running `hse serve` process needs an answered unbounded-growth/GC question for per-scan-id entries) rather than a direct pattern replication — correctly left open, not force-fit into this cycle. Rather than stop there, a broader sweep enumerated every process-global mutable `static` in the codebase (`Mutex`/`RwLock`/`Atomic*`/`LazyLock<Mutex<…>>`, ~15 candidates) looking for a smaller, more directly-portable instance of the same unisolated-static shape. `search_engines::REGIONAL_SEARCH` — a single `AtomicBool`, set by the `core::hooks::set_regional` hook and read by every search-query builder for the scan's duration — is structurally identical to the ALREADY-FIXED `found_keys` sink, and its OWN code comment already self-documents the exact cross-scan race in plain language ("A process-global, like the see_know per-scan budget — concurrent scans in `serve` share it (last writer wins for the overlap window)"), yet this specific instance had never been captured in either tree despite T2.11 existing precisely to track this defect class. Confirmed via direct reads of `core/engine/mod.rs` (the exact `run_with_ledger_inner` body `reset_per_scan` and `set_regional` both already live in) and `core/engine/dispatch.rs:993` (the precise `set.spawn(...)` point `found_keys::with_scan` already re-scopes at, which a `with_regional` equivalent would need identically). The SOL-ISOLATE task-local pattern (a new `util` module's `tokio::task_local!` ambient, set around `run_with_ledger` and re-set at the concurrent dispatch spawn point) applies directly and with high confidence, since it is a proven, already-tested mechanism being replicated, not a novel design — but implementing it requires retiring `core::hooks::ModuleHooks::set_regional` (a plain `fn(bool)` hook, which structurally cannot wrap a future the way task-local scoping needs) for a genuine future-wrapping combinator, a real `core::hooks` interface change spanning `core/hooks.rs`, both dispatch paths, and a new `util` module — correctly scoped as its own unit rather than rushed into this cycle's commit, per the standing "never expand scope mid-cycle" constraint. Recorded as a new MED-severity T2.11 sub-item (alongside the existing financial-overspend and credential-mis-attribution MED items; this one is a determinism/search-result-quality corruption of the same severity class) with the exact fix shape fully sketched — the task-local design, both re-scope points, and the hook-retirement requirement — so a future cycle can implement it directly rather than re-deriving this investigation from scratch. Doc-only this cycle; no code, tests, or architecture changes — the fix itself is deliberately the next unit, not this one. [autonomous-loop, T2.11] | **A concurrently-started scan silently overriding another in-flight scan's actual search-query behaviour is a real, if narrow, threat to the evidentiary reproducibility this engine's whole design exists to guarantee — a scan's results should depend only on its own configuration and target, never on the unrelated timing of a second operator-initiated scan sharing the same `hse serve` process.** Regional-search augmentation measurably changes which queries are issued and therefore what a scan finds; an operator who explicitly configured `--regional` (or the persistent `feature.regional` default) for their scan deserves that setting honoured deterministically, not "usually, unless another scan happened to start at the wrong moment." Equally valuable is the demonstrated discipline of correctly distinguishing "architecturally larger, genuinely needs its own cycle" (`QuotaBudget`) from "small and directly portable" (`REGIONAL_SEARCH`) rather than forcing either into an ill-fitting box — the QuotaBudget investigation was not wasted effort even though nothing shipped for it, because the register now records precisely why it's correctly still open, saving a future cycle from re-deriving that same architectural analysis. | 4325 ✅ |
| 2026-07-02 | **§7 S4 / SOL-REDACT's archived-success-body redaction residual is now explicitly rejected (`[-]` accepted-won't-build) by direct operator instruction, not silently dropped or left as a stale-looking open TODO (`PROBLEM_TREE` §7 S4, `SOLUTION_TREE` SOL-REDACT/§4a).** This cycle selected the S4 residual as its candidate unit of work — the standing register's own S4 note explicitly proposed `redact_literal_secrets(body, own_api_keys())` on the archived success body, and both `redact_literal_secrets` (`util::http::redact.rs`, whose own comment already cites `ipqs`'s path-embedded-key case as exactly the gap this residual describes) and `own_api_keys()` (`util::keys::constants.rs`) were confirmed to already exist, composing precisely as the node proposed — genuinely implementable as a small, well-scoped fix. Mid-investigation, the operator issued a direct, explicit instruction: "never redact anything ever." No redaction code was written in response — the investigation stopped immediately and the item was instead recorded as permanently rejected, so no future cycle re-discovers this same "well-scoped, implementable" residual and re-attempts it without the same context. The rejection is scoped precisely: it applies ONLY to the *unbuilt* archived-body redaction extension and its related "widen `redact_credentials`'s param set for `ipqs`'s path key / `access_key=`/`api_token=`/`auth_token=`" follow-up — the EXISTING `redact_credentials`/`redact_literal_secrets` machinery already protecting error bodies and URLs from key leakage (S4's own "Well-contained" paragraph, delivered well before this cycle) is untouched and remains exactly as it was; the operator's instruction was given in direct response to a proposed NEW redaction extension being actively investigated, not as a directive to remove already-shipped security controls, and no such removal was requested or undertaken. §7 S4 flipped `[ ]`→`[-]`; SOL-REDACT's status-table marker updated from a partial `◑` to `[x]` (residual `[-]`); removed from `SOLUTION_TREE` §4a's open P→S coverage-gap queue since it is no longer an open problem awaiting a solution. Doc-only; no code, tests, or architecture changes. [autonomous-loop, operator-directive] | **Demonstrates the loop correctly distinguishing "the operator changed their mind about a policy" from "new evidence contradicts a prior technical finding" — both are legitimate reasons to update a tree node, and this is the first instance this session of the former.** `PROBLEM_TREE` §7's own standing rule is "deferred — do not re-litigate without new evidence"; a direct, explicit, in-the-moment operator instruction is exactly the kind of new evidence that rule anticipates, and recording it durably (rather than just complying silently in the moment and leaving the tree's stale "→ Solution: … `redact_literal_secrets`…" text sitting there as bait for a future cycle) is what keeps the tree an honest, actionable record other agents and the operator can trust. It also demonstrates the loop's hard constraint in practice: an instruction that redirects scope mid-investigation is followed immediately and completely, with zero code written toward the now-forbidden direction, before any other work resumes. | 4325 ✅ |
| 2026-07-02 | **Corrected `SOLUTION_TREE` §4a's stale C5 gap-analysis claim that "Weiszfeld/Welzl centroid + provenance radius" was still open — both are fully delivered, live, and operator-facing today, verified directly against the code (`PROBLEM_TREE` §8, `SOLUTION_TREE` §4a/§5).** After closing the evidence-attribute-consistency vein in full (C5's geo-corroboration sweep), this cycle returned to the standing priority order and re-checked §4's gap analysis for any remaining item before running a fresh discovery pass. `util::geometry::location_fix` — confirmed via `git log --follow` to date to the repository's single root import commit, i.e. it predates every dated "Delivered" note in either tree, so this claim was never true at any point checked, not a drift that developed later — fuses the confidence-weighted geometric median (Weiszfeld, outlier-robust) with Welzl's minimum enclosing circle into one `LocationFix`. It is fully wired into AU-052 (`rule_au_052_geographic_area_of_operation`), whose `Correlation::description` embeds `fix.location_summary()`, surfacing BOTH the robust median radius and the Chebyshev worst-case bounding-circle radius as live, operator-facing text on every "area of operation" finding. Separately, AU-059's headline synergy fix (`au059_synergy_fix` — the multi-source "Best location estimate" the CLI dossier and JSON export both print) already uses `weighted_geometric_median` (Weiszfeld) with a `median_distance_km` provenance radius, and its own source comment explicitly cites "same fallback `LocationFix`… uses — PROBLEM_TREE C5" — direct proof the two rules were built as a deliberately consistent pair, not independently or incompletely. Welzl's worst-case bound is correctly scoped to AU-052 only (bounding a whole area of operation, where a worst-case radius is the right semantics) and correctly absent from AU-059 (a single best-point estimate, where the robust median radius is the right semantics) — a reasoned design choice visible in the code, not an oversight. Corrected the §4a bullet to drop the false claim; the primary C5 (`PROBLEM_TREE`) and SOL-GEOINT (`SOLUTION_TREE`) §2 node texts required no change, since neither of them ever claimed Weiszfeld/Welzl were open — only this one §4 gap-analysis bullet was wrong, confirming the drift was localised to the gap-analysis cross-reference, not the primary capability record. Only *auto-sync* (the `cell_local` re-sync trigger, already correctly tracked elsewhere) remains genuinely open for C5. Doc-only; no code, tests, or architecture changes. [autonomous-loop, stale-doc-audit] | **A false "still open" entry in the tracked gap-analysis queue is a standing invitation for a future cycle to spend a real unit of work re-implementing a feature that already exists and is already correctly designed** — exactly the failure mode this loop's step 1 priority order exists to prevent by checking §4 before defaulting to fresh discovery. Confirming, with `git log --follow`, that the claim was wrong from the moment the tree was authored (not a later drift) is itself useful provenance: it means the gap analysis was written without checking the code against this specific sub-claim, a pattern worth remembering when trusting other still-open §4 bullets that haven't yet been individually re-verified. Closing it here removes a plausible-looking but false target from the queue before a future cycle wastes a unit of work on it. | 4325 ✅ |
| 2026-07-02 | **`ipinfo`/`ipquery` geolocation fixes now count as person-location corroboration — closing the deferred follow-up from the `ip_whois_geo` fix, verified as the identical gap rather than assumed (`PROBLEM_TREE` C5, `SOLUTION_TREE` SOL-GEOINT).** The prior cycle fixed `ip_whois_geo`'s missing `ip` evidence-tie-back attribute but explicitly deferred `ipinfo`/`ipquery` as "plausible, but unconfirmed... left for a future cycle to verify rather than guess." This cycle did the verification the deferral called for, rather than either blindly extending the fix or leaving it open indefinitely. Both modules were confirmed genuine instances, not merely plausible ones, on two independent lines of evidence read directly from the code: (1) both gate their Coordinates emission behind the SAME "is this actually the subject, not infrastructure" trust logic — `crate::core::validation::untrusted_ip_geo_reason` (shared with `ip_geo`/`ip_whois_geo`) plus, for `ipquery`, its own additional VPN/Tor/proxy/datacenter risk-flag layer, whose doc comment states outright that admitting an untrusted fix "poisons identity-location correlation" — proving both modules' Coordinates ARE meant to represent the subject when they pass the gate; (2) both call the identical `util::geo::coarse_provider_coords(lat, lon, 0.58, scan_id)` helper carrying the identical "Confidence recalibrated 0.68 → 0.58 — see ip_geo.rs" comment, which is source-level proof they are siblings deliberately calibrated against `ip_geo`, not a differently-purposed module family (unlike `censys`/`onyphe`, which were correctly excluded as infrastructure/host-scan tools). Neither module stamped the `ip` evidence attribute that `core::correlator::rules::location::person_login_ip_coords` (shared by `best_au_location_estimate` and `au_location_corroboration`) requires to recognise a `Coordinates` fix as tied to a subject's breach/stealer login IP. Fixed both additively, mirroring `ip_geo`'s exact pattern and rationale comment: `ipinfo`'s evidence fold and `ipquery`'s shared `geo_ev()` closure (used for both its Coordinates and Address evidence — harmless, since `ip` has exactly one correlator consumer and it filters `EntityKind::Coordinates` only) now stamp `.with_attr("ip", ip)`. 2 regression tests (`coordinates_carry_the_originating_ip_for_login_ip_recognition`, one per module), red/green-verified together by reverting both production fixes and watching both assertions fail while `ip_whois_geo`'s own test (unaffected) stayed green. This closes C5's evidence-attribute-consistency sweep in full: of the 9 modules that accept an IP target and produce `Coordinates`, 6 were already correct (`ip2location`/`ip_geo`/`shodan`/`netlas`/`whois`, plus `ip_whois_geo` after the prior fix), 2 fixed across this cycle and the last (`ip_whois_geo` then `ipinfo`+`ipquery`), and `censys`/`onyphe` deliberately excluded throughout as infrastructure/host-scan tools where extension would risk fabricating corroboration rather than closing a real gap. No new `EntityKind`, no architecture-guard impact, no identity/PII decision logic, no clippy/unsafe posture touched. [autonomous-loop, correlation-accuracy] | **Completes the person-location corroboration signal across every legitimate free, no-key IP-geolocation source the engine dispatches — `ip_geo`, `ip_whois_geo`, `ipinfo`, and `ipquery` are now ALL recognised as independent corroborating classes when they agree on a subject's login-IP location, instead of only the first two.** Since all four are free and always-dispatched (no key gate), a typical scan against a subject with a breach-exposed login IP now has up to four independent geo-source opinions correctly counted toward `au_location_corroboration`'s independent-class count and `report.json`'s exported `corroboration` field, materially strengthening the evidentiary weight of a "this is where the subject connects from" finding. It also demonstrates the value of the deferral discipline established last cycle: rather than guessing whether a plausible-looking gap was real, the follow-up was scoped as an explicit open item and verified this cycle with concrete code evidence (shared trust gates, shared helper calls, matching comments) before a single line changed — exactly the register's own "never fabricate a finding" standard applied to deciding what to fix, not just what to report. | 4325 ✅ |
| 2026-07-02 | **`ip_whois_geo`'s geolocation fixes now count as person-location corroboration — it never stamped the evidence attribute the shared login-IP recognition logic requires, the third evidence-attribute-consistency silent miss found this arc (`PROBLEM_TREE` C5, `SOLUTION_TREE` SOL-GEOINT; after AU-105 dehashed/see_know and AU-011 social_probe).** Found by a systematic, code-grounded discovery pass: every evidence attribute any correlator rule reads (`grep` of every `.attributes.get(...)`/`.attr(...)` call across `src/core/correlator/`, including multi-line calls) was enumerated and cross-checked against the modules that could plausibly write the same semantic value. `core::correlator::rules::location::person_login_ip_coords` — the ONE shared definition both `best_au_location_estimate` (the CLI dossier + JSON export headline "Best location estimate") and `au_location_corroboration` (the independent-source count feeding AU-059 synergy) call — recognises a `Coordinates` fix as tied to a subject's breach/stealer login IP only when its evidence carries an `ip` attribute equal to that IP. `ip_geo` stamps it, with an explicit comment stating the tie-back exists precisely "to recognise a person's breach login IP." `ip_whois_geo` — whose own module doc comment frames it as "second-source geo module alongside `ip_geo` ... corroborating the location," and whose `build_entities` explicitly skips CDN/anycast edge IPs with the comment "its geo is the datacenter's, not the subject's" (proving in its own logic that its fix is meant to represent the subject) — never stamped it. So an ipwho.is geolocation on the EXACT SAME login IP `ip_geo` also resolved silently never counted toward the person-location signal, weakening the very multi-source corroboration this documented module pair exists to provide. The sweep was thorough, not speculative: all 9 modules that accept an IP target and produce `Coordinates` were checked (`ip2location`, `ip_whois_geo`, `ipinfo`, `ipquery`, `censys`, `shodan`, `netlas`, `onyphe`, `whois`); `ip2location`/`shodan`/`netlas`/`whois` were confirmed already correct. The fix was deliberately scoped to `ip_whois_geo` alone: `ipinfo`/`ipquery` are plausible instances of the identical gap but were not individually verified this cycle (left for a future cycle rather than guessed at); `censys`/`onyphe` are explicit infrastructure/host-scan tools whose geolocation fix more often reflects hosting infrastructure than the subject's own connection, so extending person-location recognition to them risks fabricating corroboration rather than closing a real gap — a worse outcome than missing coverage under this project's evidentiary doctrine. Fixed additively: `.with_attr("ip", ip)` added to `ip_whois_geo`'s evidence fold, mirroring `ip_geo`'s exact pattern and rationale comment verbatim. 1 regression test (`coordinates_carry_the_originating_ip_for_login_ip_recognition`), red/green-verified by reverting the production fix and watching the assertion fail. No new `EntityKind`, no architecture-guard impact, no identity/PII decision logic, no clippy/unsafe posture touched. [autonomous-loop, correlation-accuracy] | **Strengthens the exact signal an analyst leans on hardest: how many independent methods agree on where a subject actually is.** `au_location_corroboration`'s whole value is counting DISTINCT, independent geo-source classes that converge on one locality — an ipwho.is fix silently excluded from that count is not just a missed data point, it understates the corroboration strength reported to the operator (and exported structurally in `report.json`'s `best_location.corroboration` field) for every scan where both `ip_geo` and `ip_whois_geo` resolve the same subject IP, which is the common case since both are free, no-key, always-dispatched modules. This is the third instance this arc of the identical failure shape — a value present in the evidence, just filed under the wrong key — found by moving from ad hoc discovery to a systematic enumerate-then-cross-check sweep of the correlator's entire attribute-read surface, which is itself a durable technique this register should keep reapplying as new correlator rules and producing modules are added. | 4323 ✅ |
| 2026-07-02 | **`social_probe`'s multi-platform confirmations now feed AU-011 (cross-platform username footprint) — it was stamping the platform count under a non-canonical evidence attribute, so the finding silently never fired for it (`PROBLEM_TREE` §8, `SOLUTION_TREE` §5; same evidence-attr-consistency class as the AU-105 fix).** Found by a fresh code-grounded discovery pass: after confirming the determinism vein clean (every module HashMap→output path sorts with a deterministic tiebreak) and every attribute the correlator reads is written by some module, a closer comparison of the three aggregate username-probe modules exposed a real miss. AU-011 (`rule_au_011_cross_platform_username`) counts how many platforms ONE module confirmed a handle on by reading the `platforms_count` evidence attribute, falling back to counting distinct `PLATFORM_SOURCES` modules when it is absent. `username_search` and `streaming_probe` both stamp `platforms_count`; `social_probe` stamped only `found` (its profiles-checked count) and `platforms` (the CSV list) — never `platforms_count` — and `social_probe` is not on AU-011's `PLATFORM_SOURCES` fallback list. So a handle `social_probe` confirmed on ≥3 platforms (which the module itself tags `multi-platform`) registered as a count of 0 in AU-011 and silently never produced the cross-platform-footprint finding that exact confirmation exists to generate. The platform count was present all along, just under the wrong key — the identical failure shape as AU-105's breach name living under `source` instead of `dbname`. Fixed additively: `build_target_summary` now also stamps `platforms_count = found_platforms.len()` (retaining `found` for its own convention), matching the two sibling probes and the attribute AU-011 reads. 1 regression test (`build_target_summary_stamps_platforms_count_for_au011`), red/green-verified by reverting the production fix and watching the assertion fail. No identity/PII decision logic, no architecture guard, no clippy/unsafe posture touched. [autonomous-loop, correlation-accuracy] | **Restores a genuine cross-platform identity signal on the module purpose-built to produce it: `social_probe` confirms a handle across the social-platform family, and AU-011 exists precisely to surface "this username has a footprint on ≥3 platforms" — yet the two were silently disconnected, so an operator got the raw per-platform hits but not the higher-order footprint finding that weighs them into one identity assessment.** Cross-platform handle reuse is a foundational OSINT pivot (one persona spanning many services); suppressing it for one of the three modules that detect it understated the subject's confirmed online footprint. The fix also re-establishes a single `platforms_count` convention across all three aggregate probes, so the correlator reads one canonical field rather than a per-module alias — the same evidence-attribute-consistency discipline that keeps the correlation layer trustworthy, now applied a second time this arc. | 4322 ✅ |
| 2026-07-02 | **Corrected a stale "residual, real gap" claim in `SOLUTION_TREE` §4a: `hse update`'s `changelog_lines`/`commits_behind` were said to be untested, but a real git-fixture test already exists and passes (`PROBLEM_TREE` §8, `SOLUTION_TREE` §4a/§5).** This cycle's code-grounded discovery pass found no new small code gap — the TODO/FIXME/`unimplemented!` sweep was clean (only `XXXX` format placeholders in doc comments), and a `fullcontact` dropped-field hypothesis (`details.emails`/`phones` returned by the API but not deserialized) was correctly REJECTED as ungroundable: the repo carries only synthetic test fixtures built to the current struct, so asserting the live API returns those fields would be a fabricated finding, which the doctrine forbids. It did confirm one stale finish-queue claim against the code: `cli/update.rs` already carries `commits_behind_and_changelog_lines_reflect_real_git_state`, a fixture test that stands up a genuine local remote+clone git pair via `tempfile` (no network, isolated git identity, `commit.gpgsign=false` for portability) and asserts BOTH functions across three states — freshly-cloned (`Some(0)`/empty), advanced-remote (`Some(2)` with the two commit subjects newest-first), and not-a-git-repo (`None`/empty fallback). Ran it this cycle: it passes. Corrected §4a so the whole `hse update --check changelog` bullet is fully off the finish queue (feature present AND fixture-tested). Doc-only; no code, tests, or architecture touched. [autonomous-loop, stale-doc-audit] | **A false "untested" entry in the live finish queue is a self-inflicted trap: the next cycle scanning §4a for work would see it, go to add the "missing" fixture, and discover — after the effort — that it already exists. Keeping the gap analysis honest is what lets the loop trust §4 as its work queue rather than re-deriving ground truth every pass.** The rejected `fullcontact` hypothesis is equally load-bearing to record: it documents that the dropped-field vein is now exhausted for the modules with in-repo evidence, and that extending it further would require fabricating an external API's response shape — exactly the false-positive the register's own discipline exists to prevent. | n/a (docs) |
| 2026-07-02 | **Never-panics property coverage for `key_harvest::identify_api_key` — closing the `username_search` adversarial-input leg (T2.7 now 5/5) at the function that actually processes its attacker-controlled input (`PROBLEM_TREE` T2.7, `SOLUTION_TREE` SOL-F3).** Selected from the SOL-F3/T2.7 finish queue: the prior cycle left `username_search` (the last of 5 named scraper modules) "unconfirmed — table-driven, may not need a parser proptest." Scoping it against the code confirmed there is no bespoke parser — `Detect` is three total variants (`StatusEq`/`StatusAndBody`/`StatusAndNotBody`), resolved by an HTTP-status compare plus `str::contains`, both total — so a module-level proptest would be vacuous busywork. The real, uncovered surface is the function `username_search::scan_text_for_keys` feeds the fully attacker-controlled probe-page body into: `oathnet_pro::key_harvest::identify_api_key`. Its `mod prop` never-panics property covered only `identify_vendor_api_key`, but the PUBLIC `identify_api_key` is a strict superset — it adds a generic-hex gate, URL-embedded-key extraction (byte-slicing at a computed offset under a 4 KiB cap), a `user:password` split, and a RECURSIVE self-call — none exercised by the vendor-only property. Added a `.{0,512}` never-panics proptest for `identify_api_key`, plus a deterministic regression test driving a `?key=` value far past the extraction cap built from 3-byte characters, so the byte cap lands mid-codepoint (a raw `&rest[..cap]` slice would panic there; the extractor's `truncate_safe` boundary-snap keeps it total). The function proved already-total — no production bug surfaced — so this is pure adversarial-input hardening (a property test for the panic-on-hostile-input class), no production code changed. No identity/PII logic, no architecture guard, no clippy/unsafe posture touched. [autonomous-loop, F.3/T2.7] | **Puts a machine-checked crash-resistance guarantee under the exact key-harvesting path that runs over hostile profile-page bodies and stealer-log records — attacker-controlled bytes where a panic is a scan-killing DoS.** `identify_api_key` (not the vendor-only subset already covered) is what `username_search` and `oathnet_pro` stealer harvesting both feed untrusted input into, and its byte-slicing URL-param extractor was the one part with no fuzz coverage — precisely the shape (a computed-offset slice under a size cap) that panics on a multibyte boundary if the boundary-snap ever regresses. Also resolves T2.7's last open scraper-module question precisely (confirmed N/A-at-module-level, covered at the real delegate) rather than inflating it into a vacuous module-level test — the report-precisely discipline the register runs on. | 4321 ✅ |
| 2026-07-02 | **AU-105 credential-reuse now sees true per-breach granularity from `dehashed`/`see_know`, which were labelling the breach name under a non-canonical evidence attribute (`PROBLEM_TREE` C6, `SOLUTION_TREE` SOL-OFFENSIVE).** Selected from a fifth discovery pass's evidence-attr-consistency candidate (flagged only PARTIALLY_CONFIRMED), promoted to a real, actionable bug only after re-verifying the exact attribute names against both the correlator rule and the two modules — the fourth fifth-pass candidate this session requiring ground-truth re-derivation, and the reason the register logs verified defects rather than plausible summaries. AU-105 (`rule_au_105_credential_reuse`) measures a secret's reuse across DISTINCT breaches, and its `breach_of` helper reads the breach name from the `dbname` evidence attribute, then `breach`, then falls back to the Evidence `source` FIELD (which is the emitting module's name). `oathnet_pro` stamps `dbname` correctly. But `dehashed` (`build.rs::record_evidence`) and `see_know` (`extract/mod.rs::record_evidence`) stamped the per-record breach database name under a `source` **attribute** — a map key, distinct from the Evidence `source` FIELD that `breach_of` falls back to, and one `breach_of` never inspects. The consequence: every DeHashed record collapsed to the single pseudo-breach "dehashed" and every SeekNow record to "see_know", so a subject whose password recurred across three different breaches all surfaced by DeHashed registered as ONE breach — and the ≥2-distinct-breaches reuse finding, the entire purpose of AU-105 and the common case for these aggregating providers, silently never fired. (For `see_know` the miss was conditional: records whose raw JSON already held a literal `dbname` field worked through the raw-field fold; only those labelling the breach under `source`, which see_know's fold renames to `source_db`, were blind.) Fixed additively — both modules now ALSO stamp the canonical `dbname` attribute (retaining `source` for existing consumers), matching `oathnet_pro`'s convention — so AU-105 observes true per-breach granularity. 2 regression tests (one per module), each red/green-verified by reverting the production fix and watching the assertion fail. No identity/PII decision logic, no architecture guard, no clippy/unsafe posture touched. [autonomous-loop, workflow-assisted, C6] | **Restores one of the most actionable people-centric breach findings the engine produces — "this subject reuses one credential across N breaches, so a single crack opens every account" — on the exact providers (DeHashed, SeekNow) whose whole value is aggregating many breaches into one query.** Credential-reuse-across-breaches is the credential-stuffing / account-takeover surface, and it was being silently suppressed for precisely the sources most likely to surface it, because the reuse count could never exceed 1. The fix also re-establishes a single, consistent breach-name attribute convention (`dbname`) across all three breach-feeding modules, so the correlator and any future consumer read one canonical field rather than a per-module alias — the same evidence-attribute-consistency discipline that keeps the correlation layer trustworthy. | 4319 ✅ |
| 2026-07-02 | **Corrected the §7 S4 security node's stale key-in-URL module enumeration — it was BOTH overstated and understated (`PROBLEM_TREE` §7 S4, `SOLUTION_TREE` SOL-REDACT context).** Selected from a fifth discovery pass's stale-doc-sweep, with the corrected list derived by re-verifying every keyed module's actual authentication mechanism against the code rather than trusting the candidate's summary — the third fifth-pass candidate this session to require ground-truth re-derivation. The S4 node (which exists to bound the residual key-**exposure** surface) enumerated "~7 modules" putting the key in the query string: `shodan`/`hunter_io`/`whoisxml`/`numverify`/`opencellid`/`opencorporates`/`mls`. A grep of every module's URL construction and header calls showed this was wrong in both directions: (1) **overstated** — `numverify` was migrated to `apikey`-**header** auth (`.header("apikey", key)` against `api.apilayer.com`, no key in the URL), so it is no longer key-in-URL at all; (2) **understated** — four key-in-URL modules were missing: `hlr_cnam` (`api_key=` plus its OpenCNAM stage's `auth_token=`), `contact_enrich` (`access_key=`), `cell_intel` (its OpenCelliD helper's `key=`), and `ipqs`, which embeds the key in the URL **path** (`/api/json/{endpoint}/{key}/…`) rather than the query string. Corrected the enumeration to the accurate 10 modules (9 query-string + `ipqs` path), documented `numverify`'s migration as an instance of the node's own "prefer header auth" mitigation already applied, aligned the `redact_credentials` masked-param list with the actual code set, and — explicitly flagged as a follow-up NOT undertaken this cycle to avoid scope creep — noted that `ipqs`'s path form and the `access_key=`/`api_token=`/`auth_token=` param names fall outside that redaction set. Doc-only; no code, tests, or architecture touched. [autonomous-loop, workflow-assisted, stale-doc-audit] | **A wrong residual-surface enumeration in a security node is worse than a wrong count elsewhere: S4 is precisely the map an operator or auditor uses to reason about where an API key could leak, so naming a header-auth module as key-in-URL (false alarm) while omitting four real ones (blind spots) actively misdirects that reasoning.** Getting it right also surfaced two genuinely useful facts the old note obscured — that the "prefer header auth" mitigation has already been applied once (`numverify`), showing the residual is shrinking, and that a key can ride in the URL *path* (`ipqs`), a form the query-parameter redaction wasn't written to catch. This is the same doc-drift-in-a-security-claim pattern the register's own discipline exists to catch before it compounds. | n/a (docs) |
| 2026-07-02 | **`au_people` no longer discards cross-source corroboration: its `(kind, value)` dedup now GREATEST-merges duplicates instead of keeping the first and dropping the rest (`PROBLEM_TREE` SOL-MERGE enforcement, `SOLUTION_TREE` SOL-MERGE).** Selected from a fifth discovery pass's dedup-merge-correctness candidate, verified directly against the code. `au_people` scrapes two independent Australian residential directories — White Pages AU and True People Search AU (plus a relatives pass over each) — accumulates every hit, then calls `dedup_by_kind_value`, which used `HashSet::retain` to keep the FIRST entity per `(kind, value)` and discard subsequent ones. But the same address or phone is routinely listed by BOTH directories, and each source emits an entity with the same normalised `(kind, value)` — therefore the same SHA-256 UID — carrying its own distinct source evidence. Keep-first silently discarded the second directory's independent confirmation *at the module boundary*, before the ScanEngine's own UID-merge (which correctly applies the project's GREATEST-semantics `Entity::merge`) could ever observe it — permanently losing the max-of-two confidence, the summed corroboration count, and half the evidence trail on precisely the facts two sources agreed on. Fixed by rewriting `dedup_by_kind_value` to fold duplicates through `Entity::merge` (max confidence, saturating corroboration, evidence + tag union, lexicographic-min canonical spelling), preserving first-occurrence order; `merge` is commutative in the folded signal so the output is order-independent. 1 regression test, red/green-verified by reverting the fix and watching it fail: it asserts the surviving entity carries BOTH directories' evidence records, a corroboration count of 2, and the higher of the two confidences — all three of which the old keep-first behaviour failed. The pre-existing `dedup_removes_same_kind_value` test (which only asserted the post-dedup count) still passes unchanged. No identity/PII decision logic, no architecture guard, no clippy/unsafe posture touched. [autonomous-loop, workflow-assisted, SOL-MERGE] | **Restores real corroboration strength to people-finder findings — the difference between "one directory lists this address" and "two independent Australian directories both list this address," which is exactly the multi-source agreement an analyst (or a court) weighs when deciding whether a residential-address or phone attribution is trustworthy.** Corroboration count and evidence provenance are load-bearing for this engine's confidence model (`C_eff` grows with `ln(corroboration)`) and for its evidentiary-transparency promise; silently dropping a second independent source both understates confidence and erases a source from the provenance trail. It also demonstrates the GREATEST-merge invariant the whole identity model rests on being enforced at a module boundary that had quietly bypassed it — the kind of local shortcut that undermines a global correctness guarantee. | 4317 ✅ |
| 2026-07-02 | **Property-test coverage for `util::extract`, the shared free-text identifier miner every scraper/breach/stealer-log parser runs over attacker-shaped input (`PROBLEM_TREE` F.3, `SOLUTION_TREE` SOL-F3).** Selected from a fifth discovery pass's util-proptest-coverage candidate — and only after adversarially re-verifying and *rejecting* two other fifth-pass candidates that did not survive contact with the code: a claimed `HostedOn` Url→IpAddress relation-builder gap (the `HostedOn` edge is deliberately Url→**Domain** per its own type doc, and IS derived in `derive_structural`; Url→IpAddress is an intentional 2-hop `HostedOn`→`ResolvesTo` path, so fabricating a direct edge would contradict the schema), and a claimed CLI/SPA `report.json` "Exposure Index" parity gap (the CLI `render_report` and the HTTP endpoint both call the identical `api::scan_export::build_scan_report`, so the envelopes cannot diverge, and no "Exposure Index" metric exists anywhere in the tree). `util::extract` had thorough example-based tests but ZERO property coverage, despite housing exactly the code F.3's own solution statement flags as the panic surface: the byte-walking `page_emails` (which walks `text.as_bytes()` and re-slices at computed offsets — a classic UTF-8-boundary panic risk) and the char-filtering/slicing `ibans`/`macs` normalisers. Added a `mod prop` of 7 `proptest!` properties over arbitrary `.{0,256}` strings: totality-plus-well-formedness for `emails`/`page_emails`/`ibans`/`macs`/`phones`/`labeled_ssids` (each asserts the extractor's specific output contract — lower-case + dotted-host for emails, upper-alnum + valid length for IBANs, canonical lowercase colon form ≠ zero/broadcast for MACs, `+`-prefixed 10–15 digits for phones, 1–32 chars for SSIDs) and totality-plus-internal-consistency for `classify_credential_field` (an `Email` verdict implies `looks_like_email`, a `Sentinel` implies empty-or-placeholder, a `Secret` implies neither). The properties deliberately encode a real behavioural asymmetry rather than a lazy blanket no-panic: strict `page_emails` output is asserted to always satisfy the structural `looks_like_email` gate, whereas the looser regex-based `emails` is explicitly NOT (it can legitimately match a dot-leading host like `a@.example.com` that the gate rejects) — asserting `looks_like_email` on `emails` would have been a false invariant. The extractors proved already-total (no production bug surfaced), so this is a pure test-hardening addition; no production code, no identity/PII logic, no architecture guard, no clippy/unsafe posture touched. [autonomous-loop, workflow-assisted, F.3] | **Puts a machine-checked crash-resistance + output-contract guarantee under the single most exposed parsing surface in the engine — the one that ingests raw HTML, SERP snippets, breach blobs, and stealer logs, i.e. bytes an adversary fully controls.** A panic here is a denial-of-service on the whole scan (one malformed scraped page aborts the run), and a malformed extracted token (a non-lowercased email that breaks dedup, a mis-shaped MAC that fails geolocation, a bad IBAN) silently corrupts downstream correlation. Property tests over arbitrary input are exactly the right proof for a scanner-grade parser whose whole job is to stay robust on hostile text, and this closes the specific `util::extract` gap F.3's plan called out by name. Equally valuable: the cycle demonstrates the discovery→verification discipline rejecting 2 of 3 candidates as not-real before spending effort — the register logs genuine gaps, not plausible-sounding ones. | 4316 ✅ |
| 2026-07-02 | **`hse diagnostics` now correctly aggregates a self-test failure instead of dying mid-pass — `cmd_selftest` was signalling failure with a bare `std::process::exit(1)` that killed the whole process (`PROBLEM_TREE` T2.12 follow-up, `SOLUTION_TREE` §5).** Selected from a fifth discovery pass's CLI-contract sweep. The aggregate `hse diagnostics` command runs three sub-checks in sequence (doctor → selftest → engines) and both its doc comment and its code promise to run *all three* "in a single pass" and, on any failure, return one aggregate `Err` naming which section(s) failed (it accumulates failures into a `Vec<&str>`, wrapping each sub-check in `if let Err(e)`). But the second sub-check, `selftest::cmd_selftest`, signalled failure by calling `std::process::exit(1)` **directly** rather than returning an error — so on a failing self-test the entire process terminated at section 2/3: the `engines` liveness sweep (3/3) never ran, the `if let Err(e)` arm that was meant to catch selftest was structurally unreachable, and the promised "N section(s) failed" summary never printed. `cmd_selftest` was the *only* one of the three sections that hard-exited (`doctor`/`engines` correctly return `Result` and were already caught). This is the same exit-code-contract defect class T2.12 audited and fixed elsewhere (`audit` always returning `Ok`, `provision --verify` exiting 0 on failure), but it escaped that sweep because `cli/diagnostics.rs` was added 2026-06-30 — 13 days *after* the 2026-06-17 T2.12 periphery audit, so the aggregate command did not exist to be checked. Fixed by having `cmd_selftest` *return* an `Err` on failure via a new pure `report_to_result(&Report)` helper — the binary's `main` already maps any returned error to `std::process::exit(1)`, so the standalone `hse selftest` non-zero-exit contract (its CI/post-install smoke-gate role) is byte-for-byte unchanged, while `cmd_diagnostics` can finally catch the failure and complete its pass. Extracting the pure helper was necessary because the old `process::exit` path is inherently untestable (it kills the test process); the helper is now covered by 2 unit tests (all-pass report → `Ok`; any-fail report → `Err` whose message names the `failed`/`total` count). No identity/PII logic, no architecture guard, no clippy/unsafe posture touched. [autonomous-loop, workflow-assisted, T2.12] | **Restores the exact guarantee a diagnostics aggregate exists to provide: one command that checks the whole install and tells you *everything* that is broken, not just the first thing.** An operator (or a CI/post-install gate) running `hse diagnostics` on a partly-broken build would have seen the process die at the self-test with no engines check and no summary — misleading them into thinking the self-test was the sole problem when the search-engine liveness sweep was never even attempted. Worse, the same hard-exit meant the command's documented "runs every diagnostic in a single pass" contract was silently false. This is the periphery-contract-honesty discipline T2.12 established, applied to a command that postdated it — a returned error is observable and composable; a `process::exit` buried in a library function is neither. | 4309 ✅ |
| 2026-07-02 | **`shodan`'s paid host lookup now surfaces the host classification `tags` its free InternetDB path already emits — the seventh "dropped-field depth gap" this session found and closed, after `austlii`, `wigle`, `virustotal`, `hunter_io`, and `proxycurl` (`PROBLEM_TREE` C4, `SOLUTION_TREE` SOL-NETINT).** Selected from a fifth discovery pass's round-4 dropped-field sweep, verified directly against the two code paths before acting. The `shodan` module merges a free path (`InternetDbResp`, no key) and a paid path (`HostResp`, keyed `/shodan/host/{ip}`), and its own doc comment frames the paid path as a strict superset of the free one — yet `InternetDbResp` deserialized and emitted the top-level `tags` array (`compromised`, `malware`, `honeypot`, `self-signed`, `vpn`, `cloud`, `cdn`, …), applying a `tags` evidence attr plus a per-tag `shodan:<tag>` entity tag, while the paid `HostResp` struct had **no `tags` field at all** — so serde silently dropped the array (no `deny_unknown_fields`) and a keyed operator, who pays for the richer endpoint, received *less* threat classification than a free user. Shodan documents the same `tags` array on both the InternetDB and the paid host record, so this was a pure parity omission, not a schema difference. Fixed by adding `#[serde(default)] tags: Vec<String>` to `HostResp` and an emission block in `query_paid` that mirrors the free path **to the letter** — same `ev.with_attr("tags", body.tags.join(","))`, same `body.tags.iter().for_each(|t| entity.tag(format!("shodan:{t}")))` — so an entity's `shodan:compromised`/`shodan:malware`/… tags and `tags` attr are now identical regardless of which tier produced it, and existing correlation rules can pivot on them uniformly. No new `EntityKind`, no `produces()` change (the paid path already yields `IpAddress`), no architecture-guard impact. 1 new serde round-trip test (`paid_host_resp_deserializes_the_tags_array`): a populated `tags` array deserializes into the field, and an absent `tags` defaults to empty rather than failing. [autonomous-loop, workflow-assisted, C4] | **Surfaces Shodan's single highest-value threat signal on the very tier an operator pays for, closing a parity inversion where the free path returned more than the keyed one.** Shodan's host `tags` are its curated classification verdict — `compromised` (host is part of a known breach/botnet), `malware`, `honeypot`, `self-signed`, `vpn`/`cloud`/`cdn` — and are precisely the field an infrastructure analyst pivots on first. Dropping them on the paid path silently degraded the keyed operator's result below the free one while the module's own doc promised the opposite, exactly the "claims a capability it doesn't deliver" failure mode this session's data-depth sweeps keep catching. Emitting them as both an evidence attr and per-tag entity tags means downstream correlator/graph rules see one consistent tag vocabulary across both Shodan tiers. | 4307 ✅ |
| 2026-07-01 | **The full dossier now keeps its own "nothing omitted" promise — `render_full` was silently dropping three entity-level fields (`PROBLEM_TREE` C7, `SOLUTION_TREE` SOL-FORENSIC).** Selected from a fourth discovery pass's export-renderer-completeness candidate; its verifier was rate-limited before returning a verdict, so this cycle re-verified the finding directly against the code before acting rather than trusting an unverified candidate. `render_full`'s module doc comment promises the full dossier is "complete, unredacted … every attribute verbatim … nothing hashed, masked, truncated, or omitted", but its per-entity block only ever printed the normalised `value`, confidence/corroboration/class, tags, MITRE tags, and evidence attributes — it never emitted the entity's own SHA-256 `uid`, its pre-normalisation `raw_value`, or its `observed_at` timestamp, all three of which `render_json` (whole-struct serde) and the CSV export already carry. This is not cosmetic: `Entity::normalise` makes `raw_value` genuinely diverge from `value` for Email (lowercase + escape/quote stripping), Username (sigil + case), and Domain (`www.`/case/dot trimming), so a human reading the "full, unredacted" forensic artifact never saw the actual source spelling a finding was captured under. The pre-existing `render_full_dumps_every_field_and_provenance` test didn't catch the gap because its fixture was an `EntityKind::Password` entity — a passthrough-normalise kind where `raw_value == value` — so the "every field" claim was never exercised for a divergent case. Fixed by emitting `uid`/`raw_value`/`observed_at` (raw Unix seconds plus a compact-UTC rendering via the existing `util::timefmt::compact_utc` helper) in `render_full`'s per-entity block; strengthened the test with a second fixture — a mixed-case Email (`TestUser@Example.COM` → normalises to `testuser@example.com`) whose `raw_value` provably diverges — asserting both spellings, the timestamp in both forms, and the uid all appear. Confirmed it is a genuine regression test by stashing the renderer fix, re-running, watching the `raw_value` assertion fail, then restoring. Additive text-renderer change only; no identity/PII decision logic, no architecture guard, no clippy/unsafe posture touched. [autonomous-loop, workflow-assisted, C7] | **Closes a real gap between what the flagship "full dossier" artifact promises a professional interpreter and what it delivers — and the missing field is precisely the forensically load-bearing one.** For a reproducible-intelligence deliverable, the pre-normalisation `raw_value` IS the chain of custody: it is the exact bytes the source returned before Huntsman canonicalised them, so an analyst (or a court) can trace a normalised `testuser@example.com` back to the `TestUser@Example.COM` actually observed. Dropping it while the doc comment loudly promised total transparency is exactly the "claims a capability it doesn't have" failure mode this session's audits keep catching — here in the single artifact whose entire contract is that it hides nothing. | 4306 ✅ |
| 2026-07-01 | **New correlator rule AU-112 — a discovered IP inside a discovered announced network block, the one rule that reads `EntityKind::Cidr` (`PROBLEM_TREE` C4, `SOLUTION_TREE` SOL-NETINT).** Selected from a fourth discovery pass's systematic correlator-coverage sweep, which read ~45 rule doc comments against their implementations and found NO doc-vs-code mismatch (the rules are honest — a clean negative result, reported precisely), but surfaced a real cross-reference gap: `EntityKind::Cidr` (announced BGP prefixes and whois netblocks) is produced by four modules (`bgpview`, `ripestat`, `netblock`, `intelx`), each stamping the owning ASN/org into the entity's evidence, yet `grep -rln "Cidr" src/core/correlator/` and `src/core/relation/` both returned zero matches — no correlator rule and no relation builder ever read it. So on every infrastructure-heavy scan, a subject's IP and the very network block that provably contained it both sat in the graph, unconnected. AU-112 (a new entity-only rule in `infra.rs`) closes it: for each discovered `IpAddress` it tests containment against each discovered `Cidr` block and, on a hit, emits a Medium correlation attributing the address to that block's owner (read from the `asn`/`name` evidence the source already stamped, rendered as the org name or `AS<n>`). The containment test reuses the pure, offline `util::spf::{Ipv4Cidr, Ipv6Cidr}` primitives (overflow-safe bitmask maths on std `Ipv4Addr`/`Ipv6Addr`, no I/O, no deps) — the discovery pass's own plan proposed hand-rolling new masking code "adapted from netblock's expand_cidr", but the adversarial verification step caught that a general-purpose, already-tested containment primitive existed and the plan would have produced needless duplicate CIDR maths. Reusing it required a narrow addition to the `core_does_not_import_util_directly` architecture guard's allowlist for those two pure structs, mirroring the existing `util::geometry` carve-out for the geo rules' estimators exactly — the same no-I/O/no-deps justification, extending the guard's designed pure-leaf-helper mechanism rather than weakening it. Registered in the `RULES` array; all four applicable correlator architecture guards pass (`every_dispatched_correlation_rule_has_a_firing_test`, `correlation_rule_ids_match_their_function_number`, `no_two_correlation_rule_functions_share_a_number`, and the amended import guard). 5 unit tests: in-block IPv4 fire with the owner name surfaced, bare-ASN fallback rendering `AS<n>`, out-of-block no-fire, IPv6 containment, and a malformed/cross-family input case proving no panic and no false fire. [autonomous-loop, workflow-assisted, C4] | **Turns four modules' worth of already-collected BGP/netblock data from an inert entity kind into an actionable network-ownership attribution — a Maltego-style "this IP belongs to that org's announced infrastructure" pivot the engine had all the pieces for but never assembled.** Attributing a discovered IP to the owner of the block that announces it is foundational NETINT: it links a subject's address to its hosting/ISP/organisation via the routing table itself (authoritative, not inferred), and connects two entity kinds the engine spent real API calls (`bgpview`/`ripestat` are keyed) to collect but had left in separate, unlinked islands. It also demonstrates the discovery→verification discipline working as intended for the third time this session: the plan's own proposed implementation contained a real error (duplicate hand-rolled CIDR maths) that only surfaced when the verifier re-derived feasibility against the actual codebase and found the existing primitive. | 4306 ✅ |
| 2026-07-01 | **Corrected the top-of-file "Current baseline" header's two badly-stale aggregate counts: "64 native correlation rules (AU-001…AU-064)" → 109 (ceiling AU-111), and "126 modules" → 161 (`PROBLEM_TREE` baseline).** Selected from a fourth discovery pass's stale-doc sweep, which independently checked three OTHER suspected-stale claims and confirmed all three still accurate (C2's "no published throughput/RAM numbers" — no bench/fuzz artifact contradicts it; F.3's "cargo-fuzz remaining" — `find -iname "*fuzz*"` returns nothing; C4's "SSL-cert-hash pivot needs a new data source" — no cert-hash search in censys/shodan), so the sweep reports precisely rather than inflating a find. The correlation-rule count was verified against the live dispatch tables in `src/core/correlator/mod.rs`: `const RULES` holds 96 `rule_au_*` entity-rule entries and `const RELATION_RULES` holds 13 more relation-graph rules (zero overlap, confirmed by sorted-name `comm -12`), totalling 109 unique dispatched rule functions with an ID ceiling of AU-111 (added the same day in commit `ae49da6d`). The changelog's own running "Rule count N→N+1" self-tracker had stopped advancing at "68→69" roughly 40 cycles earlier while the AU-070s through AU-111 families kept landing — so the top-of-file baseline was off by 45 rules and 47 in the ID ceiling. The module count was separately verified stale via the CI-guarded README overview count (`readme_module_overview_count_matches_registry` keeps it live): 161 modules registered, not 126. Corrected both aggregate figures; deliberately did NOT re-derive the 14 per-category sub-counts (Infrastructure 21, Geo 20, …), which need running code to re-tally accurately — rather than guess, flagged them explicitly as the unverified 2026-06-18 snapshot. Doc-only; no code, tests, or architecture changes. [autonomous-loop, workflow-assisted, stale-doc-audit] | **A seventh confirmed instance of the same drift pattern this session keeps finding, and one that matters more than most because it is the very FIRST substantive claim a reader (human or a future autonomous cycle) encounters about the engine's scale.** A "Current baseline" header understating the correlation-rule count by 45 (64 vs 109) and the module count by 35 (126 vs 161) misrepresents the engine as far less mature than it is — precisely the "the docs describe a smaller/different codebase" reconciliation problem this very register was opened to guard against (see its own baseline note about the seeded v9.0.0 figures). Getting the headline scale numbers honest is foundational to every downstream claim resting on them. | n/a (docs) |
| 2026-07-01 | **`proxycurl` now surfaces the `certifications` its own `description()` promised but never parsed — the fifth "dropped-field depth gap" this session found and closed, after `austlii`, `wigle`, `virustotal`, and `hunter_io`.** Selected from a fourth discovery pass's round-3 dropped-field sweep (which also ruled out shodan/criminal_ip/onyphe/zoomeye/leakix/threatfox/ipqs/epieos/seon/whoisxml/dehashed as already-complete or lower-signal). Unusually self-contradicting: `proxycurl`'s `description()` string literally advertised "LinkedIn profile extraction — employment, education, and certifications via Proxycurl", and the module doc's own field→output mapping table covered every field *except* certifications — but `struct LinkedInProfile` (in `types.rs`) had no `certifications` field at all, so serde silently dropped the entire array Proxycurl's Person Profile API returns (no `deny_unknown_fields`). Added a `Certification` struct (`name`/`authority` fields) with a `describe()` method producing `"Name (Authority)"` (or just `"Name"` when the authority is absent), mirroring the existing `Education::describe()` precedent in the same file exactly; a `#[serde(default)] certifications: Vec<Certification>` field on `LinkedInProfile`; and a fold into a `certifications` evidence attribute on the `Person` entity in `build.rs` (capped at `MAX_LISTED`, joined with `"; "`, following the `education` evidence-attr pattern to the letter — no new `EntityKind`, no architecture-guard impact). Also added the missing table row to the module doc comment, so the code's own field→output mapping now matches the behaviour its prose already promised. 3 new unit tests: the `describe()` name+authority / name-only / no-name-yields-None cases, a full-profile fixture assertion, and an absent-certifications no-op regression. [autonomous-loop, workflow-assisted, data-depth-sweep] | **Surfaces genuine professional-credential intelligence a paid API already returns and the module already promised to extract — for free, no new API surface or key.** A subject's professional certifications (security clearances, cloud/vendor certs, professional licences) are a real identity-and-competence signal for OSINT: they corroborate claimed employment, reveal regulated-profession status, and can pivot to licence registries. The self-contradiction made this worse than a plain omission — the module's own `description()` (surfaced to the operator in `hse doctor`/module listings) claimed a capability the code demonstrably lacked, so an operator relying on that description would have silently missed data on every LinkedIn profile scanned. | 4301 ✅ |
| 2026-07-01 | **Corrected a stale "AU postcode ≈72 entries" figure to ≈100 across three prescriptive doc spots — the gazetteer grew to 96 entries 8 days after the figure was recorded, and 20 subsequent doc-touching commits never revisited it (`PROBLEM_TREE` F.2, `SOLUTION_TREE` SOL-F2).** Selected from the third discovery pass's third stale-doc sweep, which also independently checked T2.9's SQL tie-break claims and T2.12's CLI/periphery-contract claims against live code and found both fully accurate — the sweep does not always find a stale claim, and this cycle's finding is reported precisely rather than inflated. `src/util/postcode_au/mod.rs::offline_fallback` has 96 match arms today, confirmed by direct count, consistent with the function's own doc comment ("the same ~100 postcodes"). `git log -S "AU postcode ≈72"` traced the figure's origin to commit `868a83a2` (2026-06-18, correct at the time), and `git log --oneline -- src/util/postcode_au/mod.rs` found the actual growth in commit `a6f09f83` (2026-06-26, "add 24 major AU regional cities") — 72+24=96, an exact arithmetic match confirming the mechanism, not just the timing. Corrected the three genuinely prescriptive occurrences (PROBLEM_TREE.md's F.2 §2 node text, SOLUTION_TREE.md's SOL-F2 §2 node text and its §4b "finish queue" summary) to "≈100", deliberately matching the gazetteer's own doc-comment wording rather than the exact "96" so a future gazetteer addition within that range doesn't immediately re-stale the note. Left two §5/§8 historical log entries dated 2026-06-18 untouched — they are accurate point-in-time records of what the count was when written, and this session's established convention treats those as append-only rather than editable. The adjacent "phone area codes ≈65 entries" figure in the same paragraphs was checked too and found genuinely ambiguous rather than confirmed-stale: `AU_AREAS` in `phone_geo/data.rs` has only 5 entries (AU-specific), while all `*_AREAS` tables for every supported country combined total 83 — neither figure cleanly matches "≈65", so it was left uncorrected rather than guessing which scope the original note intended. [autonomous-loop, workflow-assisted, stale-doc-audit] | **A sixth confirmed instance of the same drift pattern this session has repeatedly found and fixed** (C1's AU-0xx audit, C5's provenance radius, AU-060/084's rule-number reassignment, SOL-UPDATE's diff-summary claim, C6's "no comparably small gap" self-correction, C3's ASIC/cadastre undercount, §7 S2's marker mismatch) — a table-size justification for a real architectural decision (`fst` adoption rejected as overkill for Huntsman's curated-subset table sizes) drifting stale doesn't change the decision's correctness here, but a stale supporting number left unrevisited across 20 commits is exactly the kind of small inaccuracy that erodes a reader's trust in the surrounding claim, and that this loop's own stale-doc-audit discipline exists to catch before it compounds further. | n/a (docs) |
| 2026-07-01 | **`hunter_io` now surfaces the `linkedin`/`twitter` social-profile fields its own domain-search response already returns — the fourth "dropped-field depth gap" this session found and closed, after `austlii`, `wigle`, and `virustotal`.** Selected from a third discovery pass's round-2 dropped-field sweep. `struct HunterEmail` deserialized only `value`/`confidence`/`first_name`/`last_name`/`position`/`department`/`sources`, silently discarding the per-email `linkedin`/`twitter` fields Hunter's domain-search endpoint returns — no `deny_unknown_fields`, so serde dropped them with no trace. The discovery pass's own initial plan assumed both fields are always full profile URLs and proposed pushing both as `EntityKind::Url`; the adversarial verification pass caught this was wrong — Hunter's own published example responses show `twitter` as a bare handle (e.g. `"ciaran_lee"`), not a URL, and the codebase already has an established, correct convention for exactly this ambiguity: `fullcontact::build_entities` inspects the actual value's shape (`starts_with("http")`) rather than assuming a fixed type per field name, emitting `EntityKind::Url` for URL-shaped values and a platform-prefixed `EntityKind::Username` otherwise. Implemented following that precedent instead of the flawed plan: a shared loop over `(network, value)` pairs emits `EntityKind::Url` when the value looks like a URL, else a platform-prefixed `EntityKind::Username` (`"twitter:handle"`), both tagged `social-profile`. `produces()` updated to declare the now-possible `Username` kind (verified against the `every_literal_constructed_entity_kind_is_declared_in_produces` architecture guard). 3 new unit tests: a LinkedIn-URL case asserting a `Url` pivot, a Twitter-handle case asserting a `Username` pivot, and an absent-fields case asserting no spurious pivots. [autonomous-loop, workflow-assisted, data-depth-sweep] | **Surfaces genuine social-profile pivot intelligence Hunter.io already returns on a call the engine already makes, and demonstrates the value of adversarially verifying a discovery candidate's own implementation plan, not just its evidence.** A LinkedIn or Twitter handle discovered alongside a corporate email is a real identity-correlation lead (cross-referencing a subject's professional and social presence), and the field was already paid for and returned — only unused. Independently, this is the second time this session a discovery candidate's plan itself contained a factual error caught only by re-deriving the fix from the codebase's own type signatures/conventions rather than trusting the plan's prose (the first was the AU-111 correlator rule's entity-tagging assumption) — both times the error was caught before implementation, not after, by treating "the plan says so" as a claim to verify, not a fact. | 4299 ✅ |
| 2026-07-01 | **`Address` gains the same breach/exposure-dork coverage `Phone`/`FullName` already got, closing a second, real slice of the same C6 gap (`PROBLEM_TREE` C6, `SOLUTION_TREE` SOL-OFFENSIVE).** `search_engines::queries::exposure::build_queries_exposure`'s doc comment blanket-excluded `Address` alongside `CryptoAddress` on the shared claim that "exposure dorking doesn't add signal beyond `build_queries_base`" — true for `CryptoAddress` (its own base arm already bakes in scam/fraud/sanctions/attribution dorks) but false for `Address`, whose base arm (real-estate/land-registry/strata/ABN dorks only) has zero pastebin/dehashed/leakcheck/snusbase/github-credential/s3 coverage — exactly the same shape of gap this session already fixed for `Phone`/`FullName`. Added `address_exposure` (5 dorks: breach-dump/pastebin lookups naming the address, github/s3 config-leak dorks, people-search aggregator dorks — mirroring the file's five existing per-kind helpers exactly), corrected the doc comment to drop `Address` from the excluded-kinds list. `AbnAcn` — the other unhandled identity-adjacent kind — was deliberately left excluded rather than force-fit into the same fix: a registry number's breach-relevance is genuinely weaker than a street address's (it rarely appears in a credential dump the way an address, phone, or full name does), so extending scope to it wasn't justified by the same evidence. 2 new tests (a dispatch-level test asserting the exposure pass fires for `Address` and includes a dehashed.com dork naming the address; a shape-level test asserting the exact 5-dork output), mirroring the established `_targets_breach_dumps`/`_exposure_shape` test pattern. Re-ran the full 291-test `search_engines` suite (not just the touched file) to confirm the pre-existing `build_queries_address_produces_dorks` integration test still passes unchanged. [autonomous-loop, workflow-assisted, C6] | **Closes a real search-coverage gap for a core, commonly-used OSINT seed type — a physical address is one of the most identity-anchoring seeds an operator can start from, yet it alone among several other identity/asset kinds got no breach-exposure dork pass at all.** The doc-comment correction matters independently, continuing a pattern this session has repeatedly found and fixed: a comment whose claimed exclusion list doesn't match the code's actual behavior actively misleads a future reader (including a future autonomous cycle) about what coverage already exists, risking either a missed real gap or wasted re-verification of a gap that was already closed. | 4296 ✅ |
| 2026-07-01 | **A third, differently-designed dossier hint closes real T2.14 value without hitting the per-module noise problem that blocked two prior attempts to literally reinstate either of the two original dead hints (`PROBLEM_TREE` T2.14, `SOLUTION_TREE` SOL-HINT-NOISE).** Two earlier investigations this session had already found the node's own "the 60s reinstatement is simple" premise false — the old scan-level condition carries the same per-module noise problem T2.14 explicitly reserves for the harder per-module hint. Rather than a third attempt at the same literal reinstatement, this cycle designed a new, scan-wide hint instead: `total_dead_scan_hint` fires a single "N module(s) ran and found nothing scan-wide" line only when `entities.is_empty() && scan.modules_run > 0` — categorically distinct from "many modules found nothing for this target kind" (normal on a large multi-module scan, not noteworthy), since it fires at most once per scan and only on a near-certain misconfiguration/dead-target signal, correctly staying silent when a scan legitimately dispatched nothing at all (`modules_run == 0`, e.g. every candidate module was gate-skipped for an unsupported target kind). New pure helper in `cli/scan/dossier.rs`, mirroring the established `zero_yield_keyed_or_paid_modules` pattern exactly; prepended to the OPTIMIZATION HINTS list so it's not buried. 3 unit tests covering the fire/no-fire-on-gate-skip/no-fire-on-success cases. Documented honestly rather than glossed over: this doesn't remove the pre-existing "pipeline is well-tuned for this seed" fallback line, which can still print alongside the new hint on an empty scan (a cosmetic redundancy, not a correctness issue) — left for a future polish pass. The two ORIGINAL dead hints (scan-level 60s-and-zero-yield, per-module zero-entities) remain unrestored, and the per-module noise decision this node was opened to resolve is still open. [autonomous-loop, workflow-assisted, T2.14] | **Restores an operator-facing diagnostic signal for the exact scenario it matters most: a scan that ran modules but found absolutely nothing.** That's the single strongest "something is wrong" signal a dossier can give — a misconfigured target, an unreachable seed, or a genuinely unsupported target kind — yet the dossier previously printed the same reassuring "well-tuned" message whether a scan succeeded quietly or failed to find anything at all. The design specifically avoids the noise trap two prior cycles this session already diagnosed: it fires at most once per scan on a near-certain-failure condition, not once per routinely-zero-yield module. | 4294 ✅ |
| 2026-07-01 | **`censys` and `trove_au` — both named by C9's own problem statement as the motivating waste examples — now actually opt into the inter-scan entity cache (`PROBLEM_TREE` C9, `SOLUTION_TREE` SOL-CACHE-INTERSCAN).** Selected from a third discovery pass's C8/C9 residual audit, which deliberately checked `[x]`-marked/"delivered" nodes for overstated completeness instead of trusting the status marker. The caching mechanism itself is genuinely complete and correctly wired into all three dispatch paths (sequential, Phase 1 paid-sync, Phase 2 concurrent) — no bug found there. The gap was in rollout: grepping all 33 `Paid`/`KeyGated` modules found only `hlr_cnam`, `netlas`, and `opencellid` had ever overridden the default `cache_ttl_secs() = 0` (always-live, no caching). `censys` and `trove_au` — explicitly named in this very node's own problem statement as the motivating "consuming finite query allowances or real money on repeated scans" examples — had never actually opted in, silently defaulting to always-live despite the feature existing specifically to stop this. Both now override `cache_ttl_secs()` to 86400 seconds (24h), matching the bracket the other three already use. No `dispatch.rs` changes were needed since the cache gate at all three call sites is already generic over `module.cache_ttl_secs()` — this was purely a per-module opt-in gap. 2 new test assertions extend each module's existing metadata test. ~28 other Paid/KeyGated modules remain uncached; left as a deliberate per-module data-volatility audit (some, like real-time CNAM, genuinely must stay live) rather than a blanket flip, and tracked as a separate open item rather than reopening this node. The same audit pass also confirmed C8 (`streaming_probe`) is genuinely fully delivered, modulo a trivial 42-vs-43-site doc-count drift (43 sites actually live; docs still say 42 in several places) — noted but not fixed this cycle to avoid scope creep beyond the one candidate implemented. [autonomous-loop, workflow-assisted, C9] | **Closes a real, ongoing cost-waste gap this node's own problem statement flagged by name but a prior "delivered" marker let go unaddressed.** `censys` and `trove_au` are both paid/key-gated APIs with finite query allowances; every re-scan of the same IP or organisation within 24 hours was silently spending a live API call on data that had almost certainly not changed — precisely the waste C9 was built to eliminate, on the two sources its own design document uses as the motivating example. This also demonstrates the value of actively auditing `[x]`-marked nodes rather than treating "delivered" as permanently true: the mechanism being complete didn't mean its rollout was, and only 3 of 33 eligible modules had actually been switched on. | 4291 ✅ |
| 2026-07-01 | **`Engine::checkpoint_entities` — the mid-scan durability path hit at every productive round boundary — now canonicalises evidence order too, closing the higher-impact half of a determinism gap this session's own earlier C7 fix only partly addressed (`PROBLEM_TREE` C7, `SOLUTION_TREE` SOL-FORENSIC).** The earlier fix canonicalised `Store::entities_from_events`, the event-log recovery path used only when a scan's `entities` table is completely empty. This cycle's discovery pass found the narrower scope of that fix: `entities_for_scan` falls back to the canonicalizing recovery path *only* when the table is empty — so any scan that reached even one mid-scan checkpoint before being interrupted (the common case, since `checkpoint_entities` runs at every productive round boundary, not just once at the very end) never exercises that fix at all, and reads back through the ordinary, non-canonicalizing table path instead. Reproduced empirically before the fix: two independent runs checkpointing the same two evidence sources merged in opposite in-memory order produced different stored evidence order (`EQUAL=false`). `checkpoint_entities`'s signature changed from `entities: &[Entity]` to `entities: &mut [Entity]`, canonicalising in place before the `upsert_entities_batch` call — mirroring the finalise path's existing pattern exactly. Both call sites (the seed-round checkpoint and the per-expansion-round checkpoint) updated to pass mutable snapshots. New regression test proving arrival-order independence for the checkpoint path specifically, verified as a genuine (not tautological) regression test by temporarily reverting the code fix, confirming the test fails with the exact same `EQUAL=false` signature, then restoring the fix and confirming it passes. [autonomous-loop, workflow-assisted, C7] | **Closes the far more commonly-hit instance of a real forensic-determinism bug this session already found once in its narrower form.** Checkpointing happens routinely — at every round that discovers new entities, not just in the rare "scan never wrote a single checkpoint" case the first fix covered — so this closes the gap for the actual common path an interrupted Termux/Android scan takes, not just an edge case. The self-verification discipline (deliberately reverting the fix to prove the new test is a real regression guard, not a test that would pass either way) matters as much as the fix itself: it's the same rigor this session's discovery-verification passes apply to candidates, now applied to the implementation's own test. | 4291 ✅ |
| 2026-07-01 | **`virustotal` now surfaces its passive-DNS history as pivot entities, closing the last verified candidate from this session's second discovery pass (`PROBLEM_TREE` C4, `SOLUTION_TREE` SOL-NETINT).** The passive-DNS-history leg of C4's Cloudflare-origin-unmasking goal turned out smaller than the doc assumed: `virustotal`'s already-registered, already-called domain/IP report endpoint (`GET /api/v3/domains/{d}` or `/ip_addresses/{ip}`) returns a `last_dns_records` field — VT's lightweight passive-DNS snapshot of historical A/AAAA/MX/NS/CNAME records — that the module fetched but silently dropped, decoding the response straight into a narrow `VtAttributes` struct with no field to catch it. This is the same "dropped-field depth gap" class this session's earlier `austlii` (court/legislation references capped at 10 of 20) and `wigle` (per-AP coordinates discarded for the query centroid) fixes closed — not a missing integration, a depth gap on data already in hand. Verified this is a real, unaddressed gap and not stale-doc noise: a prior agent session had implemented an almost-identical fix (commit `c809c1ad`, "amplify(virustotal): surface as_owner/asn/network/country/categories/tags + passive-DNS pivots"), but `git merge-base --is-ancestor c809c1ad HEAD` confirms it lives only on an abandoned, never-merged branch. `build_entity` was widened to `build_entities` (pure, returns `Vec<Entity>`): A/AAAA records become `IpAddress` pivots tagged `resolved`, MX/NS/CNAME hostnames become `Domain` pivots tagged `passive-dns`, capped at a new `MAX_DNS_RECORDS = 30` to bound graph expansion on a domain with a long DNS history. Deliberately scoped narrower than the abandoned commit, which also surfaced `as_owner`/`asn`/`network`/`country`/`categories`/`tags` — a separate C4 asset-depth concern distinct from passive-DNS history — left out to avoid scope creep beyond the one candidate this cycle actually verified. 2 new unit tests: positive pivot extraction across every record type (including a non-parseable-`A`-value rejection and confirming `TXT` records are correctly NOT treated as a pivot kind), plus a test proving the 30-record cap holds. [autonomous-loop, workflow-assisted, C4] | **Surfaces genuine passive-DNS pivot intelligence VirusTotal already returns on a call the engine already makes — for free, no new API surface, no new key.** Historical DNS records are a real OSINT/threat-intel signal distinct from a live lookup: an MX or CNAME target from VT's history can reveal infrastructure the subject no longer actively advertises but that still correlates against other findings (shared mail infra, historical CDN-origin hints, prior hosting). Discarding this field meant every VT-covered domain/IP scan was silently leaving intelligence VT had already handed over unused — exactly the "the API returned it, the code just never looked" failure mode this session has now found and fixed in three separate modules (`austlii`, `wigle`, `virustotal`). | 4290 ✅ |
| 2026-07-01 | **`search_engines` gains the same proptest never-panics coverage the other T2.7 modules already carry — 4 of 5 named modules now covered (`PROBLEM_TREE` T2.7, `SOLUTION_TREE` SOL-F3).** Picked directly from the second discovery pass's verified candidate. `search_engines` is structurally different from `au_people`/`au_electoral`/`au_property`: one generic `parse_results` (plus its constituent pure iterators `HrefIter`/`CiteIter`/`GoogleUrlIter` and `external_link_count`) handles all 17 SERPs, rather than a bespoke parser per source. It already carried a hand-written adversarial-input regression test, `result_parsers_never_panic_on_adversarial_html` — roughly 20 hand-picked hostile-byte cases (truncated markers, multibyte codepoints straddling structural boundaries, control characters) — proving the maintainers already recognised this as the untrusted-input attack surface, but not the randomized `proptest` never-panics guarantee the other four T2.7 modules now have, which exercises the full `.{0,256}` arbitrary-input space rather than a fixed case list. Added a `mod prop` block with 5 new `proptest!` cases (one per parsing function), mirroring the established pattern from `au_people`/`au_electoral`/`au_property` exactly. All 5 passed on first run — a proof gap, not a live panic bug. `username_search` is now the sole named T2.7 module without this coverage; its detection logic is table-driven pattern matching rather than a bespoke HTML parser, so whether this exact pattern even applies is genuinely unconfirmed — left open rather than force-fit. [autonomous-loop, workflow-assisted, T2.7] | **Closes the fourth of five T2.7-named modules for a real, previously-identified panic-safety class this codebase has already fixed once (T0.1/T0.2's `to_lowercase`-on-a-copy byte-offset panics).** `search_engines` is one of the highest-fan-out untrusted-input surfaces in the engine — 17 different search engines' arbitrary, real-world HTML flows through the same generic parser — so randomized fuzzing of it closes real risk, distinct from (and complementary to) the module's existing fixed-case regression test: a hand-picked case list only proves the cases someone thought to write down, while `proptest`'s randomized generation explores the input space the author didn't anticipate. | 4288 ✅ |
| 2026-07-01 | **Two independent stale-doc-claim corrections found by the same discovery pass's audit candidate: `SOLUTION_TREE` SOL-AU-MOAT's "fuller ASIC/ABR graph; state cadastre/property" was already delivered and never folded back in, and `PROBLEM_TREE` §7 S2's status marker (`[ ]`) contradicted its own "✅ Fixed" body (`PROBLEM_TREE` C3, §7 S2; `SOLUTION_TREE` SOL-AU-MOAT).** (1) `asic_persons` (banned/disqualified persons, financial advisers, credit/finance-broker representatives — three registers in one module), `asic_business_names` (business/trading name → ABN), and `asic_banned_orgs` (banned/disqualified organisations → ACN) — three more live, keyless ASIC registers alongside the older `asic_director` — plus `qld_cadastre` (free QLD Government DCDB coordinate-keyed cadastre lookup) are all registered in the live dispatch table today, confirmed via `grep -c "Arc::new(" src/modules/mod.rs` returning 162 against the doc's last recorded snapshot of 126 modules — clear evidence of undocumented growth. `Remaining` narrowed from "GNAF/AusPost address validation; fuller ASIC/ABR graph; state cadastre/property" down to just GNAF/AusPost and non-QLD state cadastre (QLD's own cadastre is now covered). Deliberately did not claim a delivery date for the four modules: this session's shallow git clone resolves their `git log`/`git blame` history to the single root import commit, not real authorship history — the same attribution trap this session's AU-084/AU-060 corrections already identified and worked around. (2) §7 S2 (whois-referral SSRF) read `[ ]` P1 (HIGH) in its header while its own body said "✅ Fixed (the SOL-SSRF-WHOIS solution)" with full implementation detail and a named regression test, and the paired `SOLUTION_TREE` SOL-SSRF-WHOIS entry already showed `[x]` — S2 was the sole marker/body mismatch among its S3/S4/S5 siblings, all of which correctly match. Flipped the header to `[x]`. Both corrections are doc-only; no source code, tests, or architecture guards touched. [autonomous-loop, workflow-assisted, stale-doc-audit] | **A fourth and fifth confirmed instance of the same drift pattern this session has repeatedly found** (the C1 AU-0xx audit, C5 provenance radius, AU-060/084 rule-number reassignment, SOL-UPDATE's diff-summary claim, C6's "no comparably small gap" self-correction) — the gap-analysis sections drift from reality faster than they get re-checked, and a status marker that contradicts its own prose body is exactly the kind of internal inconsistency that would mislead a future cycle (or a human reviewer) scanning the tree's headers rather than reading every bullet's full text. Getting C3's actual AU coverage right also matters operationally: understating a delivered capability risks a future cycle re-implementing something that already ships. | n/a (docs) |
| 2026-07-01 | **`au_geo` now tags its exact ABS point-in-polygon state onto the coordinate entity, so the correlator's jurisdiction rules (AU-056/AU-085) use the precise answer instead of silently falling back to a coarse bounding-box approximation (`PROBLEM_TREE` C5, `SOLUTION_TREE` SOL-GEOINT).** Selected from a second parallel discovery + adversarial-verification pass (Workflow tool, 8 fresh backlog areas). `au_geo::assemble()` resolves the exact ABS `state_name_2021` for every coordinate it processes (e.g. "New South Wales") and stores it in evidence text, but never tagged the coordinate entity itself with it — unlike every other geo module (`wigle`, etc.), which does. `core::correlator::rules::geo::coord_state()`, the helper both AU-056 and AU-085 depend on to read a Coordinates entity's jurisdiction, prefers an `au-state:XX` tag and only falls back to `util::geo::au_state_for_coords` — a rectangular bounding-box approximation whose own doc comment admits "points near a shared border can be misattributed" — when that tag is absent. So the correlator was silently discarding `au_geo`'s more precise, polygon-exact answer and re-deriving a strictly coarser one from raw lat/lon, every single time. Fixed by resolving the full state name to its canonical abbreviation via `util::address_au::state_code` (the same helper `au_people` already uses for exactly this purpose) and tagging the coordinate `au-state:XX` + `country:AU` before it's merged into the graph. 2 new unit tests: a positive assertion on the module's existing full-resolution fixture, and a negative case (no state present in any resolved layer) proving the fix never invents a tag. Confirmed via grep that `au_geo` had never been mentioned in either PROBLEM_TREE.md or SOLUTION_TREE.md before this cycle — a genuinely fresh finding, not overlapping the AU-052/057/059 centroid/median fusion work this session's earlier cycles already covered. [autonomous-loop, workflow-assisted, C5] | **Closes a real precision gap in the two correlation rules responsible for cross-checking a subject's jurisdiction against corroborating evidence — exactly the kind of AU-first differentiation this project's mission statement targets over SpiderFoot/Maltego.** A coordinate near a state border is the case where the difference matters most: the bbox fallback's own documentation says such points "can be misattributed," while `au_geo`'s ABS point-in-polygon answer is authoritative (it queries the actual administrative boundary service, not a rectangle). Every AU-located scan that resolves through `au_geo` now gets its jurisdiction cross-checks run against the correct state instead of a coin-flip near a border. | 4283 ✅ |
| 2026-07-01 | **New correlator rule AU-111 — CDN origin-candidate unmasking via a non-proxied MX/direct-connect sibling, closing one leg of C4's Cloudflare-origin-unmasking goal (`PROBLEM_TREE` C4, `SOLUTION_TREE` SOL-NETINT).** The last verified candidate from this session's parallel discovery + adversarial-verification pass. Came back `PARTIALLY_CONFIRMED`: the underlying gap and its supporting plumbing (MX-tagged Domain entities from `dns_intel`, the subdomain-brute dictionary's `cpanel`/`ftp`/`mail`/`dev` direct-connect labels, `derive_resolution`'s auto-generated `ResolvesTo` edges, `is_cdn_edge_ip`) were all confirmed real and ready — but the candidate's proposed test location was wrong, and, caught only during implementation (not by the discovery/verify pass itself), its "tag the IpAddress entity with origin-candidate" step turned out not to be achievable at all: correlator relation rules are `fn(&[Entity], &[Relation], &str, u64) -> Vec<Correlation>` — a read-only pass over an immutable entity slice, with no mechanism to mutate or tag an entity. Implemented correctly instead, following the established AU-110 (shared-hosting-IP) rule's exact shape: the origin-candidate IP is named in the fired `Correlation`'s `entity_uids` and description, the same place AU-110 already names its co-hosted IPs. New rule groups `ResolvesTo` edges by registrable domain; fires Medium when an apex domain resolves entirely to CDN/anycast edges and a sibling under the same registered domain — an MX host, or a direct-connect subdomain-brute hit — resolves to a real, routable IP. 5 new unit tests (2 positive-fire cases: MX sibling, dns-brute sibling; 3 no-fire guards: non-CDN apex, equally-CDN-fronted sibling, generic/unrelated-domain scope creep) mirroring AU-110's fixture style exactly. [autonomous-loop, workflow-assisted, C4] | **Turns Cloudflare/CDN fronting from a data blind spot into an actionable pivot — a capability neither SpiderFoot nor Maltego offers as a correlated finding.** A CDN-fronted site's real origin IP is high-value OSINT: it enables direct infrastructure attribution, geolocation of the actual hosting provider, and bypassing the CDN's DDoS/WAF protection for further authorized testing — but until now the engine collected every piece of the puzzle (MX records, brute-forced subdomains, DNS resolution) without ever connecting them into a single correlated lead. The mid-implementation catch of the "tag the entity" design error also matters: it shows this session's verification discipline extends past the discovery pass's own adversarial check, catching an architectural impossibility the verifier missed by re-deriving the implementation against the real type signatures rather than trusting the plan. | 4282 ✅ |
| 2026-07-01 | **Closed a `run_expansion`/`run_gap_fill` per-candidate O(entity_map_size) `HashSet` rebuild — one hot-loop "cap+chunk, never slurp" violation (`PROBLEM_TREE` C2, `SOLUTION_TREE` SOL-PERF-PUBLISH).** Picked up from the same parallel discovery + adversarial-verification pass that found the C7 fix above, but this candidate came back `PARTIALLY_CONFIRMED` with two accuracy nits in its self-description (a wrong test-coverage citation; an understated blast radius — 5 `DispatchState` construction sites, not 2), so this cycle independently re-verified the core finding directly against the live code before implementing, rather than trusting the plan as written. Both nits confirmed and corrected. `run_expansion`/`run_gap_fill` cloned the ENTIRE `entity_map` key set into a fresh `HashSet` before every single expansion candidate's dispatch (not once per round — once per candidate) purely to diff "what's new" afterward for `DerivedFrom` lineage, then rescanned the whole map again to find it — unbounded by default, since `ScanOptions::max_roi` defaults `false` and the only per-round candidate cap (`apply_roi_cutoff`) is gated behind it, so cost scaled worst exactly as a scan approached the 2500-entity default cap. Added a `newly_inserted: Vec<String>` field to `DispatchState`, populated only at the single true-insert chokepoint inside `finalise_module_result` (O(1) per genuinely new entity, never on merge), replacing both the per-candidate snapshot clone and the after-the-fact full-map rescan with an O(new-entities-this-candidate) drain. Updated all 5 `DispatchState` construction sites (3 real call sites, 2 test fixtures). Verified against the real `expansion_records_derived_from_lineage` end-to-end integration test — which exercises this exact lineage-attribution code path against a live two-hop expansion, not a hand-built `Relation` fixture — plus the full `cargo test` suite across every binary (lib, smoke, architecture, doctests). [autonomous-loop, workflow-assisted, C2] | **Closes a real, worst-case-quadratic-in-scan-size cost in the engine's hottest loop, and demonstrates why this cycle's own verification discipline matters.** A discovery candidate's evidence can itself contain small errors even when the core finding is real — this cycle caught and fixed two before implementing, rather than propagating them into the documentation trail. The underlying fix matters independently: C2's stated target is a *published, reproducible* on-device performance number, and a per-candidate full-graph clone in the expansion hot path is exactly the kind of unbounded per-iteration cost that would make such a number worse than it needs to be, on the resource-constrained Termux/Android target this project is built for. | 4277 ✅ |
| 2026-07-01 | **The event-log scan-recovery path now canonicalises evidence order, closing a real forensic-determinism gap (`PROBLEM_TREE` C7, `SOLUTION_TREE` SOL-FORENSIC).** `Store::entities_from_events` — the recovery path used whenever a scan never finalised, "routine on Termux/Android" per its own doc comment — folded entities via `Entity::merge` in raw event-arrival order but never called `Entity::canonicalize_order()` afterward, unlike the finalised-scan path (`core::engine::run`), which calls it explicitly so concurrent dispatch's completion order can't leak into the stored/exported result. Every export renderer (JSON, CSV, full dossier, debug bundle) reads an entity's evidence in raw vec order, so a recovered *interrupted* scan's export was not byte-stable across two otherwise-identical runs whose modules merely completed in a different order. Selected via a parallel discovery + adversarial-verification pass (Workflow tool, 8 backlog areas investigated concurrently: T2.14, C2, C7, F.1/bstr, C3, C4, a fresh stale-doc sweep, `username_search` T2.7 scope) — the verifying agent independently reproduced the bug (built its own scratch test proving `EQUAL=false` across two arrival orders), applied the exact fix, confirmed `EQUAL=true`, then reverted, before this cycle applied it for real. Fixed with the same one-line `canonicalize_order()` call already used at the finalisation site; added a permanent regression test building the same entity from two reversed event-arrival orders and asserting identical recovered evidence order. Also corrected a stale note from the prior cycle claiming "C7 has no comparably small gap." [autonomous-loop, workflow-assisted, C7] | **Closes a real gap between this codebase's own byte-stable-export doctrine and a specific, common-on-this-platform code path.** An interrupted scan recovering from the event log is not an edge case here — the code's own comment calls it routine on Termux/Android, where the OS reclaims backgrounded processes — so a forensic export whose evidence ordering silently depended on module completion timing would have made two runs of the *same* interrupted scan produce different-looking dossiers for no substantive reason, undermining exactly the "machine-diffable, reproducible intelligence" claim C7 exists to deliver. | 4277 ✅ |
| 2026-07-01 | **`au_electoral`/`au_property` gain the never-panics proptest coverage `au_people` got two cycles ago — closes T2.7's explicitly-named remaining gap (`PROBLEM_TREE` T2.7, `SOLUTION_TREE` SOL-F3).** `au_electoral::parse::extract_division` and `au_property::parse::{parse_nsw_response,parse_vic_response,parse_qld_response}` parse untrusted scraped HTML/text with zero adversarial-input regression tests — the identical gap `au_people`'s three parsers had before the prior cycle's fix, and the two modules that fix's own note explicitly left open. Added 4 new `proptest!` cases (`.{0,256}` arbitrary strings), mirroring `au_people`'s established `mod prop` shape exactly. All 4 passed on first run — a proof gap, not a live panic bug. T2.7 stays `[~]`: `search_engines`/`username_search` still lack this coverage, and the golden-fixture/health-signal legs (T2.14, SOL-HEALTH-SIGNAL) remain fully open. Picked directly from T2.7's own note (no fresh discovery pass needed — the gap was already named). [autonomous-loop, T2.7] | **Closes the two remaining instances of a gap this codebase already fixed once for a sibling module, and that its own safety doctrine (T0.1/T0.2) exists to prevent.** A HTML-scraping parser with no adversarial-input test is exactly the failure class the project's `to_lowercase`-on-a-copy byte-offset panic history warns about; leaving 2 of 5 named T2.7 modules without this proof after fixing the pattern once would be the kind of "delivered the easy 60%, silently stopped" gap this loop's own audits keep catching in other people's notes. | 4276 ✅ |
| 2026-07-01 | **Exposure-dork coverage extended to Phone/FullName targets; most of C6's other named pieces confirmed already delivered (`PROBLEM_TREE` C6, SOL-OFFENSIVE).** `search_engines::queries::exposure::build_queries_exposure` silently returned an empty `Vec` for `Phone`/`FullName` (and 10 other `TargetKind`s), while its own doc comment falsely claimed only `Coordinates`/`ASN`/`ABN-ACN` were excluded. Verified first (not assumed) that C6's other named sub-pieces — key_harvest's entropy gate + `aho-corasick` scanner, the credential-reuse graph (AU-047/AU-105/AU-048), `oathnet_pro`/`see_know`'s shared key pipeline — are all already mature. Added `phone_exposure`/`fullname_exposure` (breach-dump/pastebin/code-repo/people-search dorks, mirroring the file's existing five per-kind helpers exactly), corrected the doc comment, added dispatch + shape tests. Caught and fixed a genuine pre-existing test bug the change exposed: a "FullName dispatch == FullName base dorks" equality test had only passed because exposure happened to be empty for FullName before — now compares the verbatim-extraction claim against the correct baseline and separately asserts the exposure dorks are present in the full pipeline. Selected via a parallel discovery + adversarial-verification pass (Workflow tool). [autonomous-loop, workflow-assisted, C6] | **Closes a real search-coverage gap for two of the most common OSINT seed kinds.** Phone numbers and full names are core `hse scan` seed types, yet they alone among the module's five other identity/asset kinds got no exposure-dork pass at all — no credential-dump check, no code-repo leak check, no people-search cross-reference. The doc-comment correction matters independently: a comment that lists 3 exclusions when the real count is 12 actively misleads a future reader about the module's actual coverage, the same "code that claims a capability it doesn't have" failure mode this session's other fixes have caught. | 4272 ✅ |
| 2026-07-01 | **C1's "AU-0xx rule-gap fill" note closed as fully stale; C1's "first-class timeline output" delivered — the dossier and SPA now surface the tenure/recency headline the JSON API already computed (`PROBLEM_TREE` C1).** Cross-checked every AU-0xx number in the docs against `core::correlator::mod.rs`'s live dispatch table — all present except AU-065/066 (deliberately engine-emitted, not a gap); AU-047 ("controller behind reused secrets") already exists as a Correlation rule but was never wired into the `Relation` graph, so it's invisible to CONNECTIONS — real, scoped, left open (needs a new `RelationKind` variant, a `CONVENTIONS.md`-pinned vocabulary, past this cycle's "smallest unit"). Separately: `core::timeline::online_tenure`/`footprint_recency` were computed and returned by `GET /scans/{id}/timeline` but never read by the CLI dossier's TIMELINE section or the SPA's `renderTimeline()` — an "online since 2008, 17y span, 9 breaches, footprint active" headline sitting in unread JSON fields. Both now render it via one shared computation. New pure `tenure_headline` helper, unit-tested for exact wording including breach-count pluralisation; SPA JS syntax verified with `node --check`; live CLI run confirmed the empty-timeline path renders unchanged. Selected via the same parallel discovery + adversarial-verification pass (Workflow tool). [autonomous-loop, workflow-assisted, C1] | **Two different classes of the same underlying failure — computed-but-unsurfaced data, and a stale gap claim — closed together because they were found together.** A tenure/recency headline that only exists in a JSON response nobody reads is functionally equivalent to not having computed it at all; the operator-facing surfaces (CLI, SPA) are what an LE/defence reviewer actually looks at. Getting AU-0xx's status right also matters operationally: a false "still needs a rule gap filled" claim would have sent a future cycle hunting for correlation coverage that already exists. | 4268 ✅ |
| 2026-07-01 | **`au_people`'s three HTML parsers gain proptest never-panics coverage; corrects an overclaim in SOL-F3's "every network parser" delivered note (`PROBLEM_TREE` T2.7, partial).** `parse_whitepages_html`/`parse_tps_html`/`parse_relatives` had zero adversarial-input regression tests, despite the shared primitives they delegate to (`util::html::strip_html`, `util::str_util::find_ascii_ci`) already carrying exactly this `proptest` `mod prop` pattern. Confirmed again (not assumed) that a literal T2.7 golden fixture — a saved real HTTP response — remains genuinely infeasible without either a live fetch or a fabricated-looking snippet; this doesn't close that ask, only a narrower, safe, adjacent one. Added 3 `proptest!` cases (`.{0,256}` arbitrary strings) to `au_people/tests.rs`'s new `mod prop`; all three passed on the first run — a proof gap, not a live panic bug. `au_electoral`/`au_property` have the identical gap, left open. Selected via a parallel discovery + adversarial-verification pass (Workflow tool, 8 candidates investigated concurrently); the reviewing agent independently applied this exact code and ran it before this cycle re-applied and re-verified directly. [autonomous-loop, workflow-assisted, T2.7] | **Closes a real gap between this codebase's own stated safety doctrine and its actual coverage.** The T0.1/T0.2 panic class this project already fixed once (`to_lowercase`-on-a-copy byte-offset panics) is exactly the kind of defect a HTML-scraping parser with no adversarial-input test is most exposed to; SOL-F3's log had already (incorrectly) marked this class of protection "delivered" for every network parser, which is precisely the kind of self-reported-but-unverified claim this session's stale-note audits keep catching — this time inside a test-coverage claim, not a feature-delivery one. | 4267 ✅ |
| 2026-07-01 | **Real git-subprocess test coverage for `cli/update.rs`'s `commits_behind`/`changelog_lines` — closes the residual the previous cycle's doc correction left open.** Selected via a parallel discovery + adversarial-verification pass (8 backlog candidates investigated concurrently with the Workflow tool). New test builds a genuine local git-repo pair (a "remote" plus a tracked "clone", both under `tempfile::tempdir()`, no network — the remote is a local filesystem path) and exercises `commits_behind`/`changelog_lines` against real `git` subprocess output across three states: freshly cloned (up to date, `Some(0)`, no lines), the remote advanced by two commits (`Some(2)`, both subjects present in `git log --oneline`'s own newest-first order), and a non-git directory (the documented `None`/empty fallback). A `git_fixture` helper pins `commit.gpgsign=false` plus explicit `GIT_AUTHOR_*`/`GIT_COMMITTER_*` env vars on every fixture-building call, so the test doesn't depend on this sandbox's own ambient signing config (which would not match a clean CI runner's). Purely additive to the existing `mod tests` block — no non-test code changed. [autonomous-loop, workflow-assisted, SOL-UPDATE] | **Closes a real, previously-flagged gap in test discipline for shell-out code.** `commits_behind`/`changelog_lines` are the only functions in this module that talk to an external process, and until now nothing proved they parse real `git` output correctly rather than merely compiling — the class of bug a unit test on synthetic strings can't catch (wrong flag, wrong field order, an assumption about `git`'s output format that happens to be untested). Verified end-to-end against actual `git`, not mocked. | 4264 ✅ |
| 2026-07-01 | **Fourth stale note this session: `hse update --check` already prints commit subject lines, not just a count.** `SOLUTION_TREE`'s SOL-UPDATE node and its twin §4a entry both still claimed `--check` shows a bare commit count; `cli/update.rs::changelog_lines` already runs `git log --oneline HEAD..@{u}` and `cmd_update` already prints up to 20 lines under the count. Unlike the AU-084 correction, this repository's history begins at one root commit (`770df4c9`, 857 files / 244,800 lines, no parent — an import boundary, not incremental authorship), so no specific delivery cycle could honestly be attributed via `git log`; worded the correction to say so rather than imply a dated delivery the evidence doesn't support. Genuine residual noted rather than dropped: `changelog_lines`/`commits_behind` have no test against real `git` subprocess behaviour (`tempfile`, already a dev-dep, would support a local-repo-pair fixture) — left as its own smaller follow-on. No code change. [autonomous-loop, S→P audit, docs-only] | **A fourth confirmed instance of the same pattern in one session** (cycle-20 `securitytrails`/`bgpview`/`ripestat`, the C5 "provenance radius" audit, and the AU-084 cell-tower audit) — the gap-analysis sections drift from reality faster than they get re-checked. Getting the attribution honest matters as much as getting the "is it done" verdict right: a false "delivered on date X" claim would itself become a new inaccuracy for a future cycle to un-learn. | n/a (docs) |
| 2026-07-01 | **`SOLUTION_TREE` §4a doc correction — the "AU-060-candidate" cell-tower cross-validation gap was already delivered a day earlier, as AU-084, under a reassigned number.** Logged as an open S→P gap at cycle 20 (`opencellid` × `cell_intel` `DeviceId` cross-validation, no rule cross-validated them); shipped 2026-06-30 (`770df4c9`) as `rule_au_084_cell_tower_dual_source` — registered in the correlator dispatch table with 4 dedicated tests. The originally-proposed number, AU-060, was independently reassigned to an unrelated rule (transitive identity closure) in the interim, so the note was doubly stale. No code change; verified against the shipped rule, its dispatch registration, its tests, and `git log -S` for the delivery commit — not by inference. [autonomous-loop, S→P audit, docs-only] | **The third stale gap-analysis note found this session** (after the cycle-20 `securitytrails`/`bgpview`/`ripestat` audit and the C5 "provenance radius" audit two cycles ago) — confirms the trees' own gap-analysis sections need periodic re-verification against the live code, not just cross-checked against each other, or a future cycle (including this loop's own next invocation) risks re-implementing something that already shipped, or worse, trusting a rule-ID reference that now points at something unrelated. | n/a (docs) |
| 2026-07-01 | **Removed two more dead `optimization_hints` conditions inside `analyse()` itself, found by re-reading the whole block T2.13's bug lived in (`PROBLEM_TREE` T2.13 addendum + new T2.14).** Same unreachable `entities_emitted == 0` premise as the just-fixed ROI hint: a per-module "returned 0 entities" hint and a scan-level "60s + zero-yield module" hint, both provably unfireable for the identical reason. Removed as confirmed-dead rather than left implying a capability that cannot exist. Deliberately NOT mechanically restored — `analyse()`'s pure signature can't reach the event data a correct fix needs without either a 16-call/test-site change or per-caller duplication, and the per-module hint has a genuine unresolved noise problem: a real 42-module scan (the same one used to prove the ROI-hint fix) shows most dispatched modules land at zero yield for any given target kind, so firing one line per module would flood the hints list rather than inform it. Opened T2.14 / SOL-HINT-NOISE with the concrete reinstatement options instead of forcing one under time pressure. Renamed the one test whose name overclaimed its coverage. [autonomous-loop, T2.13-addendum, T2.14, discovery-pass] | **Code that claims a capability it structurally cannot have is worse than code that's silently absent — it actively misleads the next reader (including a future autonomous cycle) into trusting a signal that was never real.** Removing confirmed-dead branches is the same "no dead fields" doctrine this codebase already enforces for constants and modules, applied to hint logic. Deferring the per-module reinstatement rather than blindly restoring it prevents trading one bug (a silent hint) for a worse one (a flooded, ignorable hints list on every multi-module scan) — precision over noise is the same principle the ROI hint's own `KeyGated`/`Paid` filter already embodies. | 4263 ✅ |
| 2026-07-01 | **Fixed the dossier's "ROI" wasted-spend hint — structurally dead since it was written, now verified live (`PROBLEM_TREE` T2.13, new).** `cli/scan/dossier.rs` filtered `ScanDiagnostics::modules_by_yield` for `entities_emitted == 0` to warn about `KeyGated`/`Paid` modules that spent a call for nothing — but that list is built *only* from emitted entities' evidence, so a module that ran and found nothing is absent, never present-at-zero. The filter could never match, on any scan, ever. New pure `zero_yield_keyed_or_paid_modules` reads the scan's own durable `ModuleDone { module, found }` events instead. Verified empirically, not just by inspection: a real `hse scan --output dossier` against a live domain, run before the fix, dispatched 42 modules (11 `KeyGated`/`Paid`, several timed out) and the ROI line never printed; the identical scan re-run after the fix correctly named all 11. 4 new unit tests. [autonomous-loop, T2.13, discovery-pass] | **Restores a wasted-spend warning an operator on a metered plan or Termux mobile radio depends on.** The hint exists specifically to tell the operator "you spent a budgeted paid/keyed API call and got nothing — exclude it next scan" — exactly the kind of ROI signal that matters when API budgets and mobile data are both scarce. It had silently never worked, on every scan since it was written, with nothing in the test suite positioned to catch it (no test exercised the *live* dossier text path). Found via this cycle's code-grounded discovery pass after the smaller, safer open items were exhausted or blocked. | 4263 ✅ |
| 2026-07-01 | **AU-059's location fix upgraded from a plain confidence-weighted centroid to the outlier-robust Weiszfeld geometric median (`PROBLEM_TREE` C5).** `au059_synergy_fix` — the function behind the dossier's headline "Best location estimate" line — now calls `util::geometry::weighted_geometric_median`, falling back to `weighted_centroid` only on the rare non-convergent input, matching the fallback idiom already used by AU-057 and `diagnostics::cluster_coordinates`. New regression test `au059_synergy_fix_resists_a_single_high_confidence_outlier` proves the difference: 2 agreeing near-Sydney classes (64% of confidence-weighted mass) vs. 1 higher-confidence Perth outlier (36%, below the median's 50% breakdown point) — the geometric median stays anchored near Sydney (lon>145) while the plain centroid the old code computed lands a third of the way to Perth (lon≈138.6, computed inline in the same test for direct comparison). Fails against the pre-fix code and passes against the fix. [autonomous-loop, C5] | **Closes the one real gap the previous cycle's docs audit surfaced.** A single disagreeing sighting — even a plausible, high-confidence one from a legitimate but wrong source — should not be allowed to drag the operator's headline "where is this person" answer proportionally to its raw confidence; a true majority of independent, spatially-agreeing sources should win. This is exactly the failure mode a real dossier can't afford: the previous linear-average estimator gave a single well-corroborated-looking outlier undue leverage over the answer an LE/defence reviewer reads first. AU-057 and the spatial-clustering diagnostics already used the more robust estimator; AU-059 — the one actually surfaced to the operator — did not, until now. | 4259 ✅ |
| 2026-07-01 | **`PROBLEM_TREE`/`SOLUTION_TREE` C5 audit correction — "provenance radius output" was already shipped, twice, and the node text never caught up.** Cycle 29 (2026-06-20) added the confidence radius to AU-059's synergy fix and its own `SOLUTION_TREE` log said so outright, but the C5/SOL-GEOINT node text was never edited to drop the item from its own "remaining" list; `d1507539` (2026-06-26) then shipped `best_au_location_estimate` (a single-signal fallback so every AU-located scan gets a headline fix, not just the multi-source case) with a `CHANGELOG.md` entry that was never cross-referenced into either tree. Corrected both trees in place with full commit provenance; the genuinely-open legs (AU-059 using `weighted_centroid` instead of the more robust `weighted_geometric_median` already proven in AU-057/`cluster_coordinates`, AU bounding precision, movement/timeline geo, cell-DB auto-sync) are kept unchanged. No code change — verified by re-reading `rules::location::mod.rs`, `cli/scan/dossier.rs`, and `api::scan_export::extract_au_location_fix` directly, not by trusting the tree's own claim. [autonomous-loop, S→P audit, docs-only] | **Keeps the living documents honest as the actual source of "what's left."** This loop's own protocol treats the trees as the single source of truth for what to build next; a stale "remaining" note books phantom future work (or worse, a future cycle re-implementing something that already shipped) exactly as surely as a missed defect books a false sense of completeness. Same class of correction as the cycle-20 `securitytrails`/`bgpview`/`ripestat` stale-note audit — grounded in git history (`ac9114e4`, `d1507539`) and the live code, not speculation. | n/a (docs) |
| 2026-07-01 | **Concurrent dispatch no longer over-dispatches a whole target's module set past `max_entities` (`PROBLEM_TREE` T2.11 LOW).** `dispatch_target_concurrent`'s Phase-2 spawn loop judged the entity-budget cap against the count from before this target's spawn loop began, because finalising a joined module's entities only happened in the trailing blocking drain, run AFTER every accepting module had already been spawned. Fixed with a non-blocking `JoinSet::try_join_next` drain at the top of every spawn-loop iteration (sharing a new `absorb_dispatch_outcome` helper with the trailing drain, so a result is finalised exactly once) — the cap now reads a live count. [autonomous-loop, T2.11] | **Restores budget parity between the sequential and concurrent dispatch paths.** The sequential path already re-checked `entity_map.len()` before every module; the concurrent path only checked it once per target, so a `max_entities`-capped scan run with `max_concurrent > 0` could dispatch an entire extra target's worth of modules (real network calls, real budget) past the operator's stated cap — worse the more modules a target's kind accepts. Regression test `concurrent_dispatch_stops_near_max_entities_not_after_the_full_module_set` forces the interleave deterministically (`max_concurrent: 1`) and is proven to fail on the pre-fix code (all 10 of 10 modules dispatch) and pass on the fix. | 4258 ✅ |
| 2026-06-24 | **`comb_search` — free, keyless emulation of SeekNow's stealer/credential corpus (real public source, no API).** New free Breach module (pri 129) querying ProxyNova's public COMB index (`api.proxynova.com/comb`) — genuine leaked `identity:secret` lines, no key, no mock. Accepts Email/Username/Domain. The correctness core is [`line_matches_target`]: COMB matches the query as a **substring** (a query for `qwerty-zzz@nope.invalid` returns `qwerty-zzz@bk.ru:…`), so every returned line is strictly post-filtered to the EXACT identity (full email / exact host / exact local-part) before any entity is minted; username matches are candidate-quarantined; secret-echoes (`user@x:user@x`) and capture sentinels (`[fail]`) are dropped via `classify_credential_field`. Live integration test (ignored) verified end-to-end against the real endpoint: 79 entities for `example.com` (75 exact-host accounts + 3 passwords), zero stranger attribution. The paid `see_know` API path is untouched (kept as the optional, key-gated counterpart). [operator-directive, free-source emulation, no-API] | **Delivers SeekNow's most distinctive data — leaked credentials — without its paid API or any anti-bot circumvention.** The operator asked to emulate SeekNow with legitimate real execution, no mock. The provider's own free frontend (`see-know.icu`) sits behind a Cloudflare JS challenge — solving it would be brittle evasion and is correctly out of scope; so the emulation uses a real, keyless public breach index instead. The substring-exactness filter is what makes this safe for an LE/defence dossier: it never attributes a stranger's password to the subject (the exact failure a naïve COMB wrapper would commit). Free-tier, offline-deterministic parsing, single bounded request — Termux-safe. | 3789 ✅ |
| 2026-06-24 | **SeekNow dynamic quota probe + highest-tier session cap.** (1) `query_credits(key)` added to `util::see_know::endpoints` — queries `GET /api/v1/credits` without consuming a budget slot, parses four observed response shapes (`credits_remaining`, `remaining`, `data.credits_remaining`, `credits.remaining`). (2) `should_probe_quota()` / `scale_scan_cap_from_daily(limit)` added to `budget.rs`; probe fires once per scan (latch resets with the per-scan budget). (3) `SeekNow::process()` calls the probe before any budget spend on the first seed of each scan; if the daily limit is known, scan cap is set to `clamp(limit / 20, 300, 2500)`. (4) Session cap raised from 4,500 → 100,000 — the server's own daily quota is the real backstop, not a local ceiling. [operator-directive, quota-utilisation] | **Auto-scales to the operator's actual plan.** The operator is on the highest SeekNow tier. Hardcoding 5,000/day from the `premiumhq` plan was wrong — the real daily limit is whatever the account's plan provides, discovered from the API itself at runtime. A highest-tier plan returning, say, 50,000 daily lookups now yields a scan cap of 2,500/round (vs. the previous 300) — 8× more pivots per scan without any manual env-var tuning. The probe falls back gracefully if the `/credits` endpoint is unreachable (static 300 default survives). HUNTSMAN_SEEKNOW_SCAN_CAP and ScanOptions::seeknow_scan_cap always take precedence over the dynamic value. | 3781 ✅ |
| 2026-06-24 | **SeekNow maximisation: scan cap 160→300, session cap 500→4500, full endpoint matrix enabled.** (1) `BUDGET` defaults raised to 300/scan + 4500/session (90% of the 5,000-daily pool, ~15 full scans per session). (2) The `FREE_COVERED_SINGLE_ORIGIN` filter removed from `effective_plan` — github/twitter/reddit/tiktok/roblox/xbox/minecraft endpoints now fire on username seeds alongside `SocialAggregate` and `UsernameHistory`. (3) Per-endpoint yield tracing added (`tracing::debug!` on every non-empty endpoint hit, keyed by endpoint + target kind) so live logs now identify which endpoints return data for each seed kind. Tests updated to reflect the maximisation policy: `default_scan_cap_is_at_least_maximise_floor` (≥200, was `(120..=200)`), `effective_plan_fires_the_full_username_matrix` (was `effective_plan_drops_free_covered_single_origin_endpoints`). [operator-directive, quota-utilisation] | **Uses SeekNow at the scale its quota allows.** Session cap of 500 meant HSE used ≤10% of the 5,000/day pool regardless of how many scans were run; the single-origin endpoint filter meant a username scan spent only 3 paid calls (search + social + history) instead of the 10 its endpoint matrix supports. The operator's directive is explicit: SeekNow is "basically Oathnet but with many thousands of times more daily queries — utilised fully". Platform-specific endpoints (e.g. `/username/github`) return breach/stealer context tied to THAT platform account — not just presence confirmation — so they add data the free username_search stack and `SocialAggregate` don't cover. The budget cap (300 per scan) is the only rate limiter; the daily server-side quota-exhaustion response acts as the external backstop. Per-endpoint yield tracing in debug logs directly supports the operator's directive to "identify situations where SeekNow provides a distinct gain" over broad-spectrum searching. | 3781 ✅ |
| 2026-06-24 | **`search_engines` surname-family extraction de-fabricated.** (1) `extract_family_names` rejects self-doubled first-name garbage (`Fredfred`, `annaanna`) — the prior guard only caught doublings of the SUBJECT's terms. (2) Snippet-surname "family members" are now emitted candidate-tier (`demote_to_candidate`), not 0.45 PROBABLE. [live-bundle, fabricated-PII] | **Stops the module inventing relatives.** A live v1.9.0 "Matthew Diegmann" bundle minted `Fredfred Diegmann` (doubled from a radaris `/p/Fred/Diegmann/` snippet), `Diegstim Diegmann` (mangled from an Instagram handle), and global namesakes `Dominique Diegmann` (a ski-race page) / `Elaine Diegmann` (a US healthcare NPI) — all as PROBABLE `family-member`s outranking the genuine QLD-registry relatives (`family-candidate`, 0.35). A shared surname in a SERP snippet is a speculative lead, not confirmed kin: candidate-tier retains it as a lead while excluding it from the subject's confirmed footprint / correlator / exposure. | 3555 ✅ |
| 2026-06-24 | **Footprint timeline now excludes candidate-quarantined entities** (`timeline::reconstruct` skips `tags::CANDIDATE`). [live-scan, PRIVACY + precision] | **Fixes a confirmed PII leak.** A real v1.10.0 person-scan's "Footprint timeline" rendered 15 `Born …` events that ALL belonged to quarantined breach co-occurrence **strangers** (their DOB + home address + email), presented as the subject's life events — none was the subject. `reconstruct` walked every entity's date evidence with no candidate filter, so the quarantine that correctly hides those strangers from the correlator / exposure / confirmed count was simply absent from the timeline. Now excluded at the source, fixing BOTH timeline consumers (web `scan_timeline` + CLI dossier). This is the per-view candidate-consistency gap made concrete. | 3555 ✅ |
| 2026-06-23 | **§4.1 also surfaced in the human `--output dossier`** — a "Cross-scan leverage" section lists this scan's identifiers that bridge ≥ 2 prior investigations (top 8); absent on a first-ever scan (nothing bridges yet). `print_dossier` gains `leverage`; dropped its now-redundant `sid` param (== `scan.id`) to stay within the arg-count lint without an allow. [retention/enrichment] | **Completes §4.1 across both live output modes** so the operator sees the cross-investigation bridges in the normal human view, not only in scripted JSON. Same live surface (no determinism contract); only genuine bridges shown to keep a fresh-scan dossier clean. | 3554 ✅ |
| 2026-06-23 | **§4.1 surfaced in the live `--output json` scan payload** (`enrichment_leverage` field; `LeverageRanked` now `Serialize`). [retention/enrichment] | **Completes §4.1 with the CORRECT surface.** Considered the debug bundle first and rejected it: leverage is a CROSS-SCAN / time-variant view (degree grows as investigations accumulate), which would break the bundle's documented "byte-identical across time for an immutable scan" determinism contract (the back-to-back determinism test wouldn't even catch it). The live scan JSON — where `exposure` already lives — has no cross-time contract, so it's the right home; any script consuming `--output json` now gets the ranked cross-investigation bridges. Additive field, no shape test broken. | 3554 ✅ |
| 2026-06-23 | **§4.1 enrichment-leverage query** — `core::engine::rank_enrichment_leverage(store, entities, limit) -> Vec<LeverageRanked>` ranks the high-leverage retained identifiers by how many distinct investigations each bridges. [retention/enrichment, data_retention_design §4.1] | **First build step of the retention asset.** Implements "which of my retained data most empowers the rest": filters to `is_cross_scan_candidate` join keys (email/phone/crypto/distinctive-username/full-name/specific-address — never infra/coarse-geo/permutations), scores by the REALISED cross-scan degree (`observation_count`, distinct scans recording the value), no invented weighting. Read-only counterpart to `link_cross_scan_history`. Public query API awaiting its CLI/bundle consumer (same shape/precedent as P3 `low_confidence_evidence`). Pure, offline, deterministic (UID tie-break), bounded point-lookups — Termux-safe. | 3554 ✅ |
| 2026-06-23 | **`docs/data_retention_design.md`** — architecture + lawful basis for the data-retention/enrichment asset. Grounds the goal in the EXISTING cross-scan flywheel (`recall_prior_entities`, `link_cross_scan_history`/`_cooccurrence`, `promote_cross_scan_corroborated`) and shows `is_cross_scan_candidate` already encodes the "data that empowers other data" join-key priority. [strategy, OSINT value] | **Highest-value autonomous deliverable with code work exhausted.** Maps the user's retention/enrichment vision onto what HSE already builds (it's ~80% there), separates the sellable asset (first-party derived graph) from the un-resellable licensed/PII layers, front-loads the compliance guardrails the resale-of-cached-data instinct would have destroyed value on, and sequences 4 concrete next steps inside the existing rusqlite/WAL pattern (no new crates). Doc-only — no build/test impact. | n/a (docs) |
| 2026-06-23 | **CHECKPOINT — high-confidence backlog exhausted.** This loop closed the non-corroborating over-credit class (AU-010→AU-062/063→GEXF), role-mailbox/junk-handle identity gates (AU-001/045/046), a proptest-found UID-stability bug (`normalise`), the Exposure-Index-in-bundle gap, the relation-graph root-cause, and completed four curated data lists (`source_family`, `INFRA_DOMAINS`, `MEGA_DOMAINS`, `COMMON_SURNAMES`). Verified already-sound (no change): `.corroboration` unused by rules, `is_role_mailbox` scope, merge determinism (`merge_is_order_independent` proptest + 3 unit tests), `is_placeholder_username` coverage, and the `TargetKind::detect` classifier cascade + domain detection (permissive shape-matching is deliberate to avoid TLD-list staleness; `is_domain_shaped` vs `looks_like_domain`/`is_app_package_id` are two functions correctly tuned for two contexts — general input vs. package-ID-flooded stealer feeds — so unifying them would regress one side). Remaining work is qualitatively different — needs live scan data (non-Cloudflare/Fastly CDN IP ranges hold real customer hosts; unsafe to hardcode) or is product/strategy (data-retention model) or a merge-to-main decision. Loop paused on genuine-value grounds, not failure. | n/a |
| 2026-06-23 | **`COMMON_SURNAMES` extended with 15 top-~100 English/Welsh/Scottish names** (`griffiths`, `cox`, `chapman`, `reynolds`, `lloyd`, `harvey`, `owen`, `owens`, `fox`, `griffin`, `johnston`, `hamilton`, `wallace`, `fletcher`, `pearson`). [precision, false-Critical-kin] | **Completes the AU-051/AU-061 commonness gate.** `is_common` weakens a shared-surname kinship signal so a *common* surname doesn't manufacture a Critical "household of relatives". The list carried names down to ~rank 150 (`watts`, `barker`) yet omitted genuinely top-50–65 ones (`griffiths`, `cox`, `chapman`) — so two unrelated Griffiths in one metro were asserted Critical kin, the exact false-CRITICAL the AU-061 gate exists to prevent. All additions are unambiguously high-frequency (none collide with the distinctive-surname test set, incl. the subject's own `Bamford`). | 3553 ✅ |
| 2026-06-23 | **`MEGA_DOMAINS` people-search aggregator category extended** (+15: `fastpeoplesearch.com`, `thatsthem.com`, `truthfinder.com`, `instantcheckmate.com`, `clustrmaps.com`, `zoominfo.com`, `rocketreach.co`, `cyberbackgroundchecks.com`, `advancedbackgroundchecks.com`, `smartbackgroundchecks.com`, `searchpeoplefree.com`, `peoplefinders.com`, `checkpeople.com`, `fastbackgroundcheck.com`, `ussearch.com`). [scan-driven, aggregator noise] | **Dampens stranger co-occurrence noise the category exists for.** People-search sites scrape and republish each other, so one person scan surfaces *dozens* — the list's own comment notes they "flooded a real scan as stranger co-occurrence addresses." It had ~15 of the common US/AU aggregators but missed ~15 equally-common ones (FastPeopleSearch, TruthFinder, ZoomInfo, RocketReach, …); those were expanded at full weight, pulling each aggregator's stranger footprint into the graph. All are commercial aggregator services — never a subject's own domain — so suffix-dampening them is zero-risk (verified by test). | 3553 ✅ |
| 2026-06-23 | **`INFRA_DOMAINS` extended with common shared infrastructure** (Azure DNS `azure-dns.*`, Google Cloud DNS `googledomains.com`, ClouDNS/DNSimple; Azure/Edgecast/Limelight CDN; Azure/AWS app hosting `cloudapp.net`/`elasticbeanstalk.com`; Mailchimp/Postmark/Mailjet ESP; M365 `protection.outlook.com`, Barracuda, Rackspace mail). [scan-driven, infra-pollution] | **Reduces the residual infrastructure pollution at its source.** `is_infra_domain` drops incidental provider estate from EXPANSION and gates `@infra` email admission; the list missed several providers that surface constantly in real NS/MX/CNAME records, so a person scan still burned rounds mapping Azure/Google-managed DNS, CDN edges, and ESP/mail-gateway hosts. Exact-suffix match over unambiguous infrastructure (never a subject's own apex/mailbox) — zero false-positive risk, verified by test (incl. that the `protection.outlook.com` gateway suffix does NOT swallow `outlook.com` freemail, a prime finding). The IP-range analog (non-Cloudflare/Fastly CDN) stays deliberately unhandled — those ranges contain real customer hosts. | 3553 ✅ |
| 2026-06-23 | **`source_family` completed for the module registry.** Classified 23 real modules that were silently falling to `"other"`: internet scanners / IP-reputation feeds → `infra` (`securitytrails`, `onyphe`, `zoomeye`, `criminal_ip`, `netlas`, `bgpview`, `ripestat`, `abuseipdb`, `ipqs`, `portscan`, `netblock`, `domainsdb`); authoritative people/business/professional registries → `identity_registry` (`au_electoral`, `asic_director`, `gleif_lei`, `ahpra`, `acnc`, `fullcontact`, `contact_enrich`, `au_people`); `hudsonrock` → `breach`, `crates_io` → `code`, `exa_search` → `search`. [audit-driven] | **Restores the rules' cross-family recall.** `source_family` is the orthogonality measure for AU-045 / AU-062 / AU-063; with these modules unclassified, a subject genuinely confirmed across, e.g., the electoral roll AND a breach, or an asset seen by SecurityTrails AND a search engine, was under-counted as one family and the finding never fired. Every added mapping is unambiguous, so each new firing is a TRUE cross-family positive — never over-credit (genuinely ambiguous/non-family sources like `chain_intel`, `virustotal`, `threatfox`, `device_sensors`, `username_variants` are deliberately kept `other`, pinned by test). Makes the function's documented "covers the registry" contract actually hold. | 3553 ✅ |
| 2026-06-23 | **`normalise` (Domain) made idempotent when a BOM shields a control byte.** Re-trim after `strip_format_noise`, mirroring the Username arm. [proptest-found] | **UID stability — the cross-scan dedup invariant.** A property test (`normalise_is_idempotent`) shrank to `Domain "\u{feff}\u{b}¡"`: a leading BOM is not whitespace, so `value.trim()` stopped at it and left the `\u{b}` (vertical tab) behind; stripping the BOM exposed that byte, which the first pass kept (`\u{b}¡`) but a second pass trimmed (`¡`) — so the SAME host keyed to two different SHA-256 UIDs across scans, silently fragmenting one identity. The `proptest-regressions` seed already held a sibling case (`"\u{e000}\t."`), confirming the edge class; pinned now by a deterministic regression plus the re-trim fix. | 3553 ✅ |
| 2026-06-23 | **GEXF graph export: shared-evidence co-occurrence edges now key on `corroborating_sources()`, not `evidence_sources()`.** [audit-driven, same class] | **Removes false visual clusters from the Gephi/Cytoscape graph.** The co-occurrence edge means "an independent source named both entities". Keying on raw evidence wired every `name_intel`-derived candidate (the seed's own permutation engine) to every other, and every `recall`/`cross_scan_history` replay to every other, into a dense hairball that swamps the real structure an analyst opens the graph to see. Genuine co-occurrence (two values in one breach dump) is preserved; the seed-derivation lineage is still carried — correctly — by the typed `DerivedFrom` edges. Golden byte-stable test unaffected (its shared source `crtsh` is corroborating); new `gexf_excludes_non_corroborating_co_occurrence` pins it. | 3551 ✅ |
| 2026-06-23 | **AU-062 / AU-063 (multi-pathway & gap link analysis) orthogonality now counts `corroborating_sources()`, not `evidence_sources()`.** The shared `source_families` helper excluded only the `"other"` bucket, so two of the non-corroborating passes still posed as independent families: `name_intel` → `identity_registry`, `geo_normalize` → `infra`. [audit-driven, AU-010 sibling] | **Extends the AU-010 fix to the graph side.** These detectors fire only on corroboration across **≥2 orthogonal source families** — their whole precision claim. But a seed-derivation (`name_intel` mints the permuted handles/emails) and a deterministic geo-replay (`geo_normalize`) were each counted as a second family, so a link backed by `infra + a name guess` was asserted as cross-family corroborated. Proven: the new `au062_excludes_non_corroborating_source_families` test FAILS under the old `evidence_sources` form (AU-062 fired on a `name_intel`-derived family) and passes after — the same over-credit `c_effective`/AU-010 removed from the source-count side, now closed on the multipath/gap orthogonality side (also tightens the engine's multipath-corroboration promotion boost, which shares the finder). | 3550 ✅ |
| 2026-06-23 | **Relation graph `relations: 0` — root-caused (NOT a builder bug) + regression guard added.** An offline diagnostic ran all 14 `derive_*` passes over realistic person-scan shapes: a name-seeded scan anchors a graph (subject→identifier `IdentifiedBy` ×2 + alias), an email-seeded scan resolves the subject email to a breach-named Person, and orphan identifiers fabricate nothing. The builders are sound. [scan-driven] | **Correctly NOT fabricating edges.** The live bundles' empty graph is a downstream symptom of the *entity composition* — infrastructure-dominated (the same CDN/provider mass behind the 82% AU-010 pollution) and poor in person-name evidence attrs — not a defect to "fix" by inventing relationships (a false relationship is as damaging as a false CRITICAL for LE/defence). The honest deliverable is the new `person_scan_entities_anchor_a_relation_graph` guard: it pins that realistic person-scans MUST anchor a graph (catching a refactor that silently zeroes the derivers — the real failure mode) while orphans MUST NOT invent identity edges. The AU-010 fix already shrinks the dominant non-linking entity class. | 3549 ✅ |
| 2026-06-23 | **Exposure Index wired into the full dossier / debug bundle.** `render_full` now opens with the calibrated 0–100 `── EXPOSURE INDEX ──` headline + per-signal breakdown (`assess(entities, correlations)`), so every auto-saved dossier and every `--format debug` bundle carries the same verdict the live `print_dossier` shows. [scan-driven] | **Closes a live-bundle gap.** The uploaded debug bundles had the operator-facing Exposure Index in the live dossier and `report.json` but NOT in the on-disk/debug artifact — the one an analyst or reviewer actually reads back. `assess` excludes candidates + sub-floor speculation internally, so the bundle headline matches the live verdict exactly while the dossier below still lists every entity unredacted. Stays byte-deterministic (no wall-clock), so `debug_bundle_is_deterministic` holds. | 3548 ✅ |
| 2026-06-23 | **AU-045 / AU-046 (identity confirmation & cross-platform resolution) now require a real identity anchor.** Added `is_anchorable_handle` (len ≥ 4 ∧ ¬`is_generic_handle`) gating both rules' Username arms; AU-045's Email arm excludes role mailboxes (`is_role_mailbox`). Extended `is_generic_handle` with a `NON_IDENTITY_TOKENS` list (`from`, `dns`, `www`, `http`, …) so AU-034 shares the gate. [scan-driven] | **Stops junk handles becoming "confirmed identities".** A live person-scan fired AU-045 "confirmed across 2 service families" on `from` (a bare function word) and `dns` (a 3-char acronym) — parser artifacts that any breach/forum corpus echoes, not aliases — and would have keyed AU-046 resolution on them, fusing unrelated records into one person. Role mailboxes (`abuse@…`) are confirmed everywhere by definition and are an org desk, not the subject. The handle-quality gate AU-034 already enforced is now applied uniformly across the identity-cluster family, so the strongest "confirmed identity" finding can't rest on extraction noise. | 3548 ✅ |
| 2026-06-23 | **AU-001 (multi-source breach corroboration) skips role/provider mailboxes (`is_role_mailbox`).** [scan-driven] | **Kills a false CRITICAL.** A live person-scan fired AU-001 CRITICAL on `abuse@godaddy.com` "found in 2 breach sources: hibp, xposed_or_not" — but a registrar/provider role desk is in every breach list as a matter of course; it is not the subject's address or exposure. A false CRITICAL is the worst error class for LE/defence credibility. `is_role_mailbox` already gates expansion (dispatch.rs:70); now it also gates the breach Critical, per the bundle audit's "never breach-check or identity-cluster them". | 3547 ✅ |
| 2026-06-23 | **AU-010 (infra consensus) now counts `corroborating_sources()`, not `evidence_sources()`.** [scan-driven] | **Retires the #1 live-scan flaw.** Two real person-scans (508/460 entities) self-diagnosed 265/324 correlations (82%) as `AU-010` infrastructure noise — CDN/provider edge IPs "confirmed by dns_intel, doh_resolver, **recall**". `recall` is a replay of the same prior observation, not an independent source; counting only corroborating sources (the set `c_effective` uses) drops the recall-inflated edges below the 3-source bar while genuine 3-independent-source consensus still fires. Directly removes the bulk of the "infrastructure-pollution inflates correlations" finding. | 3546 ✅ |
| 2026-06-23 | **P3** — `storage::Store::low_confidence_evidence(threshold, since_seconds) -> Vec<EvidenceAnomaly>` (+ `EvidenceAnomaly { entity_uid, module_name, confidence, created_at }`, default threshold 0.3). Queries the indexed `confidence`/`observed_at` columns, resolves the module from each entity's evidence sources. | **Audit / review trail.** Surfaces the weak (Candidate-tier) findings an LE/defence reviewer must NOT yet trust — the anomaly query the audit bridge / `hse audit` consume. Tested against an in-memory WAL store (threshold + since-window). | 3545 ✅ |
| 2026-06-23 | **P1 — NOT BUILT (architecturally inapplicable).** `LlmAuditRecord` audits LLM call boundaries; HSE makes ZERO runtime LLM calls (guard `runtime_carries_no_ai_ml_inference_dependency` denylists `llm`). `temperature: f32` would be a permanently-dead field, and any inference dep would trip the guard → FAIL. Per the FAILURE RULE + "no dead fields", documented not built. | The evidentiary-reproducibility intent it reaches for is already served by `Evidence{source,recorded_at}` + ATT&CK tags + `raw_archive`. | n/a |
| 2026-06-23 | **P2 — NOT BUILT (no probability distribution exists).** Spec computes "Shannon entropy from the classifier probability distribution"; `core::classifier` is deterministic single-kind via ordered shape-gates (`TargetKind::detect`) — there is no distribution to take entropy of. Synthesising one would be pseudo-rigor, and the re-injection path carries only decisive kinds (the entropy gate would be near-dead). | Anti-wrong-attribution on autonomous pivots is instead served by the engine-expansion + correlator precision gates landed this session (AU-060 floor, surname anchors, AU-061). | n/a |
| 2026-06-23 | Made the assoc consumers multi-value-aware: `entity_residences` / `entity_phones` (AU-049/050/051) now `split("; ")` each evidence value, matching the producer convention (`with_attr` / `merge_evidence_attrs`) and `breach_pii::scan_evidence`. | **End-to-end consistency (completes the prior fix).** Verifying the prior accumulation change revealed an asymmetry: breach-PII rules split multi-value `"a; b"` attrs, but the assoc rules normalised the whole string — so two accumulated phones concatenated into one bogus digit-run key and two addresses garbled into one non-matching key. Now every distinct address/phone clusters correctly, whether multi-field (`with_attr`) or conflict-accumulated (absorb). | 3544 ✅ |
| 2026-06-23 | `merge_evidence_attrs` (the absorb path) now ACCUMULATES conflicting same-key values as sorted `"a; b"` instead of dropping the larger via min-value — aligning it with `Evidence::with_attr`'s established in-record accumulation. | **Evidence fidelity + consistency.** Two code paths handled "same key, two values" differently: the public builder kept both, the internal absorb path kept one. A conflicting re-observation (a corrected value, or a namesake's differing `date_of_birth` on the same breach source) is itself evidence the disambiguation rules (AU-073) need — silently dropping it loses an evidentiary signal. Sorted so the fold stays merge-order-independent (Determinism Requirement). | 3543 ✅ |
| 2026-06-23 | AU-061 now drops `family-candidate` Person entities whose surname differs from the subject's (when the subject surname is known) before asserting "shared surname". | **False evidentiary basis removed.** The `family-candidate` tag is also applied by the see_know household path to co-located people who do NOT share the subject's surname; AU-061 then claimed "shared surname relative" for them. A dossier used in LE/defence must not assert a basis that's untrue — different-surname co-residents are now excluded from this finding (same-region-without-shared-surname is not itself a finding). Address candidates (surname-matched by their module) are kept. | 3543 ✅ |
| 2026-06-23 | AU-061 (family geo-corroboration) now commonness-gates its Critical escalation: a COMMON subject surname caps at High (not Critical) and softens the "relatives" wording. Relocated `subject_surname` from `engine::passes` to `core::geo_family` as the single shared source. | **Wrong-attribution (Critical false kin).** Within `FAMILY_GEO_KM` (150 km) the existing namesake pass doesn't fire (it guards only >800 km), so three unrelated "Smith"s in one metro catchment were asserted Critical "household of relatives". A common surname makes shared-region co-location weak evidence — now a High lead to verify; a distinctive surname keeps the strong Critical signal. Mirrors the AU-051 / kinship commonness gates. | 3542 ✅ |
| 2026-06-23 | Closed admission-gate placeholder gaps: `is_placeholder_email_local` now rejects `test`/`redacted`/`placeholder` local-parts, and `is_placeholder_username` rejects `redacted`/`placeholder`. Updated the one smoke fixture that used `test@contoso.com` as a real target. | **Anti-garbage-promotion.** `test@gmail.com`, `redacted@…`, the `redacted` handle were admitted as real entities — polluting the graph and falsely corroborating. The admission gate doesn't consult the role-mailbox path, so these slipped through. Exact-match (separator-stripped) only, so real handles that merely contain the token (`tester`, `firstnations`, `redactedtruth`) are untouched. | 3540 ✅ |
| 2026-06-23 | Extended the invisible-noise strip to the `Username` and `Domain` normalize arms via one shared `strip_format_noise` helper (BOM/ZWSP/ZWNJ/ZWJ/WJ), borrowing on the clean path so the hot normalize path doesn't allocate. | **Identity integrity, made uniform.** The same BOM/zero-width fragmentation that hit emails also forks a `Username` (`\u{feff}@handle`) and a `Domain` (`\u{feff}example.com`) into a second UID. One helper now guarantees the three identity kinds dedup consistently — no kind is a fragmentation gap. | 3540 ✅ |
| 2026-06-23 | `core::entity::normalise` (Email) now strips invisible format/zero-width noise (BOM U+FEFF, ZWSP U+200B, ZWNJ/ZWJ/WJ) and treats control chars (NUL, …) as value terminators. | **Identity integrity / anti-fragmentation.** A BOM-prefixed or ZWSP-embedded address (common in UTF-8-BOM exports and dirty breach dumps) keyed the *same* mailbox to a *different* SHA-256 UID, splitting one person's footprint across two nodes — corrupting corroboration counts and the evidence chain. Now the clean and noisy spellings share one UID. | 3539 ✅ |
