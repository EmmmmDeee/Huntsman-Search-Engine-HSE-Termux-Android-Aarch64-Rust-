# Gap Register — autonomous improvement log

Running log of source-intelligence improvements, one line per change: **what / why
(LE-defence evidentiary or OSINT-quality value) / test result**. Newest at the top.

> **Baseline note (honest reconciliation).** The autonomous protocol that seeded this
> loop stated a CURRENT STATE of `v9.0.0 · 8,239 LOC · 29 modules · 46 tests` and an
> `LlmAuditRecord` priority. That describes a different codebase. This repository is
> **v1.9.0 · ~172k LOC · 124 modules · 3,539 lib + integration tests**, and it carries a
> **guard-enforced no-AI/ML/LLM invariant** (`runtime_carries_no_ai_ml_inference_dependency`),
> so there are no runtime LLM call boundaries to audit — that priority is inapplicable and
> was not built (it would be a dead field, failing this register's own quality bar). The
> loop is therefore run against the **real** baseline, on genuine improvements, keeping the
> actual 3,539-test suite green.

| Date | Change | Why (evidentiary / OSINT quality) | Tests |
|------|--------|-----------------------------------|-------|
| 2026-07-04 | **Correctness — two cross-scan-history idempotency probes matched a bare substring, silently dropping a genuine distinct link.** Two workflow-confirmed instances of the same bug in `core::engine::history`. `endpoint_has_cooccurrence` keyed idempotency on `ev.summary.contains(partner)` and `endpoint_has_relation_recall` on `contains(kind) && contains(partner)` — unanchored substrings — while the summaries they probe write the partner BACKTICK-delimited (`` Co-occurred with `{partner}` ``) and the kind PAREN-delimited (`(subdomain_of)`). So an entity already bridged to `` `alice2` `` made the probe report it as already linked to a NEW partner `alice` (a substring of `alice2`), so the linker skipped attaching the genuine `alice` co-occurrence/recall evidence — a real cross-investigation association lost whenever one partner (or kind) is a substring of another (numbered handles like `bob`/`bob2`; a relation kind that is a substring of another). Fixed both to match the delimited token the summary actually writes (`` `{partner}` ``, `({kind})`). [correctness, no-dropped-link, engine, code+tests] | Cross-scan co-occurrence and relation-recall are the "bridge investigations" signal — the whole point of the local intelligence database is to surface that this identifier recurred WITH a specific partner in an earlier case. Silently dropping that link because the partner's value happens to be a substring of a previously-recorded one (extremely common with `handle`/`handle2` numbering) is a real recall loss on exactly the high-value cross-case pivot the feature exists to provide, and it is invisible (the link simply never appears). Anchoring on the delimiter the summary already uses makes the probe say precisely what it means. | +1 test (`idempotency_probes_match_the_delimited_partner_token_not_a_substring`): the recorded `alice2`/`bob2` still match (idempotency preserved) but the substrings `alice`/`bob` do not (fail-before: they matched, dropping the real link); the existing idempotency tests still pass; full suite green (4551) |
| 2026-07-04 | **Parse-layer precision — the SeekNow stealer URL gate was weaker than its oathnet_pro twin (a bare `len >= 4`); now single-sourced.** Workflow-confirmed. `see_know::extract::extract_entities` minted the stealer `url`/`url_str` field as a `Url` entity on `url.len() >= 4` alone — no scheme, no host — while the sibling `oathnet_pro::stealer` (whose model see_know's own comment claims to mirror, "exactly OathNet's stealer model") gates the identical field on `u.starts_with("http") && u.contains('.')`. So a native-app URI (`android://…`), a scheme-less fragment, or a capture sentinel ≥4 chars became a bogus `Url` node in see_know that oathnet_pro rejects. Fixed by mirroring the twin's gate (trim, then scheme + dotted host); the paired `<username>@<url>` `Credential` stays ungated, as in oathnet_pro (a login for a native-app surface is still a real credential). [precision, parse, single-source, code+tests] | A phantom `Url` from a stealer log is not a cosmetic dedup issue: a captured login surface is a first-class pivot that seeds crawl / DNS / certificate expansion, so a bogus `android://…` or scheme-less node misdirects that expansion onto nothing (or onto a platform's own infra), wasting the scan and polluting the graph — the exact class the sibling parser's scheme+dot gate exists to prevent. Two stealer consumers claiming to share one model must actually share the gate, or one silently admits what the other rejects. | +1 test (`extract_entities_rejects_non_web_stealer_url_but_keeps_the_credential`): an `android://com.example.app` url mints no `Url` but still yields the login↔surface `Credential`; the existing real-`https://` stealer test still passes; full suite green (4550) |
| 2026-07-04 | **MITRE precision — the five name-less `Social` modules over-claimed ATT&CK T1589.003 (Employee Names); each now maps to its real collection.** Closes the follow-up deferred by the `username_search` fix. `streaming_probe`, `gaming_profile`, `discord_snowflake`, `structured_id`, and `fediverse` are all `ModuleCategory::Social` with no override, so each inherited the default `["T1593.001", "T1589.003"]` — but NONE emits a real-name `Person` (their `produces()` are `Url`/`Username`/`Email`/`MacAddress`), so every finding falsely stamped a gathered person-name. Corrected each: the three handle/platform modules (`streaming_probe`, `gaming_profile`, `discord_snowflake`) → `["T1593.001"]` (Social Media only); `fediverse` → `["T1589.002", "T1593.001"]` (it also emits profile emails, like nostr); `structured_id` → `["T1592.001"]` (Gather Victim Host Information: Hardware) — it's an OFFLINE structured-ID decoder whose signal is the generating machine's MAC address in a UUIDv1, not a social search, so it drops BOTH inherited social techniques. `username_variants` deliberately keeps T1589.003 (a conscious convention) and is untouched. [precision, MITRE, code+tests] | The inline `attack:` tag is HSE's only MITRE surface, so the module→technique map's precision IS the product's ATT&CK fidelity: an analyst reading Reconnaissance coverage off the tags would otherwise mis-file five handle/ID modules as "Employee Names gathered" — asserting a name was obtained on findings that carry none — and would mis-file an offline UUID→MAC host-hardware decode as social-media search. Applying the guard-encoded "emits Person ⟺ claims Employee Names" rule (and honest technique selection for the non-social decoder) to the last five inheritors makes the ATT&CK footprint uniform and truthful. | +7 guard assertions in `attack_overrides_attribute_collection_modules_precisely` (a 3-module loop → `["T1593.001"]` + `!contains T1589.003`; explicit `fediverse` → `["T1589.002","T1593.001"]`; `structured_id` → `["T1592.001"]` + neither T1589.003 nor T1593.001); full suite green (4549) |
| 2026-07-04 | **False-merge precision — AU-042 fused every `pgp-linked` email in the scan into one owner (unpartitioned by key) and fired on a single address; now partitioned by key fingerprint, ≥2 per key.** Workflow-confirmed. `rule_au_042_pgp_email_identity` collected ALL `pgp-linked` `Email` entities scan-wide and emitted ONE High "a PGP key links N emails to one owner" over the whole set, firing whenever the set was non-empty. Two defects: (1) no key-fingerprint partition — emails bound to two DIFFERENT PGP keys (two potentially-different people) were merged into a single asserted owner, a false identity merge; (2) fires on one email — a lone `pgp-linked` address produced a degenerate "links 1 email to one owner" High assertion, though the docstring's contract is "two or more." The `pgp` module already stamps each pgp-linked email with a `key_fingerprint` evidence attribute, so the fix partitions on it: group emails by fingerprint (deterministic `BTreeMap` fingerprint→address→uid), emit one finding PER KEY binding ≥2 distinct addresses, name the fingerprint; an email with several fingerprints belongs to each key it bound, an email with none is excluded. [precision, no-false-merge, correlator, code+tests] | Two PGP keys are two cryptographic identities: merging the emails they bind into one asserted "owner" fabricates a person, cross-attributing one key-holder's addresses to another — the same worst-class false merge AU-046/AU-081 guard against, here at the strongest same-owner tier the engine emits (a key holder's own signed assertion). And a "1 email → one owner" firing is a contentless High that inflates the identity graph with a non-link. Partitioning on the fingerprint the pgp module already provides makes the finding say exactly what the key proves — these addresses, this key — and nothing more. | +2 tests (`au042_does_not_fuse_emails_from_two_distinct_keys`: key A binds two, key B binds two → two findings of two, never one of four — fail-before against the single fused finding; `au042_does_not_fire_for_a_single_pgp_linked_email`: a lone bound address does not fire); the existing group test updated to attach the `key_fingerprint` a real pgp hit always carries; full suite green (4549) |
| 2026-07-04 | **False-merge precision — AU-046 fused EVERY platform-sourced identifier in the scan into every alias; now scoped to the alias's own account.** Workflow-confirmed. `rule_au_046_cross_platform_identity_resolution` built one scan-wide `resolved` set (every `Email`/`Person` with a code/forum/social corroborating source) and attributed ALL of it, at `Severity::High`, to EVERY alias — with no tie between an identifier and the alias's actual account and no role-mailbox gate. So a co-author's email surfaced from a different platform account, a second alias's identifiers, or a `noreply@` support desk were all fused into a person's "resolves to N real-world identifiers" finding — the exact stranger-fusion the docstring falsely claimed it "can't" do. Fix scopes each resolution to the alias's OWN account(s): an identifier resolves to a given alias only when it shares ≥1 concrete corroborating SOURCE with that alias (the same platform module that confirmed the handle also surfaced the identifier), and role mailboxes are excluded via `core::validation::is_role_mailbox` (the AU-045 gate). `resolved` is now per-alias, so cross-alias contamination is impossible and an alias with no own-account identifier no longer fires. Docstring corrected to describe the real shared-source linkage. [precision, no-false-merge, correlator, code+tests] | A wrong identity resolution is the worst outcome for an evidentiary OSINT tool: it doesn't add a spurious node, it asserts — at High severity, as the subject-as-hub payoff of an alias investigation — that a specific real person owns email/name identifiers that in fact belong to a co-author, another handle, or an org role desk, cross-contaminating two people's dossiers and every conclusion drawn from the merge. Requiring a shared source implements exactly what the rule's own docstring already promised ("identifiers a platform account itself published") and mirrors the account-hygiene gates AU-045 already applies. | +1 test (`au046_resolves_only_the_alias_own_account_identifiers`): an own-account email (shared `github_user` source) resolves; a stranger from an unshared `gitlab_user` account and a `noreply@github.com` role mailbox are both excluded; the finding counts exactly one identifier — fail-before against the scan-wide set / pass-after; the two existing AU-046 tests still pass; full suite green (4548) |
| 2026-07-04 | **No-fabrication precision — `streaming_probe` stamped a flat 0.92 confidence and asserted a sensitive identity on every hit, including bare status-only detections.** Workflow-confirmed. The cam/fan-subscription/adult-video prober fans out across ~43 platforms; 41 detect on a bare `Detect::StatusEq(200)` (a HEAD/GET 200 alone) and only 2 on `Detect::StatusAndNotBody` (200 AND the body lacks the platform's "not found" marker). Every `Found` minted a `Url` at a hard-coded 0.92 and any cam/fans/adult hit stamped the summary with `cam-identity-exposed` / `subscription-platform-found` / `adult-profile-found` — but a status-only 200 is weak evidence (a soft-404, a CloudFlare interstitial, or a catch-all route answers 200 for any handle), so a single unverified probe fabricated a high-confidence, reputationally-damaging "this person has a cam/adult identity" claim. Mirrored the sibling `username_search`'s established fix: a pure `detection_strength(detect) → (f64, bool)` returns `(0.92, true)` for a body-verified hit and `(0.74, false)` for a status-only one; each `Url` carries its tiered confidence and a `verified-detection` / `weak-detection` provenance tag; the strong exposure tags are gated on a body-verified hit in that category (weak-only categories still surface their 0.74 URLs — the lead isn't lost, only its unearned assertion); the summary records `hits_verified` / `hits_status_only`. Emit logic extracted to a pure, testable `build_entities`. [precision, no-fabrication, code+tests] | Asserting that a real person operates a webcam/adult/fan-subscription identity is one of the most reputationally-sensitive claims an OSINT tool can emit — fabricating it from an unverified HTTP 200 is precisely the false-positive an evidentiary product must never produce, and doing so at 0.92 confidence would carry it straight into the dossier headline and the correlator's identity fusion. Tiering the confidence and gating the exposure claim on body-verified evidence keeps the lead visible (as a weak, discountable 0.74 URL) while refusing to assert the identity without proof — the same discipline the sibling presence-prober already holds. | +2 tests (`detection_strength_tiers_status_only_below_body_verified`; `build_entities_tiers_confidence_and_gates_exposure_on_verified`: a status-only cam hit → 0.74 + weak-detection + NO cam-identity-exposed, a body-verified hit → 0.92 + verified-detection + the exposure tag); full suite green (4547) |
| 2026-07-04 | **Export integrity — the `/graph.gexf` API export leaked quarantined `candidate` breach-victims as nodes, and every GEXF caller could emit dangling edges (two coupled workflow-confirmed defects, fixed as one).** The web GEXF endpoint (`api::scan_export::scan_export_gexf`) passed `store.entities_for_scan(id)` UNFILTERED to `entities_to_gexf`, so every quarantined `candidate` (a non-subject breach co-occurrence stranger) became a labelled node — leaking a foreign breach-victim list under the subject's scan, which the CSV/`report.json`/CLI exports all strip by default. Naively filtering candidates would have exposed the second, coupled defect: `render_gexf` already dropped candidate NODES but passed the FULL relation set, so a relation to a filtered candidate emitted an `<edge>` referencing an undeclared node id — structurally-invalid GEXF Gephi rejects. Fixed both at their correct layers: (1) `entities_to_gexf` now emits a relation edge only when BOTH endpoints are declared nodes, making "every edge references a declared node" a serializer invariant that protects every caller (CLI and API); (2) `scan_export_gexf` filters `candidate` rows by default with a `?include_candidates=1` opt-in, matching the CSV endpoint. [privacy, export-integrity, no-PII-leak, code+tests] | Leaking a list of unrelated breach victims — real people who merely co-occur in a breach corpus — under a target's scan export is a serious third-party PII disclosure an intelligence tool must never make by default; the whole `candidate` quarantine exists to prevent it, and GEXF was the one export that bypassed it. A structurally-invalid graph that Gephi refuses to open is a silent capability loss for the analyst doing link analysis. Making edge-validity a serializer invariant (not a per-caller obligation) means no future export path can regress either way. | +2 tests (`gexf_drops_relation_edges_referencing_a_filtered_out_node`: a relation to a filtered node emits no dangling edge; `scan_gexf_quarantines_candidate_nodes_by_default`: candidate absent by default, present with `?include_candidates=1`); GEXF golden byte-stable test unaffected; full suite green (4545) |
| 2026-07-04 | **GEOINT precision (H5 doctrine) — `coord_state` + AU-099 let infrastructure coordinates vote the subject's location; both now apply the `is_infrastructure_geo` guard.** Top-ranked defect from an adversarially-verified precision-discovery workflow (8 subsystem finders → refute-by-default verification → 14 confirmed defects). `coord_state` (`rules/geo/mod.rs`, feeds AU-056/085/092/098) and `rule_au_099_coordinate_reverse_geocode` gated only on `kind == Coordinates && confidence ≥ 0.50` — with NO `is_infrastructure_geo` exclusion — even though every sibling location-voting rule applies it (AU-018 `profile.rs:30`, AU-030 `chain.rs:302`, AU-052/053/059 via `person_anchored_coords`) and the file's own H5 test section is literally titled "infrastructure geo must not vote the subject's location." A bare `ip_geo` coordinate (the datacentre behind the subject's domain: confidence 0.60, `au-state:NSW` tag, no anchoring source) therefore asserted NSW through `coord_state`, so a Sydney server IP manufactured a false AU-056 "jurisdiction conflict" against the subject's real QLD address, and AU-099 announced the datacentre as "the subject's coordinate fix." Added the one guard to both rules; reconciled 7 existing AU-056/085/092/098/099 fixtures (which used a placeholder non-anchoring source — `geo_normalize` or bare — for a real subject coordinate) to carry a genuine anchoring source, so only pure infra (ip_geo-only) coordinates are newly excluded. [precision, GEOINT, no-false-positive, code+tests] | Jurisdiction is a first-class OSINT attribute an analyst acts on directly: a false "your subject is in NSW, conflicting with their QLD address" is a fabricated analytic conclusion an intelligence product must never emit, and a datacentre announced as the subject's own GPS fix mis-locates the person entirely. Both violate the codebase's OWN documented H5 doctrine and diverge from the infra-guard every sibling geo rule already holds; closing the two rules that never adopted it makes the doctrine uniform instead of aspirational. Found by the standing discover→adversarially-verify workflow, not speculation. | +2 tests (`coord_state_excludes_bare_ip_geo_infrastructure_coordinate`, `au099_reverse_geocode_excludes_infrastructure_coordinates`): a bare ip_geo coord is excluded (fail-before: coord_state returned Some(NSW) / AU-099 fired) while an exif_geo control at the same point still votes; 7 fixture reconciliations; full suite green (4543). The workflow's other 13 confirmed defects are queued for subsequent cycles. |
| 2026-07-04 | **GEOINT precision (C5) — AU-059's class-diversity weight was a global scalar (a mathematical no-op); now per-point.** `au059_synergy_fix` is the single source of the dossier's headline "best AU location estimate" and the API's `best_location` fields. It boosted each coordinate's weight by `class_bonus = 1.0 + (classes.len() - 1) * 0.10` — but `classes.len()` is the SCAN-WIDE distinct-class count, so the same constant multiplied every point's weight. A weighted geometric median (and the centroid fallback) is invariant to scaling all weights by one positive constant, so the "bonus" moved the location estimate **not at all** — while its own comment claimed "a point corroborated across more orthogonal classes pulls proportionally more." Fixed to derive the bonus PER POINT from that entity's own distinct anchoring geo classes (`corroborating_sources` → `geo_source_class`), so a coordinate confirmed by several independent collection methods (e.g. a registry address AND a photo GPS AND a wifi sighting at one spot) genuinely outweighs a single-class sighting and pulls the median toward it. Deterministic (HashSet-len is order-independent); the existing outlier-robustness test (single-class points, bonus 1.0) is unaffected. [precision, GEOINT, no-dead-code, code+tests] | The operator's headline "where is this person" answer should weight a coordinate by how many INDEPENDENT kinds of evidence agree on it — that is the entire premise of the cross-seed synergy rule. A weighting factor that is silently inert means the fix treated a lone IP-geo point and a spot corroborated by three orthogonal methods identically, exactly the failure the rule exists to prevent; and a comment asserting a capability the code doesn't have actively misleads the next reader. Making the documented per-point behaviour real closes both — the same "no dead fields / code must do what it says" doctrine the codebase already enforces. | +1 test (`au059_class_diversity_bonus_is_per_point_not_a_global_no_op`): two scans differing ONLY in the eastern point's class span (3 vs 1), holding its source count / c_effective and every other point fixed — byte-identical fix under the old global scalar (fail-before), strictly east under the per-point bonus (pass-after); full suite green |
| 2026-07-04 | **MITRE precision — `username_search` no longer over-claims ATT&CK T1589.003 (Employee Names) on every finding.** Each admitted entity is stamped with its producing module's `attack:<ID>` techniques (`engine::dispatch`), so a per-finding `attack:` tag is HSE's only MITRE surface — the module→technique map's precision IS the product's ATT&CK fidelity. The codebase's guard-encoded convention is exact: a module claims T1589.003 **iff it emits a real-name `Person`** entity (keybase/gravatar/DeHashed/IntelX keep it because they do; github_user/hacker_news/lobsters/nostr/reddit_user were overridden to DROP it as "over-claimed — no Person emitted"). `username_search` ENUMERATES handle presence across 300+ sites and emits only `Url` + `Username` (`produces() = [Url, Username]`; zero `EntityKind::Person` in the module), yet it had no override and inherited the raw `Social` default `["T1593.001", "T1589.003"]` — so every profile URL / username it emitted carried a false `attack:T1589.003` claiming HSE gathered the subject's NAME when it only confirmed a handle exists. It was skipped when its twins were corrected. Added the precise override `["T1593.001"]` (Social Media search only — no bio-email path, so unlike reddit_user no T1589.002) and pinned it in the `attack_overrides_..._precisely` guard, forbidding a regression to the default. Five further un-overridden name-less Social modules (`discord_snowflake`, `fediverse`, `gaming_profile`, `streaming_probe`, `structured_id`) are noted as a discrete follow-up — each emits no `Person` but carries its own technique nuance (fediverse `Email`, structured_id `MacAddress`), so each needs an individual judgement, not a blanket drop. [precision, MITRE, code+tests] | An analyst reading Reconnaissance-technique coverage off HSE's inline tags would otherwise mis-file a pure handle-presence enumeration as "Employee Names gathered" — overstating what the tool (and a tracked adversary emulating it) actually collected, and asserting on a subject's dossier that a NAME was obtained when none was. Correct ATT&CK attribution is evidentiary integrity; the fix simply applies the same guard-encoded "emits Person ⟺ claims Employee Names" rule the codebase already enforces for its twins to the one presence enumerator the override pass overlooked. | +2 guard assertions (`username_search` → `["T1593.001"]` and `!contains("T1589.003")`) in `attack_overrides_attribute_collection_modules_precisely`, fail-before against the inherited default / pass-after; full suite green |
| 2026-07-04 | **Parse-layer precision — the `web_crawler` page email byte-scanner (a third copy) now shares the canonical alpha-TLD gate.** `util::extract::host_has_alpha_tld` was single-sourced earlier today so the two non-regex email admission paths (`looks_like_email` the provider-field gate, `page_emails` the `util` HTML byte-scanner) could never out-admit the free-text `EMAIL_RE` — but a THIRD copy of the byte-scan logic, `web_crawler::crawl_util::extract_emails`, was missed and still gated on the weak `domain.contains('.') && domain.len() > 3` heuristic plus `validate_email_syntax` (which checks dot artifacts but NOT that the TLD is alphabetic). So `admin@10.0.0.1` (IP literal), `user@host.123` (numeric TLD) and `user@host.c` (1-char TLD) each minted a bogus `Email` entity. Made `host_has_alpha_tld` `pub` (module→util is a permitted dependency) and routed `extract_emails` through it, dropping the weak inline gate while keeping `validate_email_syntax` so the combined check (alpha-TLD AND no dot artifacts) is the strictest of the three; the helper's docstring now enumerates all three consumers. [precision, parse, single-source, code+tests] | A garbage `Email` minted at the parse layer is not a cosmetic dedup issue: it seeds name/handle permutations, fires co-location/credential-reuse correlation rules, and pads the exposure index — a fabricated identifier compounding through every downstream conclusion, exactly the false-positive class the doctrine says to kill at the source. A single-sourced gate whose own docstring promises "no admission path can be more permissive" is only true if EVERY path actually uses it; finding and closing the overlooked third copy makes the guarantee real instead of aspirational. | +1 test (`email_extraction_rejects_ip_literal_and_numeric_or_short_tld_hosts`): the IP-literal, numeric-TLD and 1-char-TLD hosts all rejected, a real `ops@acme.com` alongside them still surfaces (fail-before against the `len > 3` gate / pass-after); full suite green |
| 2026-07-04 | **Correlator precision — AU-081 canonical-name match now discounts common names instead of asserting a High identity merge.** The free offline identity-bridge rule `rule_au_081_canonical_person_name_match` links two independently-sourced `Person` records that normalise to one canonical name, but emitted `Severity::High` "records for the same individual" **unconditionally** — the only identity rule with no common-name discount, in a family (AU-051/AU-061/`derive_kinship`/`derive_regional_kinship`/leads/`engine::passes`) that all discount a common surname precisely because many unrelated people share it. So two strangers who each surface as "John Smith" (e.g. a breach dump + a proxycurl profile — different source families, so the independence gate is satisfied) fused into one asserted person, cross-attributing each stranger's evidence to the other. The docstring compounded it, falsely crediting the ≥2-token floor with excluding a "known-common first name like John" (it only rejects <2-token names — "John Smith" passed). Applied the identical `is_common` discount at AU-081's emit site: a canonical name containing a common family token → `Severity::Medium` "a COMMON name many unrelated people share — a lead to VERIFY, not a confirmed merge"; a distinctive name keeps its High "same individual" bridge. Docstring corrected. [precision, correlator, no-false-merge, code+tests] | False positives are worse than missing coverage in an evidentiary tool, and a wrong identity MERGE is the worst class: it doesn't just add a spurious node, it silently re-attributes one real person's breaches/addresses/associates to a different real person, corrupting every downstream conclusion an analyst draws from the merged dossier. A common full name is the single highest-volume false-merge vector in person OSINT — "John Smith" matches across sources constantly and almost never means one person. Discounting it to a VERIFY lead (while keeping distinctive-name matches at High) is exactly the guard the kin/co-location rules already apply to shared surnames, extended to the one identity rule that lacked it. | +1 test (`au081_common_name_is_a_medium_lead_not_a_high_assert`): two "John Smith"/"Smith John" from breach vs proxycurl → Medium + "VERIFY"; control "Haigen Bamford"/"Bamford Haigen" → High + "same individual"; the existing cross-source-match test already pins the distinctive-name High path; full suite green |
| 2026-07-04 | **Stale-marker reconciliation — `PROBLEM_TREE` §7 S2 (P1 HIGH whois-referral SSRF) was fixed + tested but still shown `[ ]`.** The huntsman-cycle picks the next unit by reading the trees, so a P1 node marked open when its fix already shipped mis-directs the loop into re-investigating a done item (the same "keep the living documents honest" class as the 2026-07-01 stale-note corrections). Verified in code — not from the node's own "✅ Fixed" claim — that the referral SSRF gate is genuinely complete: `modules/whois/client.rs::resolve_public_whois` parses `host:port` (incl. `[v6]:port`), refuses any non-43 port and `is_local_domain` host, resolves to the first `!is_private_addr` address, and returns a concrete pinned `SocketAddr`; `mod.rs:284` routes the attacker-influenceable `refer:`/`whois:` referral through it while the trusted IANA bootstrap keeps its constant. Also confirmed robust against the IPv4-mapped-IPv6 smuggle (`[::ffff:127.0.0.1]:43`): `is_private_addr` runs `to_canonical()` first (+ an `embedded_ipv4` NAT64/6to4 check). `SOLUTION_TREE` SOL-SSRF-WHOIS and §4a already recorded the 2026-06-17 delivery; only this `PROBLEM_TREE` marker had drifted. Flipped `[ ]`→`[x]`; no code change. [autonomous-loop, S→P audit, docs-only] | A false "open P1 security defect" is as corrosive to the loop's own correctness as a missed defect: it books phantom future work and would send the next cycle (or a human triaging "what's the top open risk?") to re-audit a whois path that is already vetted, pinned, and tested. Reconciling it keeps the trees — the loop's single source of truth for "what's left" — honest, without touching a byte of the sound, shipped fix. | n/a (docs); the existing `blocks_ssrf_and_non_whois_referrals` regression test re-run green |
| 2026-07-04 | **Live testing restored three modules — two dead from upstream API drift, one tripping its own breaker.** Continued live validation (driving real seeds through individual modules against their REAL endpoints) caught three faults the entire unit suite is blind to — because no unit test exercises a live upstream. **(1) HudsonRock** stealer-login (free, keyless): Cavalier renamed its query parameter `username`→`email`, so every request returned HTTP 400 `"Email is required"` — the whole source was dead. Fixed to `email=` via a testable `search_by_login_url` (curl-confirmed a known-infected address returns 5 stealer records). **(2) StackOverflow** user search: the hard-coded `filter=!9Z(-x.hbL` now 400s `"Invalid filter specified"`, breaking every lookup; dropped it behind `users_by_name_url` (the API default filter returns every field the module reads — `display_name`/`location`/`website_url`/`link`/`reputation`/`creation_date` — verified live, and a default filter can't be invalidated by an API revision the way a custom encoded one can). **(3) Bluesky** user: a non-existent handle answers `400 {"message":"Profile not found"}` (not 404), which propagated as a module error — and the engine's per-module breaker counts it a soft failure, so a name scan's handle fan-out trips the breaker after 3 misses and suppresses Bluesky for the REAL handles too. Added `util::http::fetch_json_or_absent` (generalised `fetch_json_inner` to an `absent_statuses: &[u16]` set; treats 400+404 as the clean negative, 429/5xx still errors) and routed Bluesky through it. [reliability, live-tested, code+tests] | These are the highest-value faults an OSINT tool can have: a source that is silently, totally DEAD (HudsonRock, StackOverflow) returns nothing on every scan while looking healthy, and a breaker that trips on "not found" (Bluesky) suppresses a working source for the very targets that exist — both are pure, invisible recall loss on a live investigation. And they are structurally invisible to the unit suite: only a real end-to-end run against the live service reveals upstream drift. This is the concrete proof that live validation must be a standing part of the process, not a one-off. | +3 tests (`search_by_login_uses_the_email_query_parameter`, `users_url_omits_the_invalid_custom_filter`, `fetch_json_or_absent_maps_400_to_none_while_or_404_still_errors` via a one-shot local 400 server); all three re-verified LIVE against the real endpoints; full suite green |
| 2026-07-04 | **Live end-to-end validation of every target kind — the finalise `O(identities²)` pairwise-pathway sweeps are now bounded.** Real-seed live testing (self-test subject *Haigen Bamford* for identity kinds; public infrastructure — `8.8.8.8`, `github.com`, `AS15169`, the BTC genesis address, a Sydney coordinate, … — for the rest) drove ALL 19 `TargetKind`s through the full scan pipeline plus all 6 export formats and the 161-module self-test. Result: **zero panics, zero crashes, every module error environmental** (blocked cloud IP / no residential exit / provider 4xx-5xx), all exports well-formed. The ONE real flaw: a `full_name` scan took **135–185 s at depth 0** and, cold/richer, exceeded a 150 s external timeout. Instrumented phase timing isolated two finalise passes — AU-062 `multipath_corroborated_links` and AU-063 `single_route_identity_links` — each iterating every identity pair through `disjoint_pathways_in` (depth-5, 4-path). A broad name scan derives ~400 permutation identities → ~80 000 pairs → ~45 s per sweep (the `connection_templates` enumeration was only 207 ms — not the cost). Both now share ONE deterministic `IDENTITY_PAIR_PROBE_CAP = 6 000` in `core::relation::graph`; `identity_uids` is sorted so the cap is a deterministic prefix (byte-identical output preserved, unlike a wall-clock budget), and the best-effort enhancement signals degrade gracefully. **Measured on the real scan: combined pathway phase 48 s → 8 s**, now bounded regardless of identity count; typical ≲110-identity scans still examined in full. [performance, robustness, live-tested, code+tests] | For an on-device tool an agency runs on a phone, a finalise phase that grows `O(n²)` with the identities a scan derives is a real hang/timeout risk — the first live run was killed at 150 s, and a deeper/slower device would fare worse. Bounding it deterministically (not by wall clock) keeps the byte-identical-output guarantee the export/dossier layer depends on while making finalise predictable. Single-sourcing the cap so the two sweeps sharing the primitive can't drift is the same "one finder, no drift" discipline the correlator rules already hold. Found only by driving REAL seeds end-to-end — no unit test would have surfaced an 80 000-pair name graph. | +2 tests (`single_route_links_are_pair_probe_capped_deterministically`, `multipath_links_are_pair_probe_capped_deterministically`) via testable `*_capped(…, max_pair_probes)` seams (cap 0 → empty, cap 1 → ≤1, deterministic prefix); verified live before/after on the actual name scan; full suite green |
| 2026-07-04 | **Comprehensive 6-subsystem audit — cycle 7/N (final actioned): HTTP response-snippet buffers bound peak memory against a single oversized chunk.** `util::http::fetch`'s `error_snippet` and `read_body_capped` both `extend_from_slice`'d the WHOLE streamed chunk and truncated *afterwards*, so the cap only bounded the buffer after a chunk was already fully copied into RAM. A hostile/misconfigured upstream returning one multi-GB chunk (HTTP/1.1 chunked encoding permits any chunk size) is copied whole before the truncate — an OOM risk on a low-RAM Termux device, worst under the `username_search` 32-way probe fan-out. Extracted a shared pure `append_capped(buf, chunk, cap)` that copies at most `cap - buf.len()` bytes and signals when the cap is hit; both readers now use it, single-sourcing the bound. [robustness, memory-safety, code+tests] | An on-device intelligence tool must be resilient to the very upstreams it queries — a memory-exhaustion vector triggerable by any server the operator scans is a denial-of-service on the operator's own phone mid-operation. Making the cap hold at the chunk boundary (not after) closes it, and single-sourcing the two copies of the streaming-read loop stops them drifting. Small, defence-in-depth, but exactly the "never trust a hostile upstream to be well-behaved" discipline the redaction/SSRF/size-cap layer already embodies. | +3 tests (`append_capped_bounds_a_single_oversized_chunk_to_the_cap`, `..._accumulates_small_chunks_until_the_cap`, `..._is_a_noop_once_already_at_cap`); full suite green. **Closes the comprehensive audit's actioned backlog — 12 repairs across 7 cycles.** |
| 2026-07-04 | **Comprehensive 6-subsystem audit — cycle 6/N: determinism + concurrency correctness — a stable Netlas JARM and a single-probe circuit breaker.** Two independent defects. **(1) Determinism.** `modules/netlas::build_entities` accumulated a host's JARM fingerprints in a `HashSet` and surfaced one via `jarm_seen.iter().next()` — but `HashSet` order is randomised per process (SipHash, random seed), so a host exposing several JARMs emitted a DIFFERENT `jarm_fingerprint` between otherwise-identical runs, breaking byte-identical output. Switched to `BTreeSet` → the lexicographically smallest fingerprint, deterministically. **(2) Concurrency.** `util::circuit_breaker::Breaker::allow` matched `Closed | HalfOpen => true`, so once an open host's cooldown elapsed and the first caller flipped it to `HalfOpen`, EVERY concurrent caller in that window also got `true` — a thundering herd on a host that is very likely still down, defeating the single-trial-probe design. `HalfOpen` now denies concurrent callers (exactly one probe in flight); `retry_at` doubles as the probe deadline (via a new `enter_half_open`), so a probe whose outcome is never recorded self-heals into a fresh probe one cooldown later instead of wedging the breaker. [determinism, concurrency, code+tests] | Byte-identical output is a core invariant of an evidentiary tool — two runs of the same scan must produce the same dossier/export, or a reviewer can't tell a real change from serialization noise; a process-randomised JARM silently violated it. And a recovery breaker that admits a herd instead of one probe re-hammers a rate-limited/downed host exactly when it's most fragile, burning the mobile-radio budget the breaker exists to protect. Both restore invariants the codebase already holds elsewhere (deterministic ordered collections; the engine breaker's one-trip discipline). | +2 tests (`build_entities_emits_a_deterministic_jarm_fingerprint`: 3 out-of-order JARMs → the smallest; `half_open_admits_exactly_one_probe_and_self_heals_a_lost_outcome`) + the existing `after_cooldown_allows_once_and_goes_half_open` updated to assert the 2nd concurrent caller is denied; full suite green |
| 2026-07-04 | **Comprehensive 6-subsystem audit — cycle 5/N: export completeness — the dossier no longer silently drops whole entity kinds, and GEXF escapes two more injection points.** **(1) Dossier.** `cli/scan/dossier.rs::print_dossier` iterated a FIXED `kind_order` allowlist and `continue`d past anything not in it, so an entity whose kind wasn't listed NEVER printed — four real `EntityKind`s (`cidr`, `ssid`, `tracking_id`, `crypto_address`) and every `other:<custom>` were omitted from the operator's primary human-readable output, hiding collected intel (a leaked crypto wallet, a captured Wi-Fi SSID, a tracking-pixel id). Extracted a pure `order_dossier_kinds(by_kind)` that renders the curated kinds first, then EVERY remaining present kind in deterministic (BTreeMap key) order — nothing dropped, output still stable; added the four missing section headers. **(2) GEXF.** `core/gexf/mod.rs` wrote the node `kind` attvalue (`write_node`) and the `<description>` scan id (`write_preamble`) UNESCAPED, while the node label, tags, and edge labels were already escaped. An `Other(<custom>)` kind is data-derived and can carry `<`/`&`/`"`, which would break that attribute and thus the whole `.gexf` in Gephi; both now pass through `xml_escape`. [completeness, export, code+tests] | The dossier is the operator's primary human-readable product; silently omitting an entire class of finding is a completeness failure exactly as corrupting one would be — an analyst reading it would never know a leaked wallet or captured SSID was collected. And a single metachar in one data-derived kind making the ENTIRE graph export unparseable is a brittle-serialization failure the rest of the escaper already avoids. Both restore the "no silent drop / never corrupt on hostile data" discipline the other exports (JSON/CSV, the escaped GEXF fields) already embody. | +2 tests (`dossier_renders_every_present_kind_never_dropping_one`: every present kind incl. `other:passport` appears, curated order preserved; `gexf_escapes_the_kind_attribute_and_the_scan_id`); the existing GEXF golden byte-stable test proves metachar-free output is unchanged; full suite green |
| 2026-07-04 | **Comprehensive 6-subsystem audit — cycle 4/N: engine finalise/dispatch robustness — a panicking rule can't abort a scan, and a cache replay can't reset the circuit breaker.** Two robustness gaps in the same finalise/dispatch flow, both making a scan degrade gracefully rather than silently lose work. **(1) Panic containment parity.** The live incremental correlation pass already wraps `correlate_entities` in `catch_unwind`, but the authoritative finalise-time `Correlator::run` ran UNGUARDED — so a rule panicking on adversarial persisted data (a slice-index bug over a crafted entity) would unwind the entire finalise block, losing the terminal `ScanComplete` event AND the API-key pool the scan harvested (the key-pool save, WAL checkpoint, and events-prune all run after the correlator in `finalise_scan`). Lifted the guard into a pure `guarded_finalise_correlation` returning `Option` (`None` on error/panic → skip emission, still finalise). **(2) Breaker honesty on cache replay.** `finalise_module_result` recorded a circuit-breaker `record_success` on every `Ok(Ok(_))`, including inter-scan cache REPLAYS (which make no provider call) — so a replay cleared a soft-failure streak the live calls legitimately earned this scan, masking a degrading provider and keeping a dead source in rotation. A `from_cache` flag makes replays invisible to the breaker (neither success nor failure); the 3 cache-hit call sites pass `true`, the 3 real-dispatch sites `false`. [robustness, engine, code+tests] | An OSINT tool an agency runs unattended must never lose a completed scan's intel to a single bad rule, nor let stale cached data mask a provider that has actually died mid-run — both are silent failures that corrupt the operator's picture (a lost dossier reads as "nothing found"; a masked-dead provider reads as "still healthy" while it burns dispatch slots the working sources need). Closing the panic asymmetry with the live pass, and making a replay breaker-invisible, are the same fail-safe discipline the codebase already applies elsewhere, extended to the two finalise/dispatch spots that lacked it. | +2 tests (`finalise_correlation_pass_survives_a_panicking_rule`: a panicking closure is caught not unwound; `cache_replay_does_not_feed_the_circuit_breaker_success_path`: streak survives a replay so the 3rd real failure trips, while a real dispatch clears it) — both fail-before/pass-after against pure/testable seams; full suite green |
| 2026-07-04 | **Comprehensive 6-subsystem audit — cycle 3/N: no-fabrication gates on three breach/stealer pools — no phantom subject-exposure claims.** Three intelligence sources minted subject-attributed findings without proving the record identified the subject — the false-positive class the shared `TargetMatch` quarantine and `oathnet_pro::breach_parent_entity` already guard against. **DeHashed**: `build_breach_entity` pushed a 0.88 `breach` "breach-presence headline" onto the engine's pre-seeded subject anchor from ANY non-empty response, so a broad `name:` query (which returns same-name STRANGERS) merged a false breach hit + a stranger-laden aggregate. Now returns `Option`, mirroring `breach_parent_entity`: the loose `name` selector requires ≥1 target-matching row (and counts/aggregates over those rows only), while the identity-exact selectors (`email`/`username`/`phone`/`ip`/`domain`) match `value` exactly so the server `total` stays honest. **IntelX**: `username`/`full_name` run as an UNSCOPED text search (a hit = a document merely contains the term), yet a `leaks` bucket stamped `breach` + `password-at-risk` on the subject — fabricating a credential-exposure claim from a stranger's paste; new pure `exposure_tags` withholds the strong exposure tags for text searches (neutral `intelx-source:*` only), rides at 0.55 vs 0.86, and marks `intelx-text-match`. **HudsonRock**: admitted any dotted string as a victim-device IP (`!ip.contains('.')`), so a stealer log's LAN address (RFC1918/loopback/CGNAT) — or a non-IP like `unknown.host` — became a `geolocation-lead` fed to GEOINT (and every IPv6 was wrongly dropped); extracted pure `victim_ip_entities` gating each candidate on `is_public_ip`. [precision, no-fabrication, code+tests] | False positives are worse than missing coverage in an evidentiary OSINT tool: a phantom "you appear in N breaches" headline off a same-name page, a stranger's leaked paste tagged as the subject's `password-at-risk`, or a private IP geolocating to nowhere are precisely the fabricated conclusions an intelligence product must never emit onto the subject's dossier. Each pool now proves the subject (or the selector's exactness) before asserting exposure — the same gate `oathnet_pro`/`see_know`/`dehashed`-records already applied, extended to the three that lacked it. | +3 tests (`name_headline_is_gated_on_a_real_subject_match`, `text_search_withholds_the_strong_exposure_tags`, `victim_ips_only_admit_routable_public_addresses`), each pinning FP-gone + TP-survives; every fix a pure unit-tested helper; full suite green |
| 2026-07-04 | **Comprehensive 6-subsystem audit — cycle 2/N: serve-layer hardening — CSRF on every mutating endpoint, loopback-only debug logs, and a real autonomous-sweep de-dup.** Web/API audit surfaced three defects. (1) **CSRF:** every bodyless state-changing `POST` — `/update/trigger` (a binary self-update that `exec()`s the new build), `/scan/auto`, `/scan/auto/sweep`, `/scans/{id}/cancel|rerun`, `/radar`, `/radar/live` — was a CORS *simple request* (no preflight, no token), so a page open in the operator's browser could drive them cross-site; only `scans/import` (a JSON body → non-simple content-type) happened to be protected. A `enforce_csrf` middleware now requires an `X-HSE-CSRF` header on every mutating method (`POST`/`PUT`/`DELETE`/`PATCH`) across `/api`; the SPA injects it transparently via a global `fetch` wrapper and CLI/API clients send `-H 'X-HSE-CSRF: 1'`. (2) **Debug logs:** `GET /api/v1/logs` streams the TRACE ring buffer (scan targets + discovered PII) but lacked the `is_loopback()` peer gate its sibling key-pool/settings endpoints carry — added via `ConnectInfo`, so a LAN bind no longer serves it. (3) `scan_auto_sweep`'s per-target de-dup keyed on the per-call-unique `scan_id` (a silent no-op — nothing was ever skipped), so one sweep spawned duplicate scans for a target reachable by several search engines; it now de-dups on the `(kind, value)` target identity. [security, web, code+tests] | For a tool an agency runs on a phone with the UI bound locally, a same-browser CSRF that fires the self-update endpoint is a remote-code-execution pivot, and one that fires `/scan/auto` silently burns metered API budget and mobile data on an attacker-chosen target — both cross the "outward-facing state change with no origin proof" line this codebase already guards for the import path. Serving the raw TRACE log (which contains the very PII the tool exists to find) to any LAN peer is a direct subject-data leak. The de-dup fix stops a single sweep from double-spending the key pool on the same target. | +1 test (`bodyless_mutating_post_requires_csrf_header`: `/scans/{id}/cancel` → 403 without header, ≠403 with) fail-before/pass-after; `logs_endpoint...` extended with a non-loopback 192.168.1.50 → 403 assertion; existing `scan_import_requires_csrf_header` still green; every mutating test sends the header; 90-test api suite + full suite green |
| 2026-07-04 | **Comprehensive 6-subsystem audit — cycle 1/N: `raw_archive` inter-scan cache now bounded.** A parallel critical audit of web/API, export, engine, storage, HTTP, and 10 module parsers surfaced ~20 real defects (most subsystems verified sound). First repair: `raw_archive` deleted no rows — expired entries were ignored on lookup but never removed, with no row cap — so a long-lived process scanning many distinct targets grew the DB/WAL unbounded on a phone. Added `prune_raw_archive` (delete past-TTL + cap newest `RAW_ARCHIVE_MAX_ROWS`), wired at startup + each scan boundary beside `prune_events`. Also corrected a doc claiming a non-existent index. [reliability, storage, code+tests] | Unbounded disk growth is a silent operational failure for an on-device tool an agency runs unattended for weeks — the DB eventually exhausts the phone's storage and every write fails. Bounding the cache (best-effort, so eviction only costs a re-query) makes retention predictable, matching the guarantee `events` already had. | +1 test (`prune_deletes_expired_rows_and_caps_to_newest`, timing-independent); 81 storage tests + full suite green |
| 2026-07-04 | **MITRE ATT&CK mapping precision — full 160-module audit; a missing technique added and a wrong one dropped.** Reviewed every module's `attack_techniques()` against its actual collection behaviour. `dns_intel` brute-forces subdomains from a 146-label wordlist but declared only passive DNS (T1590.002) — added the missing **T1595.003 Wordlist Scanning** to the catalogue and mapped it. `opencellid` claimed DNS/Passive-DNS (T1596.001) but queries a cell-tower database with no DNS call — dropped it, leaving the honest T1591.001 + T1596. Both guard-pinned; the layer's other 108 overrides were verified precise, and debatable conventions were deliberately left. [precision, MITRE, code+tests] | The inline per-finding `attack:` tag is HSE's only, operator-endorsed MITRE surface, so the module→technique map's precision IS the product's ATT&CK fidelity. An analyst reading technique coverage off the tags would otherwise miss that HSE performs active wordlist scanning, and would mis-file a cell-tower geolocation as a DNS query — both distortions of the tool's (and a tracked adversary's) real reconnaissance footprint. | guard `attack_overrides_..._precisely` pins both corrections (+ contains/!contains cross-checks); `active_scanning_family_is_catalogued` pins T1595.003; catalogue-drift + full suite green |
| 2026-07-04 | **Reliability — durable, concurrency-safe atomic writes; the API-key vault no longer risks silent total key loss.** Two storage-layer gaps: `atomic_file::write` fsynced the temp file's data but never the parent directory, so a crash right after `rename` could lose it (ext4/f2fs) — now the parent dir is fsynced (best-effort) after every rename. And the `~/.huntsman.env` vault was hand-rolled with a FIXED temp (`env.tmp`), so two concurrent writers (overlapping scans harvesting keys, a mid-scan `PUT`) could interleave and rename a corrupt/empty file over it (read as "no keys"); it now writes through the unique-temp `atomic_file::write`, inheriting the durability fix and single-sourcing the logic. [robustness, reliability, code+tests] | The API-key vault is the crown-jewel persisted file — losing it silently drops every operator-provisioned and harvested key, degrading the whole tool to keyless-only mid-operation with no error. Crash-durability and concurrency-safety on it (and on every store) is table-stakes reliability for a tool an agency runs unattended; a torn write is a silent capability outage. | +1 test (`concurrent_vault_writes_never_corrupt_or_strand`, 8 threads) fail-before (fixed-temp corrupts/strands) / pass-after; all existing key-vault round-trip tests exercise the new path; full suite green |
| 2026-07-04 | **Financial-intel precision + de-dup — IBAN validation now enforces the ISO 13616 registered per-country length, single-sourced.** `extract::ibans` and the duplicated `oathnet_pro::validate::iban_is_valid` both accepted any mod-97-valid string of length 15–34 regardless of the country's fixed IBAN length (GB 22, DE 22, FR 27…), so a checksum-lucky wrong-length string minted a phantom leaked bank account. A single `util::extract::iban_is_valid` now pins layout + registered length (`iban_country_length` table; unregistered codes fall back to the spec range) + mod-97; `oathnet_pro` delegates. [precision, parse, code+tests] | A leaked bank account is a high-value, actionable finding an analyst may act on directly — emitting one that is merely checksum-lucky (wrong length for its country) is a fabricated financial identifier, the exact false-positive class this evidentiary tool must never produce. Enforcing the registered length turns "passes the checksum" into "is a real account of a real country," and single-sourcing stops the two copies drifting again. | +1 test (`iban_is_valid_enforces_registered_country_length`) constructing a mod-97-valid wrong-length GB string (checksum passes, length gate rejects — fail-before/pass-after); extract + 194 oathnet_pro + full suite green |
| 2026-07-04 | **GEOINT precision — AU state attribution rebuilt from overlapping first-match boxes to a border-accurate partition.** `au_state_for_coords` returned the first of several *overlapping* rectangular state boxes a point fell in, so towns in the QLD∩NSW band (Lismore, Goondiwindi) read as QLD and towns in the NSW∩VIC band (Shepparton, Wodonga) read as NSW. Replaced with a partition on Australia's real borders — exact `129/138/141°E` meridians + `26°S` parallel, and piecewise-linear fits to the QLD│NSW (29°S→Point Danger) and NSW│VIC (Murray + Cape Howe) lines. Validated by a 40-town fixture, incl. river-twin pairs (Mildura/Wentworth, Albury/Wodonga) split correctly. [precision, GEOINT, code+tests] | Jurisdiction is a first-class OSINT attribute for an AU-focused tool: a wrong state mis-routes the `qld_cadastre` gate, mislabels every IP/cell/WiFi/address geo tag, and corrupts the AU-state cross-checks the correlator runs (a NSW subject reported as QLD is a wrong analytic conclusion). The border fit turns an arbitrary box tie-break into a measured answer that even splits towns a few km apart across the Murray. | +1 test (`au_state_for_coords_is_border_accurate_across_states`, 40 towns) fail-before (Lismore→QLD, Shepparton→NSW) / pass-after; geo unit + doctest + full suite green |
| 2026-07-04 | **Parse-layer false-positive precision — `extract::macs` no longer carves a 48-bit MAC out of a longer EUI-64 run.** Same `util::extract` FP-at-source audit: `MAC_RE`'s `\b` anchoring is satisfied by the separator after the 6th octet, so an 8-octet `aa:bb:cc:dd:ee:ff:00:11` (or hyphen form) yielded a phantom `aa:bb:cc:dd:ee:ff`. Since Rust's regex has no look-around, `macs` now post-filters: a match flanked by `<sep><hex>` (an octet immediately before or after) is a fragment of a longer identifier and is dropped. Genuine standalone MACs — including punctuation-wrapped — still extract. [precision, parse, code+tests] | A fabricated MAC is not a harmless dedup issue: `mylnikov`/`wigle` geolocate a BSSID, so a phantom MAC carved from an unrelated 64-bit identifier injects a false router-location signal into the GEOINT fusion — a fabricated position in an evidentiary product. Dropping it at the extractor stops the phantom before it can seed a coordinate. | +1 test (`macs_does_not_carve_a_48bit_mac_out_of_a_longer_eui64_run`) pinning both 8-octet forms to empty + the wrapped true positive; 4268 lib + all integration tests green |
| 2026-07-04 | **Parse-layer false-positive precision — email admission gates no longer out-admit the canonical matcher.** A grounded audit of `util::extract` found the two non-regex email acceptors more permissive than `EMAIL_RE` (which requires `…\.[A-Za-z]{2,}`): `looks_like_email` (the provider-`email`-field gate) only checked `host.contains('.')`, and `page_emails` (the HTML byte-scanner) only `contains('.') && len>3`. So `admin@10.0.0.1` (IP literal), `user@host.123` (numeric TLD), `user@host.c` (1-char TLD) and `x@sub..example.com` (double-dot) each minted a bogus `Email` entity. Both paths now share one `host_has_alpha_tld` helper enforcing the regex's exact domain rule (≥1 dot, no empty label, final label ≥2 ASCII letters). No valid address newly rejected. [precision, parse, code+tests] | False positives originate at the parse layer and compound through every downstream correlation — a garbage `Email` entity can seed permutations, fire co-location/reuse rules, and pad the exposure index. Single-sourcing the domain check so a gate can never out-admit the free-text scanner it mirrors closes the class at the source, where the doctrine says precision matters most. | +1 test (`page_emails_rejects_ip_literal_and_numeric_tld_domains`) and 5 new junk cases + an EMAIL_RE-agreement cross-check in `looks_like_email_rejects_provider_field_junk`; full suite green |
| 2026-07-04 | **Correlator false-positive precision — two rules stopped forging identity links from non-personal selectors.** A grounded audit of the association/co-location rule family (confirmed against current code) found two rules missing a precision gate they should have inherited. **AU-018** (email↔physical-location) admitted role/provider mailboxes (`abuse@`, `noreply@`, `registrar@`) — a WHOIS/RDAP registrant desk co-located with the subject's address as "identity-location linkage," the exact FP AU-001/AU-045 were patched for — now gated on the existing `core::validation::is_role_mailbox`. **AU-050** (shared-phone associate cluster) grouped persons on any shared number with no line-type check, so a shared `1800`/`1300`/`13`/`190x` business/service desk linked unrelated people as "associates" — now skips groups whose key classifies as `AuLineType::is_business_service` via the existing `au_phone_line_type`. Both reuse existing vocabulary (no new predicates); personal lines/real identities still link (no false negatives). [precision, correlator, code+tests] | False positives are worse than missing coverage in an evidentiary OSINT tool: a role mailbox falsely placing a person at an address, or a shared 1800 number falsely making strangers "associates; a direct pivot to reach the subject," is exactly the fabricated-link class an intelligence product must never emit. Both were real FP surfaces the same-selector-hygiene gates elsewhere already guard against. | +2 tests (`au018_excludes_role_mailboxes_...`, `au050_excludes_shared_business_and_service_lines`), each pinning FP-gone + TP-survives; 406 correlator + 30 arch guards green |
| 2026-07-04 | **MITRE mapping precision — `subdomain_takeover` remapped from passive `T1590.001` (Domain Properties) to the new `T1595.002` (Active Scanning: Vulnerability Scanning).** The module actively resolves a dangling CNAME + HTTP-probes to prove a claimable cloud resource — an exploitable misconfiguration emitted as a `vulnerable` Domain — so labelling it passive domain-property gathering mis-described every finding's inline `attack:` provenance tag (HSE's only, operator-endorsed, MITRE surface — the separate coverage/Navigator report was deliberately retired in cycles 49/52 and was NOT rebuilt). Added `T1595.002` to the `core::attack` catalogue (id-sorted) and remapped the module, mirroring `portscan`. Audit confirmed the other active modules (`dns_axfr`, `waf_detect`, `api_key_probe`, `portscan`) are already precise — one real mislabel, not a sweep. `T1590.001` stays live via `typosquat` + the DnsRecon default. [precision, MITRE, code+tests] | Correct ATT&CK attribution is evidentiary integrity: an analyst reading Reconnaissance-technique coverage off HSE's tags would otherwise mis-file an active vulnerability probe as passive metadata collection — understating the active-scanning footprint the tool (and a tracked adversary emulating it) actually has. | guard `attack_overrides_..._precisely` re-pinned + new `active_scanning_family_is_catalogued`; gate green |
| 2026-07-01 | **Fourth stale note this session: `hse update --check` already prints commit subject lines, not just a count.** `SOLUTION_TREE`'s SOL-UPDATE node and its twin §4a entry both still claimed `--check` shows a bare commit count; `cli/update.rs::changelog_lines` already runs `git log --oneline HEAD..@{u}` and `cmd_update` already prints up to 20 lines under the count. Unlike the AU-084 correction, this repository's history begins at one root commit (`770df4c9`, 857 files / 244,800 lines, no parent — an import boundary, not incremental authorship), so no specific delivery cycle could honestly be attributed via `git log`; worded the correction to say so rather than imply a dated delivery the evidence doesn't support. Genuine residual noted rather than dropped: `changelog_lines`/`commits_behind` have no test against real `git` subprocess behaviour (`tempfile`, already a dev-dep, would support a local-repo-pair fixture) — left as its own smaller follow-on. No code change. [autonomous-loop, S→P audit, docs-only] | **A fourth confirmed instance of the same pattern in one session** (cycle-20 `securitytrails`/`bgpview`/`ripestat`, the C5 "provenance radius" audit, and the AU-084 cell-tower audit) — the gap-analysis sections drift from reality faster than they get re-checked. Getting the attribution honest matters as much as getting the "is it done" verdict right: a false "delivered on date X" claim would itself become a new inaccuracy for a future cycle to un-learn. | n/a (docs) |
| 2026-07-01 | **`SOLUTION_TREE` §4a doc correction — the "AU-060-candidate" cell-tower cross-validation gap was already delivered a day earlier, as AU-084, under a reassigned number.** Logged as an open S→P gap at cycle 20 (`opencellid` × `cell_intel` `DeviceId` cross-validation, no rule cross-validated them); shipped 2026-06-30 (`770df4c9`) as `rule_au_084_cell_tower_dual_source` — registered in the correlator dispatch table with 4 dedicated tests. The originally-proposed number, AU-060, was independently reassigned to an unrelated rule (transitive identity closure) in the interim, so the note was doubly stale. No code change; verified against the shipped rule, its dispatch registration, its tests, and `git log -S` for the delivery commit — not by inference. [autonomous-loop, S→P audit, docs-only] | **The third stale gap-analysis note found this session** (after the cycle-20 `securitytrails`/`bgpview`/`ripestat` audit and the C5 "provenance radius" audit two cycles ago) — confirms the trees' own gap-analysis sections need periodic re-verification against the live code, not just cross-checked against each other, or a future cycle (including this loop's own next invocation) risks re-implementing something that already shipped, or worse, trusting a rule-ID reference that now points at something unrelated. | n/a (docs) |
| 2026-07-01 | **Removed two more dead `optimization_hints` conditions inside `analyse()` itself, found by re-reading the whole block T2.13's bug lived in (`PROBLEM_TREE` T2.13 addendum + new T2.14).** Same unreachable `entities_emitted == 0` premise as the just-fixed ROI hint: a per-module "returned 0 entities" hint and a scan-level "60s + zero-yield module" hint, both provably unfireable for the identical reason. Removed as confirmed-dead rather than left implying a capability that cannot exist. Deliberately NOT mechanically restored — `analyse()`'s pure signature can't reach the event data a correct fix needs without either a 16-call/test-site change or per-caller duplication, and the per-module hint has a genuine unresolved noise problem: a real 42-module scan (the same one used to prove the ROI-hint fix) shows most dispatched modules land at zero yield for any given target kind, so firing one line per module would flood the hints list rather than inform it. Opened T2.14 / SOL-HINT-NOISE with the concrete reinstatement options instead of forcing one under time pressure. Renamed the one test whose name overclaimed its coverage. [autonomous-loop, T2.13-addendum, T2.14, discovery-pass] | **Code that claims a capability it structurally cannot have is worse than code that's silently absent — it actively misleads the next reader (including a future autonomous cycle) into trusting a signal that was never real.** Removing confirmed-dead branches is the same "no dead fields" doctrine this codebase already enforces for constants and modules, applied to hint logic. Deferring the per-module reinstatement rather than blindly restoring it prevents trading one bug (a silent hint) for a worse one (a flooded, ignorable hints list on every multi-module scan) — precision over noise is the same principle the ROI hint's own `KeyGated`/`Paid` filter already embodies. | 4263 ✅ |
| 2026-07-01 | **Fixed the dossier's "ROI" wasted-spend hint — structurally dead since it was written, now verified live (`PROBLEM_TREE` T2.13, new).** `cli/scan/dossier.rs` filtered `ScanDiagnostics::modules_by_yield` for `entities_emitted == 0` to warn about `KeyGated`/`Paid` modules that spent a call for nothing — but that list is built *only* from emitted entities' evidence, so a module that ran and found nothing is absent, never present-at-zero. The filter could never match, on any scan, ever. New pure `zero_yield_keyed_or_paid_modules` reads the scan's own durable `ModuleDone { module, found }` events instead. Verified empirically, not just by inspection: a real `hse scan --output dossier` against a live domain, run before the fix, dispatched 42 modules (11 `KeyGated`/`Paid`, several timed out) and the ROI line never printed; the identical scan re-run after the fix correctly named all 11. 4 new unit tests. [autonomous-loop, T2.13, discovery-pass] | **Restores a wasted-spend warning an operator on a metered plan or Termux mobile radio depends on.** The hint exists specifically to tell the operator "you spent a budgeted paid/keyed API call and got nothing — exclude it next scan" — exactly the kind of ROI signal that matters when API budgets and mobile data are both scarce. It had silently never worked, on every scan since it was written, with nothing in the test suite positioned to catch it (no test exercised the *live* dossier text path). Found via this cycle's code-grounded discovery pass after the smaller, safer open items were exhausted or blocked. | 4263 ✅ |
| 2026-07-01 | **AU-059's location fix upgraded from a plain confidence-weighted centroid to the outlier-robust Weiszfeld geometric median (`PROBLEM_TREE` C5).** `au059_synergy_fix` — the function behind the dossier's headline "Best location estimate" line — now calls `util::geometry::weighted_geometric_median`, falling back to `weighted_centroid` only on the rare non-convergent input, matching the fallback idiom already used by AU-057 and `diagnostics::cluster_coordinates`. New regression test `au059_synergy_fix_resists_a_single_high_confidence_outlier` proves the difference: 2 agreeing near-Sydney classes (64% of confidence-weighted mass) vs. 1 higher-confidence Perth outlier (36%, below the median's 50% breakdown point) — the geometric median stays anchored near Sydney (lon>145) while the plain centroid the old code computed lands a third of the way to Perth (lon≈138.6, computed inline in the same test for direct comparison). Fails against the pre-fix code and passes against the fix. [autonomous-loop, C5] | **Closes the one real gap the previous cycle's docs audit surfaced.** A single disagreeing sighting — even a plausible, high-confidence one from a legitimate but wrong source — should not be allowed to drag the operator's headline "where is this person" answer proportionally to its raw confidence; a true majority of independent, spatially-agreeing sources should win. This is exactly the failure mode a real dossier can't afford: the previous linear-average estimator gave a single well-corroborated-looking outlier undue leverage over the answer an LE/defence reviewer reads first. AU-057 and the spatial-clustering diagnostics already used the more robust estimator; AU-059 — the one actually surfaced to the operator — did not, until now. | 4259 ✅ |
| 2026-07-01 | **`PROBLEM_TREE`/`SOLUTION_TREE` C5 audit correction — "provenance radius output" was already shipped, twice, and the node text never caught up.** Cycle 29 (2026-06-20) added the confidence radius to AU-059's synergy fix and its own `SOLUTION_TREE` log said so outright, but the C5/SOL-GEOINT node text was never edited to drop the item from its own "remaining" list; `d1507539` (2026-06-26) then shipped `best_au_location_estimate` (a single-signal fallback so every AU-located scan gets a headline fix, not just the multi-source case) with a `CHANGELOG.md` entry that was never cross-referenced into either tree. Corrected both trees in place with full commit provenance; the genuinely-open legs (AU-059 using `weighted_centroid` instead of the more robust `weighted_geometric_median` already proven in AU-057/`cluster_coordinates`, AU bounding precision, movement/timeline geo, cell-DB auto-sync) are kept unchanged. No code change — verified by re-reading `rules::location::mod.rs`, `cli/scan/dossier.rs`, and `api::scan_export::extract_au_location_fix` directly, not by trusting the tree's own claim. [autonomous-loop, S→P audit, docs-only] | **Keeps the living documents honest as the actual source of "what's left."** This loop's own protocol treats the trees as the single source of truth for what to build next; a stale "remaining" note books phantom future work (or worse, a future cycle re-implementing something that already shipped) exactly as surely as a missed defect books a false sense of completeness. Same class of correction as the cycle-20 `securitytrails`/`bgpview`/`ripestat` stale-note audit — grounded in git history (`ac9114e4`, `d1507539`) and the live code, not speculation. | n/a (docs) |
| 2026-07-01 | **Concurrent dispatch no longer over-dispatches a whole target's module set past `max_entities` (`PROBLEM_TREE` T2.11 LOW).** `dispatch_target_concurrent`'s Phase-2 spawn loop judged the entity-budget cap against the count from before this target's spawn loop began, because finalising a joined module's entities only happened in the trailing blocking drain, run AFTER every accepting module had already been spawned. Fixed with a non-blocking `JoinSet::try_join_next` drain at the top of every spawn-loop iteration (sharing a new `absorb_dispatch_outcome` helper with the trailing drain, so a result is finalised exactly once) — the cap now reads a live count. [autonomous-loop, T2.11] | **Restores budget parity between the sequential and concurrent dispatch paths.** The sequential path already re-checked `entity_map.len()` before every module; the concurrent path only checked it once per target, so a `max_entities`-capped scan run with `max_concurrent > 0` could dispatch an entire extra target's worth of modules (real network calls, real budget) past the operator's stated cap — worse the more modules a target's kind accepts. Regression test `concurrent_dispatch_stops_near_max_entities_not_after_the_full_module_set` forces the interleave deterministically (`max_concurrent: 1`) and is proven to fail on the pre-fix code (all 10 of 10 modules dispatch) and pass on the fix. | 4258 ✅ |
| 2026-06-24 | **`comb_search` — free, keyless emulation of SeekNow's stealer/credential corpus (real public source, no API).** New free Breach module (pri 129) querying ProxyNova's public COMB index (`api.proxynova.com/comb`) — genuine leaked `identity:secret` lines, no key, no mock. Accepts Email/Username/Domain. The correctness core is [`line_matches_target`]: COMB matches the query as a **substring** (a query for `qwerty-zzz@nope.invalid` returns `qwerty-zzz@bk.ru:…`), so every returned line is strictly post-filtered to the EXACT identity (full email / exact host / exact local-part) before any entity is minted; username matches are candidate-quarantined; secret-echoes (`user@x:user@x`) and capture sentinels (`[fail]`) are dropped via `classify_credential_field`. Live integration test (ignored) verified end-to-end against the real endpoint: 79 entities for `example.com` (75 exact-host accounts + 3 passwords), zero stranger attribution. The paid `see_know` API path is untouched (kept as the optional, key-gated counterpart). [operator-directive, free-source emulation, no-API] | **Delivers SeekNow's most distinctive data — leaked credentials — without its paid API or any anti-bot circumvention.** The operator asked to emulate SeekNow with legitimate real execution, no mock. The provider's own free frontend (`see-know.icu`) sits behind a Cloudflare JS challenge — solving it would be brittle evasion and is correctly out of scope; so the emulation uses a real, keyless public breach index instead. The substring-exactness filter is what makes this safe for an LE/defence dossier: it never attributes a stranger's password to the subject (the exact failure a naïve COMB wrapper would commit). Free-tier, offline-deterministic parsing, single bounded request — Termux-safe. | 3789 ✅ |
| 2026-06-24 | **SeekNow dynamic quota probe + highest-tier session cap.** (1) `query_credits(key)` added to `util::see_know::endpoints` — queries `GET /api/v1/credits` without consuming a budget slot, parses four observed response shapes (`credits_remaining`, `remaining`, `data.credits_remaining`, `credits.remaining`). (2) `should_probe_quota()` / `scale_scan_cap_from_daily(limit)` added to `budget.rs`; probe fires once per scan (latch resets with the per-scan budget). (3) `SeekNow::process()` calls the probe before any budget spend on the first seed of each scan; if the daily limit is known, scan cap is set to `clamp(limit / 20, 300, 2500)`. (4) Session cap raised from 4,500 → 100,000 — the server's own daily quota is the real backstop, not a local ceiling. [operator-directive, quota-utilisation] | **Auto-scales to the operator's actual plan.** The operator is on the highest SeekNow tier. Hardcoding 5,000/day from the `premiumhq` plan was wrong — the real daily limit is whatever the account's plan provides, discovered from the API itself at runtime. A highest-tier plan returning, say, 50,000 daily lookups now yields a scan cap of 2,500/round (vs. the previous 300) — 8× more pivots per scan without any manual env-var tuning. The probe falls back gracefully if the `/credits` endpoint is unreachable (static 300 default survives). HUNTSMAN_SEEKNOW_SCAN_CAP and ScanOptions::seeknow_scan_cap always take precedence over the dynamic value. | 3781 ✅ |
| 2026-06-24 | **SeekNow maximisation: scan cap 160→300, session cap 500→4500, full endpoint matrix enabled.** (1) `BUDGET` defaults raised to 300/scan + 4500/session (90% of the 5,000-daily pool, ~15 full scans per session). (2) The `FREE_COVERED_SINGLE_ORIGIN` filter removed from `effective_plan` — github/twitter/reddit/tiktok/roblox/xbox/minecraft endpoints now fire on username seeds alongside `SocialAggregate` and `UsernameHistory`. (3) Per-endpoint yield tracing added (`tracing::debug!` on every non-empty endpoint hit, keyed by endpoint + target kind) so live logs now identify which endpoints return data for each seed kind. Tests updated to reflect the maximisation policy: `default_scan_cap_is_at_least_maximise_floor` (≥200, was `(120..=200)`), `effective_plan_fires_the_full_username_matrix` (was `effective_plan_drops_free_covered_single_origin_endpoints`). [operator-directive, quota-utilisation] | **Uses SeekNow at the scale its quota allows.** Session cap of 500 meant HSE used ≤10% of the 5,000/day pool regardless of how many scans were run; the single-origin endpoint filter meant a username scan spent only 3 paid calls (search + social + history) instead of the 10 its endpoint matrix supports. The operator's directive is explicit: SeekNow is "basically Oathnet but with many thousands of times more daily queries — utilised fully". Platform-specific endpoints (e.g. `/username/github`) return breach/stealer context tied to THAT platform account — not just presence confirmation — so they add data the free username_search stack and `SocialAggregate` don't cover. The budget cap (300 per scan) is the only rate limiter; the daily server-side quota-exhaustion response acts as the external backstop. Per-endpoint yield tracing in debug logs directly supports the operator's directive to "identify situations where SeekNow provides a distinct gain" over broad-spectrum searching. | 3781 ✅ |
| 2026-06-24 | **`search_engines` surname-family extraction de-fabricated.** (1) `extract_family_names` rejects self-doubled first-name garbage (`Fredfred`, `annaanna`) — the prior guard only caught doublings of the SUBJECT's terms. (2) Snippet-surname "family members" are now emitted candidate-tier (`demote_to_candidate`), not 0.45 PROBABLE. [live-bundle, fabricated-PII] | **Stops the module inventing relatives.** A live v1.9.0 "Matthew Diegmann" bundle minted `Fredfred Diegmann` (doubled from a radaris `/p/Fred/Diegmann/` snippet), `Diegstim Diegmann` (mangled from an Instagram handle), and global namesakes `Dominique Diegmann` (a ski-race page) / `Elaine Diegmann` (a US healthcare NPI) — all as PROBABLE `family-member`s outranking the genuine QLD-registry relatives (`family-candidate`, 0.35). A shared surname in a SERP snippet is a speculative lead, not confirmed kin: candidate-tier retains it as a lead while excluding it from the subject's confirmed footprint / correlator / exposure. | 3555 ✅ |
| 2026-06-24 | **Footprint timeline now excludes candidate-quarantined entities** (`timeline::reconstruct` skips `tags::CANDIDATE`). [live-scan, PRIVACY + precision] | **Fixes a confirmed PII leak.** A real v1.10.0 person-scan's "Footprint timeline" rendered 15 `Born …` events that ALL belonged to quarantined breach co-occurrence **strangers** (their DOB + home address + email), presented as the subject's life events — none was the subject. `reconstruct` walked every entity's date evidence with no candidate filter, so the quarantine that correctly hides those strangers from the correlator / exposure / confirmed count was simply absent from the timeline. Now excluded at the source, fixing BOTH timeline consumers (web `scan_timeline` + CLI dossier). This is the per-view candidate-consistency gap made concrete. | 3555 ✅ |
| 2026-06-23 | **§4.1 also surfaced in the human `--output dossier`** — a "Cross-scan leverage" section lists this scan's identifiers that bridge ≥ 2 prior investigations (top 8); absent on a first-ever scan (nothing bridges yet). `print_dossier` gains `leverage`; dropped its now-redundant `sid` param (== `scan.id`) to stay within the arg-count lint without an allow. [retention/enrichment] | **Completes §4.1 across both live output modes** so the operator sees the cross-investigation bridges in the normal human view, not only in scripted JSON. Same live surface (no determinism contract); only genuine bridges shown to keep a fresh-scan dossier clean. | 3554 ✅ |
| 2026-06-23 | **§4.1 surfaced in the live `--output json` scan payload** (`enrichment_leverage` field; `LeverageRanked` now `Serialize`). [retention/enrichment] | **Completes §4.1 with the CORRECT surface.** Considered the debug bundle first and rejected it: leverage is a CROSS-SCAN / time-variant view (degree grows as investigations accumulate), which would break the bundle's documented "byte-identical across time for an immutable scan" determinism contract (the back-to-back determinism test wouldn't even catch it). The live scan JSON — where `exposure` already lives — has no cross-time contract, so it's the right home; any script consuming `--output json` now gets the ranked cross-investigation bridges. Additive field, no shape test broken. | 3554 ✅ |
| 2026-06-23 | **§4.1 enrichment-leverage query** — `core::engine::rank_enrichment_leverage(store, entities, limit) -> Vec<LeverageRanked>` ranks the high-leverage retained identifiers by how many distinct investigations each bridges. [retention/enrichment, data_retention_design §4.1] | **First build step of the retention asset.** Implements "which of my retained data most empowers the rest": filters to `is_cross_scan_candidate` join keys (email/phone/crypto/distinctive-username/full-name/specific-address — never infra/coarse-geo/permutations), scores by the REALISED cross-scan degree (`observation_count`, distinct scans recording the value), no invented weighting. Read-only counterpart to `link_cross_scan_history`. Public query API awaiting its CLI/bundle consumer (same shape/precedent as P3 `low_confidence_evidence`). Pure, offline, deterministic (UID tie-break), bounded point-lookups — Termux-safe. | 3554 ✅ |
| 2026-06-23 | **`docs/data_retention_design.md`** — architecture + lawful basis for the data-retention/enrichment asset. Grounds the goal in the EXISTING cross-scan flywheel (`recall_prior_entities`, `link_cross_scan_history`/`_cooccurrence`, `promote_cross_scan_corroborated`) and shows `is_cross_scan_candidate` already encodes the "data that empowers other data" join-key priority. [strategy, OSINT value] | **Highest-value autonomous deliverable with code work exhausted.** Maps the user's retention/enrichment vision onto what HSE already builds (it's ~80% there), separates the sellable asset (first-party derived graph) from the un-resellable licensed/PII layers, front-loads the compliance guardrails the resale-of-cached-data instinct would have destroyed value on, and sequences 4 concrete next steps inside the existing rusqlite/WAL pattern (no new crates). Doc-only — no build/test impact. | n/a (docs) |
| 2026-06-23 | **CHECKPOINT — high-confidence backlog exhausted.** This loop closed the non-corroborating over-credit class (AU-010→AU-062/063→GEXF), role-mailbox/junk-handle identity gates (AU-001/045/046), a proptest-found UID-stability bug (`normalise`), the Exposure-Index-in-bundle gap, the relation-graph root-cause, and completed four curated data lists (`source_family`, `INFRA_DOMAINS`, `MEGA_DOMAINS`, `COMMON_SURNAMES`). Verified already-sound (no change): `.corroboration` unused by rules, `is_role_mailbox` scope, merge determinism (`merge_is_order_independent` proptest + 3 unit tests), `is_placeholder_username` coverage, and the `TargetKind::detect` classifier cascade + domain detection (permissive shape-matching is deliberate to avoid TLD-list staleness; `is_domain_shaped` vs `looks_like_domain`/`is_app_package_id` are two functions correctly tuned for two contexts — general input vs. package-ID-flooded stealer feeds — so unifying them would regress one side). Remaining work is qualitatively different — needs live scan data (non-Cloudflare/Fastly CDN IP ranges hold real customer hosts; unsafe to hardcode) or is product/strategy (data-retention model) or a merge-to-main decision. Loop paused on genuine-value grounds, not failure. | n/a |
| 2026-06-23 | **`COMMON_SURNAMES` extended with 15 top-~100 English/Welsh/Scottish names** (`griffiths`, `cox`, `chapman`, `reynolds`, `lloyd`, `harvey`, `owen`, `owens`, `fox`, `griffin`, `johnston`, `hamilton`, `wallace`, `fletcher`, `pearson`). [precision, false-Critical-kin] | **Completes the AU-051/AU-061 commonness gate.** `is_common` weakens a shared-surname kinship signal so a *common* surname doesn't manufacture a Critical "household of relatives". The list carried names down to ~rank 150 (`watts`, `barker`) yet omitted genuinely top-50–65 ones (`griffiths`, `cox`, `chapman`) — so two unrelated Griffiths in one metro were asserted Critical kin, the exact false-CRITICAL the AU-061 gate exists to prevent. All additions are unambiguously high-frequency (none collide with the distinctive-surname test set, incl. the subject's own `Bamford`). | 3553 ✅ |
| 2026-06-23 | **`MEGA_DOMAINS` people-search aggregator category extended** (+15: `fastpeoplesearch.com`, `thatsthem.com`, `truthfinder.com`, `instantcheckmate.com`, `clustrmaps.com`, `zoominfo.com`, `rocketreach.co`, `cyberbackgroundchecks.com`, `advancedbackgroundchecks.com`, `smartbackgroundchecks.com`, `searchpeoplefree.com`, `peoplefinders.com`, `checkpeople.com`, `fastbackgroundcheck.com`, `ussearch.com`). [scan-driven, aggregator noise] | **Dampens stranger co-occurrence noise the category exists for.** People-search sites scrape and republish each other, so one person scan surfaces *dozens* — the list's own comment notes they "flooded a real scan as stranger co-occurrence addresses." It had ~15 of the common US/AU aggregators but missed ~15 equally-common ones (FastPeopleSearch, TruthFinder, ZoomInfo, RocketReach, …); those were expanded at full weight, pulling each aggregator's stranger footprint into the graph. All are commercial aggregator services — never a subject's own domain — so suffix-dampening them is zero-risk (verified by test). | 3553 ✅ |
| 2026-06-23 | **`INFRA_DOMAINS` extended with common shared infrastructure** (Azure DNS `azure-dns.*`, Google Cloud DNS `googledomains.com`, ClouDNS/DNSimple; Azure/Edgecast/Limelight CDN; Azure/AWS app hosting `cloudapp.net`/`elasticbeanstalk.com`; Mailchimp/Postmark/Mailjet ESP; M365 `protection.outlook.com`, Barracuda, Rackspace mail). [scan-driven, infra-pollution] | **Reduces the residual infrastructure pollution at its source.** `is_infra_domain` drops incidental provider estate from EXPANSION and gates `@infra` email admission; the list missed several providers that surface constantly in real NS/MX/CNAME records, so a person scan still burned rounds mapping Azure/Google-managed DNS, CDN edges, and ESP/mail-gateway hosts. Exact-suffix match over unambiguous infrastructure (never a subject's own apex/mailbox) — zero false-positive risk, verified by test (incl. that the `protection.outlook.com` gateway suffix does NOT swallow `outlook.com` freemail, a prime finding). The IP-range analog (non-Cloudflare/Fastly CDN) stays deliberately unhandled — those ranges contain real customer hosts. | 3553 ✅ |
| 2026-06-23 | **`source_family` completed for the module registry.** Classified 23 real modules that were silently falling to `"other"`: internet scanners / IP-reputation feeds → `infra` (`securitytrails`, `onyphe`, `zoomeye`, `criminal_ip`, `netlas`, `bgpview`, `ripestat`, `abuseipdb`, `ipqs`, `portscan`, `netblock`, `domainsdb`); authoritative people/business/professional registries → `identity_registry` (`au_electoral`, `asic_director`, `gleif_lei`, `ahpra`, `acnc`, `fullcontact`, `contact_enrich`, `au_people`); `hudsonrock` → `breach`, `crates_io` → `code`, `exa_search` → `search`. [audit-driven] | **Restores the rules' cross-family recall.** `source_family` is the orthogonality measure for AU-045 / AU-062 / AU-063; with these modules unclassified, a subject genuinely confirmed across, e.g., the electoral roll AND a breach, or an asset seen by SecurityTrails AND a search engine, was under-counted as one family and the finding never fired. Every added mapping is unambiguous, so each new firing is a TRUE cross-family positive — never over-credit (genuinely ambiguous/non-family sources like `chain_intel`, `virustotal`, `threatfox`, `device_sensors`, `username_variants` are deliberately kept `other`, pinned by test). Makes the function's documented "covers the registry" contract actually hold. | 3553 ✅ |
| 2026-06-23 | **`normalise` (Domain) made idempotent when a BOM shields a control byte.** Re-trim after `strip_format_noise`, mirroring the Username arm. [proptest-found] | **UID stability — the cross-scan dedup invariant.** A property test (`normalise_is_idempotent`) shrank to `Domain "\u{feff}\u{b}¡"`: a leading BOM is not whitespace, so `value.trim()` stopped at it and left the `\u{b}` (vertical tab) behind; stripping the BOM exposed that byte, which the first pass kept (`\u{b}¡`) but a second pass trimmed (`¡`) — so the SAME host keyed to two different SHA-256 UIDs across scans, silently fragmenting one identity. The `proptest-regressions` seed already held a sibling case (`"\u{e000}\t."`), confirming the edge class; pinned now by a deterministic regression plus the re-trim fix. | 3553 ✅ |
| 2026-06-23 | **GEXF graph export: shared-evidence co-occurrence edges now key on `corroborating_sources()`, not `evidence_sources()`.** [audit-driven, same class] | **Removes false visual clusters from the Gephi/Cytoscape graph.** The co-occurrence edge means "an independent source named both entities". Keying on raw evidence wired every `name_intel`-derived candidate (the seed's own permutation engine) to every other, and every `recall`/`cross_scan_history` replay to every other, into a dense hairball that swamps the real structure an analyst opens the graph to see. Genuine co-occurrence (two values in one breach dump) is preserved; the seed-derivation lineage is still carried — correctly — by the typed `DerivedFrom` edges. Golden byte-stable test unaffected (its shared source `crtsh` is corroborating); new `gexf_excludes_non_corroborating_co_occurrence` pins it. | 3551 ✅ |
| 2026-06-23 | **AU-062 / AU-063 (multi-pathway & gap link analysis) orthogonality now counts `corroborating_sources()`, not `evidence_sources()`.** The shared `source_families` helper excluded only the `"other"` bucket, so two of the non-corroborating passes still posed as independent families: `name_intel` → `identity_registry`, `geo_normalize` → `infra`. [audit-driven, AU-010 sibling] | **Extends the AU-010 fix to the graph side.** These detectors fire only on corroboration across **≥2 orthogonal source families** — their whole precision claim. But a seed-derivation (`name_intel` mints the permuted handles/emails) and a deterministic geo-replay (`geo_normalize`) were each counted as a second family, so a link backed by `infra + a name guess` was asserted as cross-family corroborated. Proven: the new `au062_excludes_non_corroborating_source_families` test FAILS under the old `evidence_sources` form (AU-062 fired on a `name_intel`-derived family) and passes after — the same over-credit `c_effective`/AU-010 removed from the source-count side, now closed on the multipath/gap orthogonality side (also tightens the engine's multipath-corroboration promotion boost, which shares the finder). | 3550 ✅ |
| 2026-06-23 | **Relation graph `relations: 0` — root-caused (NOT a builder bug) + regression guard added.** An offline diagnostic ran all 14 `derive_*` passes over realistic person-scan shapes: a name-seeded scan anchors a graph (subject→identifier `IdentifiedBy` ×2 + alias), an email-seeded scan resolves the subject email to a breach-named Person, and orphan identifiers fabricate nothing. The builders are sound. [scan-driven] | **Correctly NOT fabricating edges.** The live bundles' empty graph is a downstream symptom of the *entity composition* — infrastructure-dominated (the same CDN/provider mass behind the 82% AU-010 pollution) and poor in person-name evidence attrs — not a defect to "fix" by inventing relationships (a false relationship is as damaging as a false CRITICAL for LE/defence). The honest deliverable is the new `person_scan_entities_anchor_a_relation_graph` guard: it pins that realistic person-scans MUST anchor a graph (catching a refactor that silently zeroes the derivers — the real failure mode) while orphans MUST NOT invent identity edges. The AU-010 fix already shrinks the dominant non-linking entity class. | 3549 ✅ |
| 2026-06-23 | **Exposure Index wired into the full dossier / debug bundle.** `render_full` now opens with the calibrated 0–100 `── EXPOSURE INDEX ──` headline + per-signal breakdown (`assess(entities, correlations)`), so every auto-saved dossier and every `--format debug` bundle carries the same verdict the live `print_dossier` shows. [scan-driven] | **Closes a live-bundle gap.** The uploaded debug bundles had the operator-facing Exposure Index in the live dossier and `report.json` but NOT in the on-disk/debug artifact — the one an analyst or reviewer actually reads back. `assess` excludes candidates + sub-floor speculation internally, so the bundle headline matches the live verdict exactly while the dossier below still lists every entity unredacted. Stays byte-deterministic (no wall-clock), so `debug_bundle_is_deterministic` holds. | 3548 ✅ |
| 2026-06-23 | **AU-045 / AU-046 (identity confirmation & cross-platform resolution) now require a real identity anchor.** Added `is_anchorable_handle` (len ≥ 4 ∧ ¬`is_generic_handle`) gating both rules' Username arms; AU-045's Email arm excludes role mailboxes (`is_role_mailbox`). Extended `is_generic_handle` with a `NON_IDENTITY_TOKENS` list (`from`, `dns`, `www`, `http`, …) so AU-034 shares the gate. [scan-driven] | **Stops junk handles becoming "confirmed identities".** A live person-scan fired AU-045 "confirmed across 2 service families" on `from` (a bare function word) and `dns` (a 3-char acronym) — parser artifacts that any breach/forum corpus echoes, not aliases — and would have keyed AU-046 resolution on them, fusing unrelated records into one person. Role mailboxes (`abuse@…`) are confirmed everywhere by definition and are an org desk, not the subject. The handle-quality gate AU-034 already enforced is now applied uniformly across the identity-cluster family, so the strongest "confirmed identity" finding can't rest on extraction noise. | 3548 ✅ |
| 2026-06-23 | **AU-001 (multi-source breach corroboration) skips role/provider mailboxes (`is_role_mailbox`).** [scan-driven] | **Kills a false CRITICAL.** A live person-scan fired AU-001 CRITICAL on `abuse@godaddy.com` "found in 2 breach sources: hibp, xposed_or_not" — but a registrar/provider role desk is in every breach list as a matter of course; it is not the subject's address or exposure. A false CRITICAL is the worst error class for LE/defence credibility. `is_role_mailbox` already gates expansion (dispatch.rs:70); now it also gates the breach Critical, per the bundle audit's "never breach-check or identity-cluster them". | 3547 ✅ |
| 2026-06-23 | **AU-010 (infra consensus) now counts `corroborating_sources()`, not `evidence_sources()`.** [scan-driven] | **Retires the #1 live-scan flaw.** Two real person-scans (508/460 entities) self-diagnosed 265/324 correlations (82%) as `AU-010` infrastructure noise — CDN/provider edge IPs "confirmed by dns_intel, doh_resolver, **recall**". `recall` is a replay of the same prior observation, not an independent source; counting only corroborating sources (the set `c_effective` uses) drops the recall-inflated edges below the 3-source bar while genuine 3-independent-source consensus still fires. Directly removes the bulk of the "infrastructure-pollution inflates correlations" finding. | 3546 ✅ |
| 2026-06-23 | **P3** — `storage::Store::low_confidence_evidence(threshold, since_seconds) -> Vec<EvidenceAnomaly>` (+ `EvidenceAnomaly { entity_uid, module_name, confidence, created_at }`, default threshold 0.3). Queries the indexed `confidence`/`observed_at` columns, resolves the module from each entity's evidence sources. | **Audit / review trail.** Surfaces the weak (Candidate-tier) findings an LE/defence reviewer must NOT yet trust — the anomaly query the audit bridge / `hse audit` consume. Tested against an in-memory WAL store (threshold + since-window). | 3545 ✅ |
| 2026-06-23 | **P1 — NOT BUILT (architecturally inapplicable).** `LlmAuditRecord` audits LLM call boundaries; HSE makes ZERO runtime LLM calls (guard `runtime_carries_no_ai_ml_inference_dependency` denylists `llm`). `temperature: f32` would be a permanently-dead field, and any inference dep would trip the guard → FAIL. Per the FAILURE RULE + "no dead fields", documented not built. | The evidentiary-reproducibility intent it reaches for is already served by `Evidence{source,recorded_at}` + ATT&CK tags + `raw_archive`. | n/a |
| 2026-06-23 | **P2 — NOT BUILT (no probability distribution exists).** Spec computes "Shannon entropy from the classifier probability distribution"; `core::classifier` is deterministic single-kind via ordered shape-gates (`TargetKind::detect`) — there is no distribution to take entropy of. Synthesising one would be pseudo-rigor, and the re-injection path carries only decisive kinds (the entropy gate would be near-dead). | Anti-wrong-attribution on autonomous pivots is instead served by the engine-expansion + correlator precision gates landed this session (AU-060 floor, surname anchors, AU-061). | n/a |
| 2026-06-23 | Made the assoc consumers multi-value-aware: `entity_residences` / `entity_phones` (AU-049/050/051) now `split("; ")` each evidence value, matching the producer convention (`with_attr` / `merge_evidence_attrs`) and `breach_pii::scan_evidence`. | **End-to-end consistency (completes the prior fix).** Verifying the prior accumulation change revealed an asymmetry: breach-PII rules split multi-value `"a; b"` attrs, but the assoc rules normalised the whole string — so two accumulated phones concatenated into one bogus digit-run key and two addresses garbled into one non-matching key. Now every distinct address/phone clusters correctly, whether multi-field (`with_attr`) or conflict-accumulated (absorb). | 3544 ✅ |
| 2026-06-23 | `merge_evidence_attrs` (the absorb path) now ACCUMULATES conflicting same-key values as sorted `"a; b"` instead of dropping the larger via min-value — aligning it with `Evidence::with_attr`'s established in-record accumulation. | **Evidence fidelity + consistency.** Two code paths handled "same key, two values" differently: the public builder kept both, the internal absorb path kept one. A conflicting re-observation (a corrected value, or a namesake's differing `date_of_birth` on the same breach source) is itself evidence the disambiguation rules (AU-073) need — silently dropping it loses an evidentiary signal. Sorted so the fold stays merge-order-independent (Determinism Requirement). | 3543 ✅ |
| 2026-06-23 | AU-061 now drops `family-candidate` Person entities whose surname differs from the subject's (when the subject surname is known) before asserting "shared surname". | **False evidentiary basis removed.** The `family-candidate` tag is also applied by the see_know household path to co-located people who do NOT share the subject's surname; AU-061 then claimed "shared surname relative" for them. A dossier used in LE/defence must not assert a basis that's untrue — different-surname co-residents are now excluded from this finding (same-region-without-shared-surname is not itself a finding). Address candidates (surname-matched by their module) are kept. | 3543 ✅ |
| 2026-06-23 | AU-061 (family geo-corroboration) now commonness-gates its Critical escalation: a COMMON subject surname caps at High (not Critical) and softens the "relatives" wording. Relocated `subject_surname` from `engine::passes` to `core::geo_family` as the single shared source. | **Wrong-attribution (Critical false kin).** Within `FAMILY_GEO_KM` (150 km) the existing namesake pass doesn't fire (it guards only >800 km), so three unrelated "Smith"s in one metro catchment were asserted Critical "household of relatives". A common surname makes shared-region co-location weak evidence — now a High lead to verify; a distinctive surname keeps the strong Critical signal. Mirrors the AU-051 / kinship commonness gates. | 3542 ✅ |
| 2026-06-23 | Closed admission-gate placeholder gaps: `is_placeholder_email_local` now rejects `test`/`redacted`/`placeholder` local-parts, and `is_placeholder_username` rejects `redacted`/`placeholder`. Updated the one smoke fixture that used `test@contoso.com` as a real target. | **Anti-garbage-promotion.** `test@gmail.com`, `redacted@…`, the `redacted` handle were admitted as real entities — polluting the graph and falsely corroborating. The admission gate doesn't consult the role-mailbox path, so these slipped through. Exact-match (separator-stripped) only, so real handles that merely contain the token (`tester`, `firstnations`, `redactedtruth`) are untouched. | 3540 ✅ |
| 2026-06-23 | Extended the invisible-noise strip to the `Username` and `Domain` normalize arms via one shared `strip_format_noise` helper (BOM/ZWSP/ZWNJ/ZWJ/WJ), borrowing on the clean path so the hot normalize path doesn't allocate. | **Identity integrity, made uniform.** The same BOM/zero-width fragmentation that hit emails also forks a `Username` (`\u{feff}@handle`) and a `Domain` (`\u{feff}example.com`) into a second UID. One helper now guarantees the three identity kinds dedup consistently — no kind is a fragmentation gap. | 3540 ✅ |
| 2026-06-23 | `core::entity::normalise` (Email) now strips invisible format/zero-width noise (BOM U+FEFF, ZWSP U+200B, ZWNJ/ZWJ/WJ) and treats control chars (NUL, …) as value terminators. | **Identity integrity / anti-fragmentation.** A BOM-prefixed or ZWSP-embedded address (common in UTF-8-BOM exports and dirty breach dumps) keyed the *same* mailbox to a *different* SHA-256 UID, splitting one person's footprint across two nodes — corrupting corroboration counts and the evidence chain. Now the clean and noisy spellings share one UID. | 3539 ✅ |
